---
phase: commit-ingest-router-auth-revert
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-backend/main.py
autonomous: true
requirements: []  # N/A — quick fix reverting an unintended CI-breaking side effect of commit 88b13d8, not a new phase requirement

estimate:
  tokens: 12000
  raw_tokens: 8000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "app.include_router(commit_ingest.router) in main.py no longer carries a dependencies=[Depends(get_current_pessoa)] kwarg — GET /projects/{project_id}/current-sprint and POST /ingest/commit are reachable without a session/cookie, matching how the docudata-sync.yml CI workflow calls them (anonymous curl)"
    - "Every other app.include_router(...) call in main.py keeps its existing dependencies kwarg exactly as before — projects, sprints, sprint_docs, ingest, generate, ingestions, search, export, enrich, funcionalidades, painel, revisao_ingest, composer, aceite_ingest, boletins, sprint_funcionalidades, operacionais, tasks, metricas, avaliacoes, pontuacao all remain gated"
  artifacts:
    - "docudata-backend/main.py — commit_ingest.router registration is a bare app.include_router(commit_ingest.router) call with no dependencies kwarg"
  key_links:
    - "The single edited line is the only call site wiring commit_ingest.router into the FastAPI app; docudata-sync.yml's anonymous curl calls to POST /ingest/commit and GET /projects/{id}/current-sprint depend on this line carrying no auth dependency"
---

<objective>
Revert commit_ingest.router to public/unauthenticated, undoing an unintended side effect of commit 88b13d8 ("feat(16-03): gate global de enforcement") that added `dependencies=[Depends(get_current_pessoa)]` to every router registration in `docudata-backend/main.py`, including `commit_ingest.router`. That router exposes `POST /ingest/commit` and `GET /projects/{project_id}/current-sprint`, both called anonymously (no session/cookie) by the GitHub Actions workflow `.github/workflows/docudata-sync.yml`. The added auth gate causes every CI sync call to fail with HTTP 401 — confirmed live in GitHub Actions logs (`curl: (22) The requested URL returned error: 401`).

Purpose: Restore the CI-to-backend sync pipeline (`docudata-sync.yml`) that anonymously reports commit metadata/diffs and reads the current sprint — this pipeline has no session to authenticate with and was working before the global auth rollout in Phase 16 inadvertently caught it.
Output: `docudata-backend/main.py` with `commit_ingest.router` registered without the `dependencies=` kwarg; every other router's auth gating untouched.
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drop the auth dependency from commit_ingest.router's registration</name>
  <files>docudata-backend/main.py</files>
  <read_first>
    - `docudata-backend/main.py` line 51 — the exact current line: `app.include_router(commit_ingest.router, dependencies=[Depends(get_current_pessoa)])`. This is the only line to change.
    - `docudata-backend/main.py` lines 42-65 — confirm no other router registration is touched; all 20 other `dependencies=[Depends(...)]` router registrations (auth, painel, operacionais, metricas, avaliacoes use `require_not_operacional`; the rest use `get_current_pessoa`; `performance.router` already has no dependency) stay exactly as they are.
  </read_first>
  <action>
In `docudata-backend/main.py`, change the single line `app.include_router(commit_ingest.router, dependencies=[Depends(get_current_pessoa)])` to `app.include_router(commit_ingest.router)` — drop the `dependencies=` kwarg entirely so this router goes back to being registered with no auth gate, exactly as it behaved before commit 88b13d8 added the global enforcement gate. Do not remove the `Depends` or `get_current_pessoa` imports at the top of the file (both remain in active use by every other router registration on lines 43-50 and 52-64, and by `require_not_operacional`-gated routers). Do not touch any other `app.include_router(...)` line, the CORS block, the lifespan scheduler, or the `/health` / `/notifications/check` / `/tasks/travamento/check` route handlers below the router registrations.
  </action>
  <verify>
    <automated>test "$(grep -c -F 'app.include_router(commit_ingest.router)' "docudata-backend/main.py")" -eq 1</automated>
    <automated>test "$(grep -c -F 'app.include_router(commit_ingest.router, dependencies=' "docudata-backend/main.py")" -eq 0</automated>
    <automated>test "$(grep -c -F 'Depends(get_current_pessoa)' "docudata-backend/main.py")" -eq 17</automated>
    <automated>cd docudata-backend && python3 -c "import ast; ast.parse(open('main.py').read())"</automated>
  </verify>
  <done>
    `docudata-backend/main.py` registers `commit_ingest.router` with no `dependencies` kwarg (bare `app.include_router(commit_ingest.router)`); every other router registration's `Depends(get_current_pessoa)` / `Depends(require_not_operacional)` gating is byte-for-byte unchanged (18 → 17 occurrences of `Depends(get_current_pessoa)`, exactly one fewer); the file remains valid, parseable Python.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions CI → docudata-backend API | `docudata-sync.yml` calls `POST /ingest/commit` and `GET /projects/{id}/current-sprint` anonymously (no session/cookie) — this boundary is now explicitly unauthenticated by design |
| Public internet → docudata-backend API | Any anonymous client can reach these two endpoints after this change, same as before commit 88b13d8 introduced the global auth gate |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-quick-01 | Elevation of Privilege | `POST /ingest/commit`, `GET /projects/{id}/current-sprint` (commit_ingest.router) | medium | accept | Intentional revert to pre-Phase-16 behavior at explicit user request — these two endpoints must stay reachable by anonymous CI. `POST /ingest/commit` only writes ingestion records scoped to a caller-supplied `project_id`/`sprint_number` (no destructive/admin action); `GET /projects/{id}/current-sprint` is read-only. Risk is the same one the project already accepted pre-Phase-16 and is explicitly reintroduced here, scoped to exactly this one router, not expanded further. |
| T-quick-02 | Information Disclosure | `GET /projects/{id}/current-sprint` | low | accept | Returns only a sprint number for a project the caller already knows the UUID of; no PII or credentials exposed. Unchanged risk profile from the pre-Phase-16 baseline. |

</threat_model>

<verification>
- `grep -c -F 'app.include_router(commit_ingest.router)' docudata-backend/main.py` returns 1
- `grep -c -F 'app.include_router(commit_ingest.router, dependencies='` returns 0
- `grep -c -F 'Depends(get_current_pessoa)' docudata-backend/main.py` returns 17 (was 18 — exactly the commit_ingest.router occurrence removed)
- `python3 -c "import ast; ast.parse(open('main.py').read())"` from `docudata-backend/` exits 0 (file still parses)
- Manual: no other line in `main.py` differs from before this plan ran (single-line diff)
</verification>

<success_criteria>
- [ ] `commit_ingest.router` is registered with no `dependencies=` kwarg in `docudata-backend/main.py`
- [ ] All other 20 router registrations keep their existing auth dependencies unchanged
- [ ] `main.py` remains syntactically valid Python
- [ ] Diff touches exactly one line
</success_criteria>

<output>
Create `.planning/quick/260905-tf7-remover-autentica-o-get-current-pessoa-d/260905-tf7-SUMMARY.md` when done
</output>
