---
phase: 15-travamento-automatico-por-tempo-trava-do-baseline-do-sprintcard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-backend/supabase_schema.sql
  - docudata-backend/models/schemas.py
  - docudata-backend/routers/tasks.py
  - docudata-backend/services/travamento_checker.py
  - docudata-backend/main.py
  - docudata-backend/tests/test_travamento_relogio.py
  - docudata-backend/tests/test_travamento_job_override.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/components/TasksKanbanTab.tsx
autonomous: true
requirements:
  - "ALERT-01"
  - "ALERT-02"
  - "ALERT-03"

estimate:
  tokens: 78000
  raw_tokens: 78000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Ao confirmar uma transição de coluna_kanban para em_andamento (de qualquer coluna, inclusive a reabertura concluida->em_andamento), a task grava entrou_em_andamento_em=agora e reseta travado_automatico=false/travado_override=false/travado_override_por=null/travado_override_em=null — ALERT-01, ALERT-02"
    - "Ao confirmar uma transição de coluna_kanban saindo de em_andamento para qualquer outra coluna, a task reseta entrou_em_andamento_em=null, travado_automatico=false, travado_override=false, travado_override_por=null, travado_override_em=null — ALERT-02"
    - "O job diário check_travamento_automatico, registrado no mesmo AsyncIOScheduler já usado por check_and_send_notifications em main.py, varre tasks com coluna_kanban=em_andamento e marca travado_automatico=true quando dias_desde(entrou_em_andamento_em) >= pontos_da_task × 2 e travado_override não é true — ALERT-01"
    - "POST /tasks/{task_id}/travado/override grava travado_override=true, travado_override_por, travado_override_em sem tocar em travado_automatico (preserva o histórico de que o sistema sinalizou); o badge de alerta em TasksKanbanTab.tsx só aparece quando travado_automatico=true E travado_override=false — ALERT-03"
    - "travado_automatico nunca é lido nem escrito por nenhum código de score/SPI/pontuação — comentário inline em travamento_checker.py documenta essa restrição para quando a Phase 18 (Motor de Score) existir"
  artifacts:
    - "docudata-backend/supabase_schema.sql — bloco '-- Phase 15' com entrou_em_andamento_em, travado_automatico, travado_override, travado_override_por, travado_override_em em tasks"
    - "docudata-backend/models/schemas.py — TaskResponse com os 5 campos novos"
    - "docudata-backend/routers/tasks.py — patch_task/create_task setando/resetando entrou_em_andamento_em + travado_*; endpoint POST /{task_id}/travado/override"
    - "docudata-backend/services/travamento_checker.py — check_travamento_automatico"
    - "docudata-backend/main.py — job registrado no _scheduler + POST /tasks/travamento/check (trigger manual, mesmo padrão de /notifications/check)"
    - "docudata-frontend/app/lib/api.ts — campos novos em TaskKanbanResponse + overrideTravamentoTask"
    - "docudata-frontend/app/components/TasksKanbanTab.tsx — badge 'Travada' no TaskCard + ação de suprimir no TaskModal"
    - "docudata-backend/tests/test_travamento_relogio.py, test_travamento_job_override.py — cobertura automatizada"
  key_links:
    - "TasksKanbanTab.tsx (drag-and-drop via ConfirmTransicaoModal, já existente da Phase 14) -> moverTaskKanban -> POST /tasks/{id}/mover -> patch_task -> mesmo loop que já grava task_transicoes/task_reaberturas agora também seta/reseta entrou_em_andamento_em + travado_*"
    - "main.py lifespan -> _scheduler.add_job(check_travamento_automatico, interval hours=24) -> services/travamento_checker.py -> tasks.update(travado_automatico=true)"
    - "TaskCard badge 'Travada' -> TaskModal botão 'Suprimir alerta' -> overrideTravamentoTask -> POST /tasks/{id}/travado/override -> travado_override=true -> badge some porque a condição de exibição é travado_automatico && !travado_override"
---

<objective>
Implementa a Phase 15, Parte 4 do SDD (ALERT-01/02/03): tasks paradas em `em_andamento` além de um limiar proporcional a pontos (`limiar_dias = pontos_da_task × 2`) viram um alerta visível — nunca pontuação. O campo `entrou_em_andamento_em` ancora o relógio e é escrito/resetado no mesmo caminho já usado por `patch_task` para gravar `task_transicoes`/`task_reaberturas` (Phase 14); um job diário no mesmo `AsyncIOScheduler` de `main.py` marca `travado_automatico=true` quando o limiar é cruzado; um endpoint de override do gerente suprime a exibição sem apagar o histórico de que o sistema sinalizou; o alerta aparece no `TasksKanbanTab.tsx` seguindo o mesmo padrão visual já usado para `bloqueado_manual` (Phase 14).

RBAC (Parte 6, Phase 16) e o Motor de Score (Phase 17-18) não existem ainda — nenhuma checagem de papel é adicionada aqui, e `travado_automatico` nunca é referenciado por nenhum código de score (trivialmente verdadeiro hoje; comentário inline documenta a restrição para quando a Phase 18 existir).

**Achado sobre Parte 8 (fora do foco desta plan, apenas documentado por instrução explícita):** a trava do baseline do SprintCard (`baseline_locked_at` em `docudata-backend/routers/sprints.py`, `PATCH /sprints/{sprint_id}/baseline`) já está implementada e cobre a primeira metade do critério de sucesso 5 da Phase 15 ("o campo vira somente leitura assim que a sprint entra em estado ativo" — confirmado: após `lock`, qualquer novo PATCH retorna 409 incondicional). A segunda metade do critério **não está coberta**: não existe nenhum caminho para "revisão posterior gera registro separado de replanejamento, preservando o valor original para o SPI histórico" — hoje, um baseline lockado é permanentemente imutável, sem mecanismo de replanejamento. Este é um gap real, não implementado nesta plan por instrução explícita do escopo ("não mexer, só documentar"). `docudata-backend/routers/sprints.py` **não é modificado** por nenhuma task abaixo.

Purpose: fechar as três dimensões de ALERT-01/02/03 sem criar nenhum acoplamento com score (que ainda não existe) e sem duplicar a lógica de detecção de transição já existente desde a Phase 14.
Output: 5 colunas novas em `tasks`; relógio `entrou_em_andamento_em` setado/resetado no mesmo caminho gated de `patch_task`/`create_task`; job diário `check_travamento_automatico`; endpoint de override; badge + ação de supressão no kanban de tasks; testes automatizados.
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/STATE.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/intel/constraints.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Relógio entrou_em_andamento_em — set/reset ponta a ponta em patch_task/create_task (ALERT-01, ALERT-02)</name>
  <files>
    docudata-backend/supabase_schema.sql,
    docudata-backend/models/schemas.py,
    docudata-backend/routers/tasks.py,
    docudata-backend/tests/test_travamento_relogio.py
  </files>
  <read_first>
    - docudata-backend/supabase_schema.sql linhas 332-351 — bloco `-- Phase 14: ...` (estilo exato de `ALTER TABLE tasks ADD COLUMN IF NOT EXISTS` a seguir para o novo bloco `-- Phase 15: ...`, incluindo o `CHECK` opcional usado em `bloqueado_resolvido_por` como referência de como documentar constraints)
    - docudata-backend/models/schemas.py linhas 383-465 — seção `# ── Tasks ──` completa: `TaskCreate`, `TaskUpdate` (linhas 411-441, com `_BLOQUEADO_RESOLVIDO_POR_VALIDOS` e `@field_validator` de exemplo), `TaskResponse` (linhas 444-465, onde `contador_reaberturas: int = 0` e os campos `bloqueado_*` já mostram o padrão exato de campo server-computed com default)
    - docudata-backend/routers/tasks.py linhas 81-138 (`create_task` — payload dict montado nas linhas 122-134, sem nenhum campo de timestamp de transição hoje) e linhas 265-395 (`patch_task` completo: o loop de transições nas linhas 340-350 é o ponto exato onde a Phase 14 já detecta `coluna_kanban` mudando e decide reabertura; a montagem final de `updates` nas linhas 355-380 é onde campos server-computed como `contador_reaberturas`/`bloqueado_em`/`bloqueado_por` são adicionados condicionalmente — mesmo padrão a seguir aqui)
    - docudata-backend/tests/test_bloqueio_manual.py arquivo inteiro — padrão exato de mock do Supabase (`_make_mock_client`/`_patch_and_client`, monkeypatch de `get_client` em `routers.tasks`, `TestClient(app)` a partir de `main`) a reusar/adaptar
    - docudata-backend/tests/test_task_reabertura.py arquivo inteiro — mesmo padrão, já cobre `concluida -> em_andamento`; útil para não duplicar asserts de `task_reaberturas`/`contador_reaberturas` que já são testados lá (esta plan só precisa asserir os campos novos, não re-testar reabertura em si)
  </read_first>
  <behavior>
    - Teste: `PATCH /tasks/{id}` movendo de `coluna_kanban="planejado"` para `"em_andamento"` -> `updates` gravados incluem `entrou_em_andamento_em` (timestamp do request, não nulo), `travado_automatico=False`, `travado_override=False`, `travado_override_por=None`, `travado_override_em=None`.
    - Teste: `PATCH /tasks/{id}` movendo de `"concluida"` para `"em_andamento"` (reabertura) -> mesmo resultado do caso acima (o relógio reinicia também na reabertura, ALERT-01: "reinicia em cada reentrada, inclusive por reabertura").
    - Teste: `PATCH /tasks/{id}` movendo de `"em_andamento"` para `"concluida"` (saindo) -> `updates` gravados incluem `entrou_em_andamento_em=None`, `travado_automatico=False`, `travado_override=False`, `travado_override_por=None`, `travado_override_em=None`.
    - Teste: `PATCH /tasks/{id}` movendo de `"em_andamento"` para `"planejado"` (saindo, caminho diferente) -> mesmo reset do caso acima.
    - Teste: `PATCH /tasks/{id}` sem mudar `coluna_kanban` (ex.: só `titulo`) -> `updates` NÃO contém `entrou_em_andamento_em` nem `travado_automatico`/`travado_override`/`travado_override_por`/`travado_override_em` (nenhuma escrita nesses campos quando não há transição de coluna).
    - Teste: `PATCH /tasks/{id}` movendo de `"planejado"` para `"concluida"` direto (nunca passou por `em_andamento`) -> mesmo resultado do caso "sem mudar coluna_kanban": nenhum dos 5 campos aparece em `updates` (nem entrou nem saiu de `em_andamento`).
    - Teste: `POST /tasks` criando uma task já com `coluna_kanban="em_andamento"` -> o payload de insert em `tasks` inclui `entrou_em_andamento_em` setado (não nulo).
    - Teste: `POST /tasks` criando uma task com `coluna_kanban="planejado"` (default) -> o payload de insert NÃO inclui `entrou_em_andamento_em`.
  </behavior>
  <action>
Migration (`docudata-backend/supabase_schema.sql`): adicione ao final do arquivo um novo bloco `-- Phase 15: Travamento Automático por Tempo (Parte 4 do SDD — ALERT-01/02/03)`, seguindo o estilo exato do bloco `-- Phase 14` (uma linha `ALTER TABLE tasks ADD COLUMN IF NOT EXISTS ...` por coluna). Colunas: `entrou_em_andamento_em timestamptz` (nullable — ausente quando a task nunca esteve em `em_andamento` ou não está atualmente), `travado_automatico boolean NOT NULL DEFAULT false`, `travado_override boolean NOT NULL DEFAULT false`, `travado_override_por text`, `travado_override_em timestamptz`.

Schemas (`docudata-backend/models/schemas.py`): adicione a `TaskResponse` os 5 campos com os mesmos defaults da migration: `entrou_em_andamento_em: Optional[datetime] = None`, `travado_automatico: bool = False`, `travado_override: bool = False`, `travado_override_por: Optional[str] = None`, `travado_override_em: Optional[datetime] = None`. NÃO adicione nenhum desses campos a `TaskCreate` nem a `TaskUpdate` — são sempre server-computed (o cliente nunca os define diretamente; `travado_override` só é gravado pelo endpoint dedicado da Task 2 desta plan, não por `PATCH /tasks/{id}`).

Backend — `create_task` (`docudata-backend/routers/tasks.py`): logo após a montagem do dict `payload` (antes do `client.table("tasks").insert(payload).execute()`), se `payload.get("coluna_kanban") == "em_andamento"`, adicione `payload["entrou_em_andamento_em"] = datetime.now(timezone.utc).isoformat()` (cobre o caso de uma task já criada diretamente em `em_andamento`, sem passar por transição — sem isso o job diário nunca teria âncora para essa task).

Backend — `patch_task`: dentro do loop `for campo in ("coluna_kanban", "operacional_id", "sprint_id"):`, quando `campo == "coluna_kanban"`, além da lógica de reabertura já existente (TRANS-03, Phase 14, não altere), adicione a detecção de entrada/saída de `em_andamento` usando o valor original da task (`task.get("coluna_kanban")`, capturado antes do loop, não muda durante a iteração) e `novo_valor`: se `novo_valor == "em_andamento"`, marque uma flag local `houve_entrada_em_andamento = True`; senão, se `task.get("coluna_kanban") == "em_andamento"` (estava em andamento e está saindo), marque `houve_saida_de_em_andamento = True`. Declare as duas flags como `False` antes do loop, junto de `houve_reabertura = False` (já existente). Depois que o dict `updates` é inicializado (`updates: dict = {"updated_at": agora.isoformat()}`) e antes do loop de campos genéricos que popula `updates` a partir de `data` (linhas ~356-363 do arquivo original), adicione: se `houve_entrada_em_andamento`, `updates["entrou_em_andamento_em"] = agora.isoformat()`, `updates["travado_automatico"] = False`, `updates["travado_override"] = False`, `updates["travado_override_por"] = None`, `updates["travado_override_em"] = None`; senão se `houve_saida_de_em_andamento`, `updates["entrou_em_andamento_em"] = None` e os mesmos 4 resets de `travado_*`. Os dois blocos são mutuamente exclusivos (uma única transição de `coluna_kanban` por PATCH só pode ser entrada OU saída de `em_andamento`, nunca as duas).

Backend tests: crie `docudata-backend/tests/test_travamento_relogio.py` reusando o padrão `_make_mock_client`/`_patch_and_client` de `tests/test_bloqueio_manual.py` (a fixture de task base precisa incluir `entrou_em_andamento_em: None, travado_automatico: False, travado_override: False, travado_override_por: None, travado_override_em: None` além dos campos já existentes de `_BASE_TASK`). Cubra os 8 casos do `<behavior>` acima — os testes de `POST /tasks` podem usar `TestClient(app).post("/tasks", json={...})` mockando `client.table("tasks").insert(...)` para capturar o payload recebido (mesmo espírito de captura de `calls["tasks_update"]`, mas para `insert`).
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python3 -c "import ast; ast.parse(open('routers/tasks.py').read()); ast.parse(open('models/schemas.py').read()); print('syntax OK')"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_travamento_relogio.py -q</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_tasks_dod_gate.py tests/test_task_reabertura.py tests/test_bloqueio_manual.py tests/test_task_confirmacao_sugestao.py -q</automated>
    <automated>grep -c "entrou_em_andamento_em" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/supabase_schema.sql"</automated>
  </verify>
  <done>
    Entrar em `em_andamento` (de qualquer coluna, inclusive reabertura) grava `entrou_em_andamento_em` e reseta `travado_automatico`/`travado_override`/`travado_override_por`/`travado_override_em`; sair de `em_andamento` zera `entrou_em_andamento_em` e os mesmos 4 campos; criar uma task já em `em_andamento` também ancora o relógio; nenhum PATCH que não toca `coluna_kanban` ou que transiciona entre colunas fora de `em_andamento` escreve nesses 5 campos; toda a suíte de testes de `tasks.py` (DoD, reabertura, bloqueio, confirmação de sugestão, relógio) passa junto.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Job diário de travamento automático + endpoint de override do gerente (ALERT-01, ALERT-03)</name>
  <files>
    docudata-backend/services/travamento_checker.py,
    docudata-backend/main.py,
    docudata-backend/routers/tasks.py,
    docudata-backend/tests/test_travamento_job_override.py
  </files>
  <read_first>
    - docudata-backend/services/notification_checker.py linhas 1-60 — padrão exato de um job de scheduler: função `def check_and_send_notifications() -> None:` sem parâmetros, que chama `get_client()` internamente (diferente dos services chamados a partir de um router, que recebem `client` como parâmetro — ex. `services/wip_check.py`, `services/spi_health.py`), usa `logging.getLogger(...)` e loga um resumo ao final
    - docudata-backend/main.py linhas 1-69 completo — `_scheduler = AsyncIOScheduler()`, `lifespan()` (linha 20: `_scheduler.add_job(check_and_send_notifications, "interval", hours=1, id="sprint_notifications")`), e o endpoint `POST /notifications/check` (linhas 64-68) que dispara o job manualmente via `BackgroundTasks` — mesmo padrão a replicar para o job de travamento, com `hours=24` e um `id` diferente
    - docudata-backend/routers/tasks.py — resultado da Task 1 desta plan (`entrou_em_andamento_em`, `travado_automatico`, `travado_override`, `travado_override_por`, `travado_override_em` já existem em `TaskResponse` e são setados/resetados em `patch_task`/`create_task`); linhas 397-402 (`mover_task`, endpoint semântico `POST /{task_id}/mover` — mesmo padrão de endpoint semântico com query params `autor`/`motivo` a seguir para o override)
    - docudata-backend/models/schemas.py — `TaskResponse` (resultado da Task 1) — o endpoint de override reusa `response_model=TaskResponse`, nenhum schema novo necessário
  </read_first>
  <behavior>
    - Teste (job): task com `coluna_kanban="em_andamento"`, `pontos=2` (limiar=4 dias), `entrou_em_andamento_em` 5 dias atrás, `travado_automatico=False`, `travado_override=False` -> `check_travamento_automatico()` chama `tasks.update({"travado_automatico": True})` para essa task.
    - Teste (job): mesma task mas `entrou_em_andamento_em` 2 dias atrás (< limiar de 4 dias) -> nenhuma chamada de update.
    - Teste (job): task com `travado_override=True` (mesmo cruzando o limiar) -> nenhuma chamada de update (job nunca marca `travado_automatico=true` quando `travado_override` é true — condição literal do SDD).
    - Teste (job): task com `travado_automatico=True` já (idempotência) -> nenhuma chamada de update redundante.
    - Teste (job): task com `entrou_em_andamento_em=None` (dado legado ou edge case) -> pulada sem erro, nenhuma chamada de update.
    - Teste (job): o select inicial filtra só `coluna_kanban="em_andamento"` — tasks em outras colunas não entram na varredura (verificar que o mock de `.eq("coluna_kanban", "em_andamento")` foi chamado).
    - Teste (endpoint): `POST /tasks/{id}/travado/override?autor=Gerente%20X` numa task com `coluna_kanban="em_andamento"` -> 200; `updates` gravados incluem `travado_override=True`, `travado_override_por="Gerente X"`, `travado_override_em` (timestamp do request); `travado_automatico` NÃO aparece em `updates` (o endpoint nunca toca nesse campo — preserva o histórico).
    - Teste (endpoint): mesma chamada numa task com `coluna_kanban="planejado"` (não está em andamento) -> 409, `detail` menciona que a task não está em Em Andamento; nenhuma escrita.
    - Teste (endpoint): task inexistente -> 404.
  </behavior>
  <action>
Job diário: crie `docudata-backend/services/travamento_checker.py` seguindo o padrão exato de `services/notification_checker.py` (função sem parâmetros, `get_client()` interno, `logging.getLogger("travamento_checker")`). Implemente `def check_travamento_automatico() -> None:` que: (1) busca `client.table("tasks").select("id, pontos, entrou_em_andamento_em, travado_automatico, travado_override").eq("coluna_kanban", "em_andamento").execute()`; (2) para cada task retornada, pula (continue) se `travado_override` for truthy, se `travado_automatico` já for truthy (idempotência — nada a fazer), ou se `entrou_em_andamento_em` for nulo (task sem âncora, não há como calcular dias); (3) calcula `dias_decorridos = (agora - entrou_dt).total_seconds() / 86400` (parseando `entrou_em_andamento_em` com `datetime.fromisoformat`, normalizando para `timezone.utc` se o parse vier naive) e `limiar_dias = (task.get("pontos") or 0) * 2`; (4) se `dias_decorridos >= limiar_dias`, chama `client.table("tasks").update({"travado_automatico": True}).eq("id", task["id"]).execute()`. Adicione um docstring/comentário no topo do arquivo deixando explícito, por escrito, a restrição do NFR do SDD: "este job NUNCA escreve em nenhum campo que alimenta fórmula de score — quando a Phase 18 (Motor de Score) existir, nenhuma dimensão deve ler travado_automatico". Logue ao final quantas tasks foram marcadas.

Wiring do scheduler (`docudata-backend/main.py`): importe `from services.travamento_checker import check_travamento_automatico` junto aos outros imports de `routers`/`services`. Dentro de `lifespan()`, adicione uma segunda linha `_scheduler.add_job(check_travamento_automatico, "interval", hours=24, id="task_travamento_check")`, logo após a linha existente de `check_and_send_notifications` — mesmo scheduler, mesmo padrão de registro, não crie um segundo `AsyncIOScheduler`. Adicione um endpoint de trigger manual espelhando `POST /notifications/check` (linhas 64-68): `@app.post("/tasks/travamento/check")` recebendo `background_tasks: BackgroundTasks`, chamando `background_tasks.add_task(check_travamento_automatico)` e retornando `{"status": "ok", "message": "Check de travamento automático iniciado em background — veja os logs do Railway para detalhes."}` (mesma mensagem/estilo do endpoint existente).

Endpoint de override (`docudata-backend/routers/tasks.py`): adicione, logo após `mover_task` (antes de `reordenar_tasks`), `@router.post("/{task_id}/travado/override", response_model=TaskResponse)` com assinatura `async def override_travamento(task_id: str, autor: Optional[str] = None):`. Busca a task (`client.table("tasks").select("*").eq("id", task_id).execute()`), 404 se não existir. Se `task["coluna_kanban"] != "em_andamento"`, `raise HTTPException(status_code=409, detail="Task não está em Em Andamento — não há travamento automático para suprimir.")`. Caso contrário, monta `updates = {"travado_override": True, "travado_override_por": autor, "travado_override_em": agora.isoformat()}` (usando `datetime.now(timezone.utc)`) — **não inclua `travado_automatico` em `updates`** (o override suprime a exibição sem apagar o histórico de que o sistema sinalizou; `travado_automatico` permanece com o valor que já tinha). Executa `client.table("tasks").update(updates).eq("id", task_id).execute()` e retorna `result.data[0]`.

Backend tests: crie `docudata-backend/tests/test_travamento_job_override.py` com duas seções. Para o job, mocke `client.table("tasks").select(...).eq(...).execute()` retornando uma lista de dicts de tasks fixture (sem passar por `TestClient`/FastAPI — chame `check_travamento_automatico()` diretamente, importando de `services.travamento_checker`, com `monkeypatch` em `services.travamento_checker.get_client`) e capture as chamadas a `.update(...)` para asserção (mesmo estilo `calls["tasks_update"]` já usado nos outros arquivos de teste). Para o endpoint, reuse `_make_mock_client`/`_patch_and_client` do padrão de `tests/test_bloqueio_manual.py`. Cubra os 9 casos do `<behavior>` acima.
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python3 -c "import ast; ast.parse(open('routers/tasks.py').read()); ast.parse(open('main.py').read()); ast.parse(open('services/travamento_checker.py').read()); print('syntax OK')"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_travamento_job_override.py -q</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_travamento_relogio.py tests/test_travamento_job_override.py tests/test_tasks_dod_gate.py tests/test_task_reabertura.py tests/test_bloqueio_manual.py tests/test_task_confirmacao_sugestao.py -q</automated>
    <automated>grep -c "task_travamento_check" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/main.py"</automated>
    <automated>grep -c "nunca escreve em nenhum campo que alimenta fórmula de score" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/services/travamento_checker.py"</automated>
  </verify>
  <done>
    `check_travamento_automatico` roda diariamente no mesmo scheduler de `main.py`, marca `travado_automatico=true` só quando o limiar proporcional a pontos é cruzado e `travado_override` não é true, e é idempotente (não re-escreve tasks já marcadas); `POST /tasks/{id}/travado/override` grava a supressão sem apagar o histórico de que o sistema sinalizou e rejeita tasks fora de `em_andamento` com 409; todos os testes novos e os já existentes de `tasks.py` passam.
  </done>
</task>

<task type="auto">
  <name>Task 3: Badge "Travada" + ação de suprimir no kanban de tasks (ALERT-03, visibilidade)</name>
  <files>
    docudata-frontend/app/lib/api.ts,
    docudata-frontend/app/components/TasksKanbanTab.tsx
  </files>
  <read_first>
    - docudata-frontend/app/lib/api.ts linhas 1139-1161 (`TaskKanbanResponse`, já tem os campos de bloqueio da Phase 14 como referência de estilo `campo?: tipo | null`) e linhas 1211-1242 (`patchTaskKanban`, `moverTaskKanban` — estilo exato de função fetch com `URLSearchParams` e tratamento de 409 a seguir para `overrideTravamentoTask`)
    - docudata-frontend/app/components/TasksKanbanTab.tsx linhas 107-135 (estado do `TaskModal`, incluindo `jaEstavaBloqueadoManual` como padrão de derivar um booleano a partir de `task?.campo`) e linhas 280-332 (JSX completo do bloco `bloqueado`/`bloqueado_manual` dentro de `mode === "edit"` — checkbox, inputs condicionais, select "Quem resolveu?" — é o padrão visual exato a espelhar para o bloco de travamento, mas SEM checkbox: aqui é só leitura de estado + um botão de ação)
    - docudata-frontend/app/components/TasksKanbanTab.tsx linhas 532-580 (`TaskCard` completo — o padrão de chip condicional `{task.bloqueado && (<span style={{ ...chip, background: "#fee2e2", color: "#dc2626" }}>Bloqueada</span>)}` nas linhas 568-570 é o padrão exato a replicar para o badge "Travada")
    - docudata-backend/routers/tasks.py — resultado da Task 2 desta plan (`POST /{task_id}/travado/override` aceita `autor` como query param opcional, retorna `TaskResponse` completo)
  </read_first>
  <action>
Tipos e função de API (`docudata-frontend/app/lib/api.ts`): adicione à interface `TaskKanbanResponse` os 5 campos novos, mesmo estilo dos campos de bloqueio já existentes: `entrou_em_andamento_em?: string | null; travado_automatico: boolean; travado_override: boolean; travado_override_por?: string | null; travado_override_em?: string | null;`. Adicione uma nova função `export async function overrideTravamentoTask(id: string, autor?: string): Promise<TaskKanbanResponse>` que monta `URLSearchParams` com `autor` se presente, faz `fetch(`${API}/tasks/${id}/travado/override?${q}`, { method: "POST" })`, trata `res.status === 409` extraindo `detail` e lançando `Error` (mesmo padrão de tratamento de 409 já usado em `patchTaskKanban`/`moverTaskKanban`), e `res.ok` check genérico antes de `return res.json()`.

Badge no card (`TaskCard` em `TasksKanbanTab.tsx`): logo após o bloco `{task.bloqueado && (...)}` (linhas ~568-570), adicione: `{task.travado_automatico && !task.travado_override && (<span style={{ ...chip, background: "#fef3c7", color: "#a16207" }}>⏱ Travada</span>)}` — a condição `!task.travado_override` é o que faz o badge sumir imediatamente quando o gerente suprime o alerta, mesmo que `travado_automatico` continue `true` no banco (preserva histórico, suprime só a exibição — exatamente o comportamento de ALERT-03).

Ação de suprimir no modal (`TaskModal` em `TasksKanbanTab.tsx`): adicione estado `const [travadoOverridePor, setTravadoOverridePor] = useState("");` e `const [overridingTravamento, setOverridingTravamento] = useState(false);` junto aos outros estados do componente (perto de `bloqueadoResolvidoPor`). Dentro do bloco `mode === "edit"`, após a seção de bloqueio manual (depois do `<>` que fecha em torno da linha ~332), adicione um bloco condicional `{task?.travado_automatico && !task?.travado_override && (...)}`: uma caixa de destaque (reuse a paleta âmbar do badge — `background: "#fef3c7"`, `border: "1px solid #fde68a"`, `borderRadius: 8`, `padding: 10`) com um texto explicando o alerta (ex.: `Task parada em Em Andamento além do limiar esperado para {task.pontos} ponto(s) (~{task.pontos * 2} dias).`), um input de texto livre para `travadoOverridePor` (placeholder "Seu nome (gerente)...", reuse `inputSt`) e um botão (`btnGhost`) "Suprimir alerta" que chama um novo handler async `handleOverrideTravamento`: se `!task`, retorna; seta `overridingTravamento=true`; chama `overrideTravamentoTask(task.id, travadoOverridePor.trim() || undefined)`; em sucesso chama `onSaved(updated)` e `onClose()` (mesmo padrão de finalização de `handleSubmit`); em erro, `setErr(...)` com a mensagem (sem fechar o modal); sempre `setOverridingTravamento(false)` no `finally`. O botão fica `disabled={overridingTravamento}` com texto "Suprimindo..." enquanto em andamento.

Nota de escopo: nenhuma checagem de papel (RBAC) é adicionada — a ação de suprimir fica visível para qualquer pessoa que abra o modal de edição da task, igual a todas as outras ações do sistema hoje (sem auth v1, mesma postura já aceita em toda a Phase 14). O `travado_override_por` é texto livre digitado pelo usuário, não vem de nenhuma sessão autenticada — mesma limitação já aceita para `bloqueado_resolvido_por` na Phase 14.
  </action>
  <verify>
    <automated>grep -c "overrideTravamentoTask" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/lib/api.ts"</automated>
    <automated>grep -c "travado_automatico" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/components/TasksKanbanTab.tsx"</automated>
    <automated>grep -c "!task.travado_override\|!task?.travado_override" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/components/TasksKanbanTab.tsx"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend" && npx tsc --noEmit 2>&1 | grep -i "TasksKanbanTab\|lib/api" || echo "no TS errors in touched files"</automated>
  </verify>
  <done>
    O badge "⏱ Travada" aparece no `TaskCard` só quando `travado_automatico=true` e `travado_override=false`; o `TaskModal` mostra a caixa de alerta com o botão "Suprimir alerta" quando a task editada está travada e não suprimida; clicar em "Suprimir alerta" chama `POST /tasks/{id}/travado/override`, atualiza a task local e fecha o modal, fazendo o badge sumir imediatamente; `npx tsc --noEmit` não aponta erros novos nos dois arquivos tocados.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> POST /tasks/{id}/travado/override | `autor` (quem suprimiu) é fornecido pelo cliente em texto livre, sem autenticação (v1 sem auth, decisão de projeto) |
| client -> PATCH /tasks/{id}, POST /tasks | `entrou_em_andamento_em`/`travado_automatico`/`travado_override`/`travado_override_por`/`travado_override_em` nunca são aceitos como campos de `TaskUpdate`/`TaskCreate` — só o backend (patch_task/create_task/job/endpoint de override) escreve neles |
| scheduler job -> tasks table | sem input externo, roda internamente no processo do backend; risco é de volume (varredura sem paginação), não de tampering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure | Todos os endpoints de tasks (sem auth v1) | low | accept | Postura idêntica a todos os endpoints já existentes no projeto (CLAUDE.md "Sem auth v1") — não é um risco novo introduzido por esta phase |
| T-15-02 | Tampering | POST /tasks/{id}/travado/override — `travado_override_por` é texto livre client-supplied, sem auth | low | accept | Idêntico à postura já aceita para `bloqueado_resolvido_por` na Phase 14 (T-14-04) — limitação conhecida do MVP sem RBAC, resolvida na Phase 16 |
| T-15-03 | Tampering | `entrou_em_andamento_em`/`travado_automatico`/`travado_override` — client não pode setar diretamente | low | mitigate | Nenhum desses campos existe em `TaskUpdate`/`TaskCreate`; só `patch_task`/`create_task` (transição de coluna) e o endpoint dedicado de override escrevem neles no servidor |
| T-15-04 | Denial of Service | Job diário varrendo todas as tasks em `em_andamento` sem paginação | low | accept | Volume do MVP é baixo (uso interno da subárea CITi, poucos projetos/tasks simultâneas); paginação fica para quando o volume justificar, não é um risco hoje |
| T-15-05 | Tampering | Job diário nunca deve escrever em campo de score (NFR explícito do SDD) | medium | mitigate | `check_travamento_automatico` só escreve `travado_automatico`; nenhuma tabela/coluna de score existe hoje (Phase 18 não implementada) para o job acidentalmente tocar; comentário inline documenta a restrição para quando a Phase 18 existir |

</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(...)"` em `routers/tasks.py`, `models/schemas.py`, `main.py`, `services/travamento_checker.py`: todos parseiam sem SyntaxError
- `docudata-backend/.venv/bin/pytest tests/test_travamento_relogio.py tests/test_travamento_job_override.py tests/test_tasks_dod_gate.py tests/test_task_reabertura.py tests/test_bloqueio_manual.py tests/test_task_confirmacao_sugestao.py -q`: todos passam, incluindo os testes já existentes de Phase 13/14 (garante que nada foi quebrado)
- `grep -c "entrou_em_andamento_em"` em `supabase_schema.sql`: >= 1
- `grep -c "task_travamento_check"` em `main.py`: >= 1 (job registrado no scheduler)
- `grep -c "overrideTravamentoTask"` em `api.ts`: >= 1
- `npx tsc --noEmit` (frontend): sem erros novos em `TasksKanbanTab.tsx`/`api.ts`
- Manual spot-check (não automatizado): `POST /tasks/travamento/check` dispara o job manualmente; uma task com `pontos=1` e `entrou_em_andamento_em` de 3 dias atrás (inserida manualmente via SQL para o teste) aparece com `travado_automatico=true` após o trigger; o badge "⏱ Travada" aparece no kanban; clicar em "Suprimir alerta" faz o badge sumir sem apagar `travado_automatico` no banco
</verification>

<success_criteria>
- [ ] `entrou_em_andamento_em` é setado em toda transição confirmada para `em_andamento` (qualquer coluna de origem, inclusive reabertura) e no `POST /tasks` que já cria em `em_andamento` — ALERT-01
- [ ] `entrou_em_andamento_em`, `travado_automatico` e `travado_override`/`_por`/`_em` resetam ao sair de `em_andamento` ou ao reentrar — ALERT-02
- [ ] Job diário `check_travamento_automatico`, registrado no mesmo `AsyncIOScheduler` de `main.py`, marca `travado_automatico=true` quando `dias_desde(entrou_em_andamento_em) >= pontos_da_task × 2` e `travado_override` não é true — ALERT-01
- [ ] `POST /tasks/{id}/travado/override` grava a supressão sem apagar `travado_automatico` (histórico preservado) — ALERT-03
- [ ] Badge "Travada" no kanban de tasks só aparece quando `travado_automatico=true` E `travado_override=false`, seguindo o mesmo padrão visual de `bloqueado_manual` — ALERT-03
- [ ] `travado_automatico` nunca é lido/escrito por nenhum código relacionado a score — comentário inline documenta a restrição
- [ ] Nenhuma checagem de papel/permissão (RBAC) foi adicionada — fora de escopo desta phase
- [ ] `docudata-backend/routers/sprints.py` (Parte 8, baseline lock) não foi modificado; o gap do critério 5 (registro separado de replanejamento) permanece documentado no `<objective>` desta plan, não implementado
- [ ] Campos legados e testes de Phase 13/14 (DoD, reabertura, bloqueio, confirmação de sugestão) continuam passando sem alteração de comportamento
</success_criteria>

<output>
Create `.planning/quick/260903-bq4-implementar-phase-15-parte-4-do-sdd-trav/260903-bq4-SUMMARY.md` when done
</output>
