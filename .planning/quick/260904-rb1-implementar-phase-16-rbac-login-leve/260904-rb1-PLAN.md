---
phase: 16-rbac-login-leve-e-papeis-de-acesso
plan: 01
type: execute
wave: 1-2
depends_on: []
files_modified:
  - docudata-backend/requirements.txt
  - docudata-backend/supabase_schema.sql
  - docudata-backend/models/schemas.py
  - docudata-backend/services/auth.py
  - docudata-backend/services/audit.py
  - docudata-backend/routers/auth.py
  - docudata-backend/routers/performance.py
  - docudata-backend/routers/sprints.py
  - docudata-backend/routers/tasks.py
  - docudata-backend/main.py
  - docudata-backend/tests/test_auth_login.py
  - docudata-backend/tests/test_auth_signup.py
  - docudata-backend/tests/test_rbac_gate.py
  - docudata-backend/tests/test_performance_stub.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/components/AuthGuard.tsx
  - docudata-frontend/app/login/page.tsx
  - docudata-frontend/app/cadastro/page.tsx
  - docudata-frontend/app/layout.tsx
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - "RBAC-01"
  - "RBAC-02"
  - "RBAC-03"
  - "RBAC-04"
  - "RBAC-05"

estimate:
  tokens: 145000
  raw_tokens: 145000
  tasks: 5
  confidence: low

must_haves:
  truths:
    - "POST /auth/login com email/senha corretos seta um cookie httpOnly (docudata_session) contendo um JWT (sub=pessoa.id, email, cargo, exp) e retorna {nome, cargo}; senha errada ou email inexistente retornam 401 sem vazar qual dos dois é inválido — RBAC-01"
    - "Toda rota do backend (exceto a allowlist pública /auth/login, /auth/logout, /auth/me, /auth/operacionais-sem-conta, /auth/signup/claim, /auth/signup/novo, /health, /notifications/check, /tasks/travamento/check) passa a exigir cookie de sessão válido — sem cookie ou com JWT inválido/expirado, 401 — RBAC-01"
    - "cargo=operacional só recebe 200 em rotas escopadas por projeto (GET /tasks?project_id=X, GET/POST /projects/{project_id}/sprints) quando existe uma linha em operacionais com esse project_id e email igual ao da pessoa logada — senão 403; cargo=lider/gerente sempre passa, sem essa checagem — RBAC-02"
    - "cargo=operacional recebe 403 em qualquer rota de metricas.py, painel.py, operacionais.py, e nos endpoints PATCH /sprints/{id}/baseline, PATCH /sprints/{id}/health, DELETE /sprints/{id} — cargo=gerente e cargo=lider continuam recebendo 200 nessas mesmas rotas, sem nenhuma mudança de comportamento — RBAC-02, RBAC-03"
    - "GET /performance retorna 403 para cargo != lider (dependency dedicada require_role('lider'), não reaproveita require_not_operacional nem require_project_access); para cargo=lider retorna 200 e grava uma linha em audit_log com pessoa_email, rota='/performance', acao='acesso' — RBAC-04, RBAC-05"
    - "Nenhuma rota de cadastro (/auth/signup/claim, /auth/signup/novo) aceita ou grava cargo diferente de 'operacional' — cargo=gerente/lider só existem via INSERT manual direto no Supabase, fora da API"
    - "Nenhum endpoint de metricas.py, MetricasTab.tsx ou qualquer outro código já existente da Phase 13 muda de comportamento para cargo=gerente — só ganham a checagem require_not_operacional (que gerente sempre passa)"
  artifacts:
    - "docudata-backend/supabase_schema.sql — bloco '-- Phase 16' com as tabelas pessoa e audit_log"
    - "docudata-backend/services/auth.py — hash_senha, verificar_senha, criar_jwt, decodificar_jwt, get_current_pessoa, require_role, require_not_operacional, require_project_access"
    - "docudata-backend/services/audit.py — registrar_auditoria"
    - "docudata-backend/routers/auth.py — login, logout, me, operacionais_sem_conta, signup_claim, signup_novo"
    - "docudata-backend/routers/performance.py — stub protegido"
    - "docudata-backend/main.py — CORS com FRONTEND_URL (não mais allow_origins=['*']), auth.router público, todos os outros routers com dependencies=[Depends(get_current_pessoa)] ou camada mais restrita"
    - "docudata-frontend/app/login/page.tsx, app/cadastro/page.tsx, app/components/AuthGuard.tsx — telas e guarda de rota novas"
    - "docudata-frontend/app/lib/api.ts — apiFetch com credentials:'include' substituindo as 66 chamadas fetch( existentes, + funções de auth novas"
    - "docudata-frontend/app/projects/[id]/page.tsx — abas filtradas por cargo"
  key_links:
    - "main.py lifespan/app.include_router(...) -> services/auth.get_current_pessoa (Depends) -> Cookie('docudata_session') -> decodificar_jwt -> 401/pessoa dict disponível para toda rota protegida"
    - "routers/tasks.py GET '' e routers/sprints.py POST/GET /projects/{project_id}/sprints -> Depends(require_project_access) -> services/auth.py consulta operacionais.email == pessoa.email AND operacionais.project_id == project_id -> 403/200"
    - "AuthGuard.tsx (layout.tsx) -> GET /auth/me -> 401 -> router.replace('/login'); login/page.tsx -> POST /auth/login -> cookie setado pelo browser -> requests seguintes do apiFetch já autenticados (credentials:'include')"
    - "app/projects/[id]/page.tsx -> useAuth() -> cargo -> filtra array de tabs antes de passar pro componente Tabs -> abas escopo/painel/metricas/config somem da UI pra cargo=operacional (a fonte real de verdade continua sendo o backend)"
---

<objective>
Implementa a Phase 16 do ROADMAP.md (RBAC-01..05): substitui a postura "sem auth v1" do DocuData por um login leve real. Uma nova tabela `pessoa` (email/nome/senha_hash/cargo ∈ {lider, gerente, operacional}) resolve quem está logado; o login emite um JWT em cookie httpOnly (sem refresh token, sem tabela de sessão — o próprio token é a fonte de verdade). Um gate global de autenticação passa a exigir sessão válida em toda rota do backend por padrão, com camadas adicionais (`require_role`, `require_not_operacional`, `require_project_access`) restringindo o que cada cargo pode fazer: Operacional só acessa projetos onde tem uma linha correspondente em `operacionais` (casada por email, sem FK nova) e não vê nada de métricas/painel/escopo/configurações — só Sprints, Tasks, Tecnologias e Documentos. Um mecanismo genérico de auditoria (`audit_log` + `registrar_auditoria`) fica pronto para as Phases 17/18 reutilizarem; nesta phase, só o novo endpoint `GET /performance` (stub protegido, sem dado real — a Phase 19 preenche o corpo) o utiliza.

Auto-cadastro é o caminho de entrada para cargo=operacional: uma pessoa nova vê a lista global de `operacionais` sem conta (todos os projetos) e clica "sou eu" para criar sua senha — isso vale para todas as linhas de `operacionais` com esse email, em qualquer projeto, mesmo que ela só tenha clicado numa. Se não se encontra na lista, cria uma conta sem nenhum projeto vinculado ainda (fica sem acesso até um Gerente/Líder adicioná-la a algum projeto pela tela que já existe). Nenhuma rota de cadastro cria cargo=gerente/lider — essas continuam exclusivamente por INSERT manual no Supabase.

**Decisão explícita de escopo** (validada com o usuário durante o brainstorming, ver `docs/superpowers/specs/2026-09-04-rbac-login-leve-papeis-acesso-design.md`): o critério RBAC-03 ("nenhum payload retorna score/peso/fórmula/ranking para Gerente/Operacional") se aplica só aos dados que a Phase 18 (Motor de Score) e Phase 19 (Ranking) ainda vão criar — o SPI-por-operacional interino e as outras métricas de `metricas.py`/`MetricasTab.tsx` (Phase 13) continuam 100% visíveis para Gerente, sem nenhuma mudança de comportamento; só ganham a exigência de `require_not_operacional` (que Gerente sempre passa).

**Gaps aceitos e documentados** (não implementados nesta plan, por decisão explícita de escopo — não é esquecimento):
- `require_project_access` só é aplicado às rotas de listagem/criação com `project_id` disponível diretamente no path/query (`GET /tasks`, `GET`/`POST /projects/{project_id}/sprints`). Rotas escopadas por `task_id`/`sprint_id`/`operacional_id` sem `project_id` direto no path (ex.: `PATCH /tasks/{task_id}`, `POST /tasks/{task_id}/mover`) não ganham essa checagem nesta plan — um Operacional autenticado que já soubesse o UUID de uma task de outro projeto (não exposta em nenhuma tela sua) ainda conseguiria mexer nela. Risco aceito: MVP interno, sem superfície de enumeração de UUIDs de outros projetos na UI.
- As abas Tecnologias e Documentos (routers `search.py`, `ingestions.py`, `generate.py`, `boletins.py`, `composer.py`, `sprint_docs.py`, `export.py`, etc.) só ganham o gate básico de login (`require_project_access` não aplicado) — Operacional autenticado tecnicamente consegue ler/gerar documentos de qualquer projeto via API direta, mesmo que a UI só mostre os seus. Mesmo risco aceito acima.
- `funcionalidades.router` (dados da aba Escopo) **não** ganha `require_not_operacional` nesta plan, mesmo a aba sumindo da UI (Task 5) — `listFuncionalidades` é chamado num bootstrap compartilhado em `page.tsx` (linha ~257) que também alimenta o badge de pendências da aba Sprints (que Operacional pode acessar); bloquear o router inteiro quebraria esse carregamento. Operacional autenticado tecnicamente consegue ler `GET /funcionalidades` de qualquer projeto via API direta. Fechar isso direito exigiria separar o endpoint de leitura usado no bootstrap do resto do CRUD de `funcionalidades.py` — fora do escopo desta plan, documentado aqui em vez de silenciosamente ignorado.
- `projects.router` (chave de API do Gemini, budget, email do gerente, WIP config — parte da aba Configurações) também só tem o gate básico, pelo mesmo motivo: esse router também serve `GET /projects/{id}`, usado por toda página do projeto (inclusive pelas abas que Operacional pode acessar). Fechar isso exigiria separar leitura básica de projeto das rotas de configuração — mesmo gap aceito.
- `/notifications/check`, `/tasks/travamento/check` e `/health` continuam públicos (endpoints operacionais de trigger interno, não expõem dado de negócio).
- Recuperação de senha, convite de Gerente/Líder pela UI, e refresh token ficam fora de escopo (documentado na spec).

Purpose: fechar o RBAC-01..05 do ROADMAP sem acoplar com Motor de Score (Phase 18) nem Ranking (Phase 19), que ainda não existem.
Output: tabelas `pessoa`/`audit_log`; `services/auth.py` e `services/audit.py`; router `auth.py` completo (login/logout/me/signup); gate global de enforcement no `main.py`; `GET /performance` protegido; telas `/login` e `/cadastro`; abas filtradas por cargo no frontend; testes automatizados cobrindo os cinco requisitos.
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/STATE.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docs/superpowers/specs/2026-09-04-rbac-login-leve-papeis-acesso-design.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Migração (pessoa, audit_log) + schemas + dependências novas</name>
  <files>
    docudata-backend/requirements.txt,
    docudata-backend/supabase_schema.sql,
    docudata-backend/models/schemas.py
  </files>
  <read_first>
    - docudata-backend/supabase_schema.sql linhas 332-359 — blocos `-- Phase 14` e `-- Phase 15`, estilo exato de `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` a seguir para o bloco `-- Phase 16`
    - docudata-backend/models/schemas.py linhas 1-3 (imports: `BaseModel, Field, field_validator`, `Optional`, `date, datetime`) e linhas 373-380 (`OperacionalResponse` — padrão exato de schema simples a seguir para os novos)
    - docudata-backend/requirements.txt arquivo inteiro — nenhuma lib de JWT/hash existe hoje; PyJWT e bcrypt são dependências novas
  </read_first>
  <behavior>
    - `python3 -c "import ast; ast.parse(open('models/schemas.py').read())"` continua passando (nenhum erro de sintaxe)
    - `grep -c "CREATE TABLE IF NOT EXISTS pessoa" supabase_schema.sql` >= 1 e `grep -c "CREATE TABLE IF NOT EXISTS audit_log" supabase_schema.sql` >= 1
    - `.venv/bin/python -c "import jwt, bcrypt; print('ok')"` imprime `ok` depois de instalar as deps novas
  </behavior>
  <action>
Migration (`docudata-backend/supabase_schema.sql`): adicione ao final do arquivo:

```sql
-- Phase 16: RBAC — Login Leve e Papéis de Acesso (RBAC-01..05)
CREATE TABLE IF NOT EXISTS pessoa (
    id           uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
    email        text        NOT NULL UNIQUE,
    nome         text        NOT NULL,
    senha_hash   text        NOT NULL,
    cargo        text        NOT NULL CHECK (cargo IN ('lider','gerente','operacional')),
    created_at   timestamptz DEFAULT now()
);

CREATE TABLE IF NOT EXISTS audit_log (
    id            uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
    pessoa_id     uuid        REFERENCES pessoa(id),
    pessoa_email  text        NOT NULL,
    rota          text        NOT NULL,
    acao          text        NOT NULL,
    created_at    timestamptz DEFAULT now()
);
```

`pessoa.email` casa em runtime com `operacionais.email` (tabela já existente) — sem FK nova, sem migration em `operacionais`.

`requirements.txt`: adicione duas linhas ao final:
```
PyJWT>=2.8,<3
bcrypt>=4.1,<5
```
Rode `.venv/bin/pip install -r requirements.txt` para instalar as duas novas dependências no venv do projeto (mesmo passo que já foi necessário na Phase 13 quando `apscheduler` estava faltando).

Schemas (`docudata-backend/models/schemas.py`): adicione ao final do arquivo uma nova seção:

```python
# ── Auth / RBAC (Phase 16) ─────────────────────────────────────────────────

_CARGOS_VALIDOS = {"lider", "gerente", "operacional"}


class LoginRequest(BaseModel):
    email: str
    senha: str


class LoginResponse(BaseModel):
    nome: str
    cargo: str


class MeResponse(BaseModel):
    nome: str
    email: str
    cargo: str


class OperacionalSemContaResponse(BaseModel):
    operacional_id: str
    nome: str
    project_id: str
    project_name: str


class SignupClaimRequest(BaseModel):
    operacional_id: str
    email: str
    senha: str = Field(..., min_length=6)


class SignupNovoRequest(BaseModel):
    nome: str
    email: str
    senha: str = Field(..., min_length=6)
```

Nenhum desses schemas expõe ou aceita `senha_hash` — o cliente nunca vê o hash, só `senha` em texto puro no request (transmitido só sobre HTTPS em produção).
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python3 -c "import ast; ast.parse(open('models/schemas.py').read()); print('syntax OK')"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && grep -c "CREATE TABLE IF NOT EXISTS pessoa" supabase_schema.sql && grep -c "CREATE TABLE IF NOT EXISTS audit_log" supabase_schema.sql</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pip install -r requirements.txt -q && .venv/bin/python -c "import jwt, bcrypt; print('deps OK')"</automated>
  </verify>
  <done>
    Tabelas `pessoa` e `audit_log` no bloco `-- Phase 16` de `supabase_schema.sql`; `PyJWT`/`bcrypt` instalados no venv; seis schemas Pydantic novos (`LoginRequest`, `LoginResponse`, `MeResponse`, `OperacionalSemContaResponse`, `SignupClaimRequest`, `SignupNovoRequest`) parseando sem erro.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: services/auth.py + router auth.py (login, logout, me, auto-cadastro)</name>
  <files>
    docudata-backend/services/auth.py,
    docudata-backend/routers/auth.py,
    docudata-backend/main.py,
    docudata-backend/tests/test_auth_login.py,
    docudata-backend/tests/test_auth_signup.py
  </files>
  <read_first>
    - docudata-backend/services/supabase_client.py inteiro — `get_client()` lazy, mesmo padrão a reusar
    - docudata-backend/routers/tasks.py linhas 1-20 (imports, `router = APIRouter(prefix=..., tags=...)`) e linhas 81-140 (`create_task` — padrão de montar payload dict e inserir via `client.table(...).insert(...).execute()`)
    - docudata-backend/tests/test_bloqueio_manual.py inteiro — padrão de mock do Supabase com `MagicMock`, `_patch_and_client` fazendo `monkeypatch.setattr(<router_module>, "get_client", lambda: mock_client)` e `TestClient(app)` a partir de `main`
    - docudata-backend/main.py inteiro (78 linhas) — vai receber `app.include_router(auth.router)` nesta task (sem `dependencies=`, é a única rota pública) e o resto do gate global fica pra Task 3
  </read_first>
  <behavior>
    - `POST /auth/login` com email+senha corretos -> 200, body `{"nome": ..., "cargo": ...}`, cookie `docudata_session` presente na resposta
    - `POST /auth/login` com senha errada -> 401
    - `POST /auth/login` com email que não existe -> 401 (mesma mensagem genérica da senha errada — não revela qual campo está errado)
    - `POST /auth/signup/claim` com `operacional_id` cujo `operacionais.email` bate com o email informado -> 201, cria `pessoa` com `cargo="operacional"`, loga automaticamente (cookie setado)
    - `POST /auth/signup/claim` com email que NÃO bate com o `operacionais.email` daquela linha -> 403, nenhuma escrita em `pessoa`
    - `POST /auth/signup/claim` com email já cadastrado em `pessoa` -> 409
    - `POST /auth/signup/novo` com email novo -> 201, cria só `pessoa` (`cargo="operacional"`), loga automaticamente; nenhuma escrita em `operacionais`
    - `POST /auth/signup/novo` com email já cadastrado -> 409
  </behavior>
  <action>
Service (`docudata-backend/services/auth.py`, novo arquivo):

```python
"""
Autenticação e RBAC (Phase 16). JWT em cookie httpOnly — sem tabela de
sessão, o próprio token é a fonte de verdade (sub=pessoa.id, email, cargo,
exp). Sem refresh token no MVP: expirado, pede login de novo.
"""
import os
from datetime import datetime, timedelta, timezone
from typing import Optional

import bcrypt
import jwt
from fastapi import Cookie, Depends, HTTPException

from services.supabase_client import get_client

_JWT_ALG = "HS256"
_JWT_EXP_HOURS = 8
COOKIE_NAME = "docudata_session"


def hash_senha(senha: str) -> str:
    return bcrypt.hashpw(senha.encode("utf-8"), bcrypt.gensalt()).decode("utf-8")


def verificar_senha(senha: str, senha_hash: str) -> bool:
    return bcrypt.checkpw(senha.encode("utf-8"), senha_hash.encode("utf-8"))


def criar_jwt(pessoa_id: str, email: str, cargo: str) -> str:
    agora = datetime.now(timezone.utc)
    payload = {
        "sub": pessoa_id,
        "email": email,
        "cargo": cargo,
        "iat": agora,
        "exp": agora + timedelta(hours=_JWT_EXP_HOURS),
    }
    return jwt.encode(payload, os.environ["JWT_SECRET"], algorithm=_JWT_ALG)


def decodificar_jwt(token: str) -> dict:
    return jwt.decode(token, os.environ["JWT_SECRET"], algorithms=[_JWT_ALG])


async def get_current_pessoa(
    session: Optional[str] = Cookie(default=None, alias=COOKIE_NAME),
) -> dict:
    if not session:
        raise HTTPException(status_code=401, detail="Não autenticado")
    try:
        payload = decodificar_jwt(session)
    except jwt.PyJWTError:
        raise HTTPException(status_code=401, detail="Sessão inválida ou expirada")
    return {"id": payload["sub"], "email": payload["email"], "cargo": payload["cargo"]}


def require_role(cargo_permitido: str):
    async def _dep(pessoa: dict = Depends(get_current_pessoa)) -> dict:
        if pessoa["cargo"] != cargo_permitido:
            raise HTTPException(status_code=403, detail=f"Acesso restrito a {cargo_permitido}")
        return pessoa
    return _dep


async def require_not_operacional(pessoa: dict = Depends(get_current_pessoa)) -> dict:
    if pessoa["cargo"] == "operacional":
        raise HTTPException(status_code=403, detail="Acesso restrito a Gerente/Líder")
    return pessoa


async def require_project_access(project_id: str, pessoa: dict = Depends(get_current_pessoa)) -> dict:
    if pessoa["cargo"] in ("lider", "gerente"):
        return pessoa
    client = get_client()
    check = (
        client.table("operacionais")
        .select("id")
        .eq("project_id", project_id)
        .eq("email", pessoa["email"])
        .execute()
    )
    if not check.data:
        raise HTTPException(status_code=403, detail="Você não tem acesso a este projeto")
    return pessoa
```

Router (`docudata-backend/routers/auth.py`, novo arquivo):

```python
from fastapi import APIRouter, Depends, HTTPException, Response

from models.schemas import (
    LoginRequest,
    LoginResponse,
    MeResponse,
    OperacionalSemContaResponse,
    SignupClaimRequest,
    SignupNovoRequest,
)
from services.auth import (
    COOKIE_NAME,
    criar_jwt,
    get_current_pessoa,
    hash_senha,
    verificar_senha,
)
from services.supabase_client import get_client

router = APIRouter(prefix="/auth", tags=["auth"])


def _set_session_cookie(response: Response, pessoa_id: str, email: str, cargo: str) -> None:
    token = criar_jwt(pessoa_id, email, cargo)
    response.set_cookie(
        key=COOKIE_NAME,
        value=token,
        httponly=True,
        secure=True,
        samesite="none",
        max_age=8 * 3600,
    )


@router.post("/login", response_model=LoginResponse)
async def login(data: LoginRequest, response: Response):
    client = get_client()
    resp = client.table("pessoa").select("*").eq("email", data.email).execute()
    if not resp.data:
        raise HTTPException(status_code=401, detail="Email ou senha inválidos")
    pessoa = resp.data[0]
    if not verificar_senha(data.senha, pessoa["senha_hash"]):
        raise HTTPException(status_code=401, detail="Email ou senha inválidos")
    _set_session_cookie(response, pessoa["id"], pessoa["email"], pessoa["cargo"])
    return {"nome": pessoa["nome"], "cargo": pessoa["cargo"]}


@router.post("/logout")
async def logout(response: Response):
    response.delete_cookie(COOKIE_NAME)
    return {"status": "ok"}


@router.get("/me", response_model=MeResponse)
async def me(pessoa: dict = Depends(get_current_pessoa)):
    client = get_client()
    resp = client.table("pessoa").select("nome, email, cargo").eq("id", pessoa["id"]).execute()
    if not resp.data:
        raise HTTPException(status_code=401, detail="Sessão inválida")
    row = resp.data[0]
    return {"nome": row["nome"], "email": row["email"], "cargo": row["cargo"]}


@router.get("/operacionais-sem-conta", response_model=list[OperacionalSemContaResponse])
async def operacionais_sem_conta():
    client = get_client()
    ops = client.table("operacionais").select("id, nome, email, project_id").eq("ativo", True).execute().data or []
    pessoas = client.table("pessoa").select("email").execute().data or []
    emails_com_conta = {p["email"] for p in pessoas}
    projects = client.table("projects").select("id, name").execute().data or []
    nome_por_projeto = {p["id"]: p["name"] for p in projects}

    resultado = []
    for op in ops:
        if op.get("email") and op["email"] in emails_com_conta:
            continue
        resultado.append({
            "operacional_id": op["id"],
            "nome": op["nome"],
            "project_id": op["project_id"],
            "project_name": nome_por_projeto.get(op["project_id"], "—"),
        })
    return resultado


@router.post("/signup/claim", response_model=LoginResponse, status_code=201)
async def signup_claim(data: SignupClaimRequest, response: Response):
    client = get_client()
    op_resp = client.table("operacionais").select("*").eq("id", data.operacional_id).execute()
    if not op_resp.data:
        raise HTTPException(status_code=404, detail="Operacional não encontrado")
    operacional = op_resp.data[0]

    if operacional.get("email") and operacional["email"] != data.email:
        raise HTTPException(status_code=403, detail="Email não confere com o cadastro deste operacional")

    existing = client.table("pessoa").select("id").eq("email", data.email).execute()
    if existing.data:
        raise HTTPException(status_code=409, detail="Já existe uma conta com este email")

    if not operacional.get("email"):
        client.table("operacionais").update({"email": data.email}).eq("id", data.operacional_id).execute()

    senha_hash = hash_senha(data.senha)
    novo = client.table("pessoa").insert({
        "email": data.email,
        "nome": operacional["nome"],
        "senha_hash": senha_hash,
        "cargo": "operacional",
    }).execute()
    pessoa = novo.data[0]
    _set_session_cookie(response, pessoa["id"], pessoa["email"], pessoa["cargo"])
    return {"nome": pessoa["nome"], "cargo": pessoa["cargo"]}


@router.post("/signup/novo", response_model=LoginResponse, status_code=201)
async def signup_novo(data: SignupNovoRequest, response: Response):
    client = get_client()
    existing = client.table("pessoa").select("id").eq("email", data.email).execute()
    if existing.data:
        raise HTTPException(status_code=409, detail="Já existe uma conta com este email")

    senha_hash = hash_senha(data.senha)
    novo = client.table("pessoa").insert({
        "email": data.email,
        "nome": data.nome,
        "senha_hash": senha_hash,
        "cargo": "operacional",
    }).execute()
    pessoa = novo.data[0]
    _set_session_cookie(response, pessoa["id"], pessoa["email"], pessoa["cargo"])
    return {"nome": pessoa["nome"], "cargo": pessoa["cargo"]}
```

`main.py`: adicione `auth` ao import de `routers` (linha 10) e `app.include_router(auth.router)` logo após a linha `app = FastAPI(...)` — SEM `dependencies=`, é a única rota pública (a Task 3 aplica o gate global nos outros routers, este fica de fora de propósito).

**Nota de teste importante**: não dependa do cookie jar do `TestClient` persistir automaticamente entre chamadas (o cookie é `Secure`+`SameSite=None`, e `TestClient` roda sobre `http://testserver`, não https — o comportamento de persistência automática do jar pode ser inconsistente nesse cenário). Para testar uma chamada autenticada subsequente, extraia o token da resposta do login (`resp.cookies.get("docudata_session")`) ou construa o JWT diretamente via `services.auth.criar_jwt(...)` e passe explicitamente via `cookies={"docudata_session": token}` na chamada seguinte — nunca dependa de reuso implícito do mesmo client.

Testes (`docudata-backend/tests/test_auth_login.py`, novo):

```python
"""Testes para POST /auth/login (RBAC-01)."""
from unittest.mock import MagicMock
from fastapi.testclient import TestClient

from services.auth import hash_senha


def _mock_client_com_pessoa(pessoa_row):
    client = MagicMock()

    def table_side_effect(name):
        tbl = MagicMock()
        if name == "pessoa":
            def select_side_effect(cols):
                query = MagicMock()

                def eq_side_effect(field, value):
                    resp = MagicMock()
                    resp.data = [pessoa_row] if pessoa_row and pessoa_row.get(field) == value else []
                    q = MagicMock()
                    q.execute = MagicMock(return_value=resp)
                    return q

                query.eq = MagicMock(side_effect=eq_side_effect)
                return query

            tbl.select = MagicMock(side_effect=select_side_effect)
        return tbl

    client.table = MagicMock(side_effect=table_side_effect)
    return client


def _patch_and_client(monkeypatch, mock_supabase):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    import routers.auth as auth_router
    monkeypatch.setattr(auth_router, "get_client", lambda: mock_supabase)
    from main import app
    return TestClient(app)


_PESSOA = {
    "id": "pessoa-1",
    "email": "gerente@citi.com",
    "nome": "Gerente Um",
    "senha_hash": hash_senha("senha-correta"),
    "cargo": "gerente",
}


def test_login_valido_seta_cookie_e_retorna_nome_cargo(monkeypatch):
    mock_sb = _mock_client_com_pessoa(_PESSOA)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/login", json={"email": "gerente@citi.com", "senha": "senha-correta"})

    assert resp.status_code == 200
    assert resp.json() == {"nome": "Gerente Um", "cargo": "gerente"}
    assert resp.cookies.get("docudata_session") is not None


def test_login_senha_errada_401(monkeypatch):
    mock_sb = _mock_client_com_pessoa(_PESSOA)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/login", json={"email": "gerente@citi.com", "senha": "errada"})

    assert resp.status_code == 401


def test_login_email_inexistente_401(monkeypatch):
    mock_sb = _mock_client_com_pessoa(None)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/login", json={"email": "ninguem@citi.com", "senha": "qualquer"})

    assert resp.status_code == 401
```

Testes (`docudata-backend/tests/test_auth_signup.py`, novo) — reuse `_patch_and_client` (redefinida localmente neste arquivo, mesmo corpo do arquivo acima):

```python
"""Testes para POST /auth/signup/claim e /auth/signup/novo."""
from unittest.mock import MagicMock
from fastapi.testclient import TestClient


def _patch_and_client(monkeypatch, mock_supabase):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    import routers.auth as auth_router
    monkeypatch.setattr(auth_router, "get_client", lambda: mock_supabase)
    from main import app
    return TestClient(app)


def _mock_client(operacional_row=None, pessoa_existente=None):
    client = MagicMock()
    calls = {"pessoa_insert": [], "operacionais_update": []}

    def table_side_effect(name):
        tbl = MagicMock()

        if name == "operacionais":
            def select_side_effect(cols):
                query = MagicMock()

                def eq_side_effect(field, value):
                    resp = MagicMock()
                    resp.data = [operacional_row] if operacional_row and operacional_row.get(field) == value else []
                    q = MagicMock()
                    q.execute = MagicMock(return_value=resp)
                    return q

                query.eq = MagicMock(side_effect=eq_side_effect)
                return query

            def update_side_effect(payload):
                calls["operacionais_update"].append(payload)
                q = MagicMock()
                inner = MagicMock()
                resp = MagicMock()
                resp.data = [dict(operacional_row or {}, **payload)]
                inner.execute = MagicMock(return_value=resp)
                q.eq = MagicMock(return_value=inner)
                return q

            tbl.select = MagicMock(side_effect=select_side_effect)
            tbl.update = MagicMock(side_effect=update_side_effect)

        elif name == "pessoa":
            def select_side_effect(cols):
                query = MagicMock()

                def eq_side_effect(field, value):
                    resp = MagicMock()
                    resp.data = [pessoa_existente] if pessoa_existente and pessoa_existente.get(field) == value else []
                    q = MagicMock()
                    q.execute = MagicMock(return_value=resp)
                    return q

                query.eq = MagicMock(side_effect=eq_side_effect)
                return query

            def insert_side_effect(payload):
                calls["pessoa_insert"].append(payload)
                q = MagicMock()
                resp = MagicMock()
                resp.data = [dict(payload, id="pessoa-nova-1")]
                q.execute = MagicMock(return_value=resp)
                return q

            tbl.select = MagicMock(side_effect=select_side_effect)
            tbl.insert = MagicMock(side_effect=insert_side_effect)

        return tbl

    client.table = MagicMock(side_effect=table_side_effect)
    return client, calls


_OPERACIONAL = {"id": "op-1", "nome": "Ana Op", "email": "ana@citi.com", "project_id": "proj-1"}


def test_claim_com_email_batendo_cria_pessoa_e_loga(monkeypatch):
    mock_sb, calls = _mock_client(operacional_row=_OPERACIONAL)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/signup/claim", json={
        "operacional_id": "op-1", "email": "ana@citi.com", "senha": "senha123",
    })

    assert resp.status_code == 201
    assert resp.json() == {"nome": "Ana Op", "cargo": "operacional"}
    assert resp.cookies.get("docudata_session") is not None
    assert calls["pessoa_insert"][0]["cargo"] == "operacional"
    assert calls["pessoa_insert"][0]["email"] == "ana@citi.com"


def test_claim_email_nao_bate_403(monkeypatch):
    mock_sb, calls = _mock_client(operacional_row=_OPERACIONAL)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/signup/claim", json={
        "operacional_id": "op-1", "email": "outra@citi.com", "senha": "senha123",
    })

    assert resp.status_code == 403
    assert len(calls["pessoa_insert"]) == 0


def test_claim_email_duplicado_409(monkeypatch):
    pessoa_existente = {"id": "pessoa-x", "email": "ana@citi.com"}
    mock_sb, calls = _mock_client(operacional_row=_OPERACIONAL, pessoa_existente=pessoa_existente)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/signup/claim", json={
        "operacional_id": "op-1", "email": "ana@citi.com", "senha": "senha123",
    })

    assert resp.status_code == 409
    assert len(calls["pessoa_insert"]) == 0


def test_signup_novo_cria_pessoa_sem_operacionais(monkeypatch):
    mock_sb, calls = _mock_client()
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/signup/novo", json={
        "nome": "Bruno Novo", "email": "bruno@citi.com", "senha": "senha123",
    })

    assert resp.status_code == 201
    assert resp.json() == {"nome": "Bruno Novo", "cargo": "operacional"}
    assert calls["pessoa_insert"][0]["cargo"] == "operacional"
    assert len(calls["operacionais_update"]) == 0


def test_signup_novo_email_duplicado_409(monkeypatch):
    pessoa_existente = {"id": "pessoa-x", "email": "bruno@citi.com"}
    mock_sb, calls = _mock_client(pessoa_existente=pessoa_existente)
    tc = _patch_and_client(monkeypatch, mock_sb)

    resp = tc.post("/auth/signup/novo", json={
        "nome": "Bruno Novo", "email": "bruno@citi.com", "senha": "senha123",
    })

    assert resp.status_code == 409
    assert len(calls["pessoa_insert"]) == 0
```
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python3 -c "import ast; ast.parse(open('services/auth.py').read()); ast.parse(open('routers/auth.py').read()); ast.parse(open('main.py').read()); print('syntax OK')"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_auth_login.py tests/test_auth_signup.py -q</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest -q</automated>
  </verify>
  <done>
    `POST /auth/login` autentica por email+senha (bcrypt), seta cookie httpOnly com JWT auto-contido, rejeita credenciais inválidas sem revelar qual campo está errado; `POST /auth/signup/claim` cria conta de operacional só quando o email bate com o cadastro existente, atualizando `operacionais.email` se estava vazio; `POST /auth/signup/novo` cria conta sem vínculo de projeto; nenhuma das duas rotas de cadastro aceita `cargo` diferente de `operacional`; `auth.router` é a única rota pública registrada em `main.py` até aqui.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate global de enforcement + CORS + camadas por papel</name>
  <files>
    docudata-backend/main.py,
    docudata-backend/routers/sprints.py,
    docudata-backend/routers/tasks.py,
    docudata-backend/tests/test_rbac_gate.py
  </files>
  <read_first>
    - docudata-backend/main.py (resultado da Task 2) — 10 chamadas `app.include_router(...)` existentes (linhas ~38-57 do arquivo original) mais a nova `auth.router` sem `dependencies=`
    - docudata-backend/routers/sprints.py linhas 1-25 (`router = APIRouter(tags=["sprints"])`, sem prefix — os paths completos já incluem `/projects/{project_id}/sprints` e `/sprints/{sprint_id}/...`) e os 5 decorators de rota: `POST /projects/{project_id}/sprints` (linha 22), `GET /projects/{project_id}/sprints` (linha 63), `PATCH /sprints/{sprint_id}/baseline` (linha 144), `DELETE /sprints/{sprint_id}` (linha 187), `PATCH /sprints/{sprint_id}/health` (linha 229)
    - docudata-backend/routers/tasks.py linhas 146-171 (`list_tasks` — `project_id: str = Query(...)`, é o único endpoint de `tasks.py` que ganha `require_project_access` nesta plan; os demais ficam com o gate básico, conforme o gap documentado em `<objective>`)
    - docudata-backend/routers/metricas.py linhas 1-59 (`router = APIRouter(prefix="/metricas", ...)`, `get_spi` — usado como rota de referência no teste de `require_not_operacional`)
  </read_first>
  <behavior>
    - `GET /metricas/{project_id}/spi` sem cookie -> 401
    - `GET /metricas/{project_id}/spi` com cookie de cargo=operacional -> 403
    - `GET /metricas/{project_id}/spi` com cookie de cargo=gerente -> 200 (mesmo comportamento de hoje, sem mudança de dado)
    - `GET /tasks?project_id=X` com cookie de cargo=operacional SEM linha em `operacionais` casando `project_id=X` + email -> 403
    - `GET /tasks?project_id=X` com cookie de cargo=operacional COM linha em `operacionais` casando -> 200
    - `GET /tasks?project_id=X` com cookie de cargo=gerente, independente de `operacionais` -> 200 (bypass total pra gerente/lider)
  </behavior>
  <action>
Service (`docudata-backend/services/auth.py` — já existe da Task 2, sem mudança adicional nesta task; `require_role`/`require_not_operacional`/`require_project_access` já foram escritos lá).

CORS + gate global (`docudata-backend/main.py`): adicione `import os` no topo (junto aos outros imports). Troque o bloco `app.add_middleware(CORSMiddleware, allow_origins=["*"], ...)` por:

```python
_frontend_url = os.environ.get("FRONTEND_URL", "http://localhost:3000")

app.add_middleware(
    CORSMiddleware,
    allow_origins=[_frontend_url],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

Depois do `app.include_router(auth.router)` (registrado na Task 2, continua sem `dependencies=`), troque cada uma das chamadas `app.include_router(X.router)` existentes por uma versão com o gate básico, EXCETO `metricas.router`, `painel.router` e `operacionais.router`, que recebem `require_not_operacional` (que já implica login válido, não precisa das duas):

```python
from services.auth import get_current_pessoa, require_not_operacional

app.include_router(projects.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(sprints.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(sprint_docs.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(ingest.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(generate.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(ingestions.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(search.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(export.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(commit_ingest.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(enrich.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(funcionalidades.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(painel.router, dependencies=[Depends(require_not_operacional)])
app.include_router(revisao_ingest.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(composer.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(aceite_ingest.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(boletins.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(sprint_funcionalidades.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(operacionais.router, dependencies=[Depends(require_not_operacional)])
app.include_router(tasks.router, dependencies=[Depends(get_current_pessoa)])
app.include_router(metricas.router, dependencies=[Depends(require_not_operacional)])
```

Adicione `from fastapi import BackgroundTasks, Depends, FastAPI` (acrescenta `Depends` ao import já existente de `fastapi` na linha 7). `/health`, `/notifications/check` e `/tasks/travamento/check` (definidos direto no `app`, não via `include_router`) ficam sem gate — decisão documentada em `<objective>`.

Camada extra em `sprints.py`: nos 3 endpoints administrativos, adicione `dependencies=[Depends(require_not_operacional)]` ao decorator (import `from fastapi import APIRouter, Depends, HTTPException` e `from services.auth import require_not_operacional, require_project_access` no topo do arquivo):

```python
@router.patch("/sprints/{sprint_id}/baseline", response_model=SprintBaselineResponse, dependencies=[Depends(require_not_operacional)])
async def update_baseline(sprint_id: str, data: SprintBaselineUpdate):
    ...

@router.delete("/sprints/{sprint_id}", status_code=204, dependencies=[Depends(require_not_operacional)])
async def delete_sprint(sprint_id: str):
    ...

@router.patch("/sprints/{sprint_id}/health", response_model=SprintResponse, dependencies=[Depends(require_not_operacional)])
async def update_health(sprint_id: str, data: SprintHealthUpdate):
    ...
```

Os outros 2 endpoints (`POST`/`GET /projects/{project_id}/sprints`) ganham `require_project_access` em vez de `require_not_operacional` (Operacional pode ler/criar sprint do próprio projeto):

```python
@router.post("/projects/{project_id}/sprints", response_model=SprintResponse, status_code=201, dependencies=[Depends(require_project_access)])
async def create_sprint(project_id: str, data: SprintCreate):
    ...

@router.get("/projects/{project_id}/sprints", response_model=list[SprintStatusResponse], dependencies=[Depends(require_project_access)])
async def list_sprints(project_id: str):
    ...
```

Não altere o corpo de nenhuma das 5 funções — só o decorator.

Camada extra em `tasks.py`: import `from fastapi import APIRouter, Depends, HTTPException, Query` (acrescenta `Depends`) e `from services.auth import require_project_access` no topo. No decorator de `list_tasks` (linha 146 do arquivo original):

```python
@router.get("", response_model=list[TaskResponse], dependencies=[Depends(require_project_access)])
async def list_tasks(
    project_id: str = Query(...),
    ...
```

Não altere o corpo da função nem nenhum outro endpoint de `tasks.py` (o gap de cobertura em `task_id`-scoped endpoints é intencional e documentado em `<objective>`).

Testes (`docudata-backend/tests/test_rbac_gate.py`, novo):

```python
"""Testes do gate global de enforcement (RBAC-02, RBAC-03) — require_not_operacional
via /metricas/{id}/spi, require_project_access via GET /tasks."""
from unittest.mock import MagicMock
from fastapi.testclient import TestClient

from services.auth import criar_jwt


def _mock_client_projeto_existe():
    client = MagicMock()

    def table_side_effect(name):
        tbl = MagicMock()

        def select_side_effect(cols):
            query = MagicMock()
            query.eq = MagicMock(return_value=query)
            query.order = MagicMock(return_value=query)
            resp = MagicMock()
            resp.data = [{"id": "proj-1"}] if name == "projects" else []
            query.execute = MagicMock(return_value=resp)
            return query

        tbl.select = MagicMock(side_effect=select_side_effect)
        return tbl

    client.table = MagicMock(side_effect=table_side_effect)
    return client


def _mock_client_operacional_vinculado(vinculado: bool):
    client = MagicMock()

    def table_side_effect(name):
        tbl = MagicMock()
        if name == "operacionais":
            def select_side_effect(cols):
                query = MagicMock()
                query.eq = MagicMock(return_value=query)
                resp = MagicMock()
                resp.data = [{"id": "op-1"}] if vinculado else []
                query.execute = MagicMock(return_value=resp)
                return query
            tbl.select = MagicMock(side_effect=select_side_effect)
        elif name == "tasks":
            def select_side_effect(cols):
                query = MagicMock()
                query.eq = MagicMock(return_value=query)
                query.order = MagicMock(return_value=query)
                resp = MagicMock()
                resp.data = []
                query.execute = MagicMock(return_value=resp)
                return query
            tbl.select = MagicMock(side_effect=select_side_effect)
        return tbl

    client.table = MagicMock(side_effect=table_side_effect)
    return client


def test_rota_protegida_sem_cookie_retorna_401(monkeypatch):
    from main import app
    tc = TestClient(app)

    resp = tc.get("/metricas/proj-1/spi")

    assert resp.status_code == 401


def test_operacional_bloqueado_em_rota_require_not_operacional(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-op-1", "op@citi.com", "operacional")

    resp = tc.get("/metricas/proj-1/spi", cookies={"docudata_session": token})

    assert resp.status_code == 403


def test_gerente_acessa_rota_require_not_operacional(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    mock_sb = _mock_client_projeto_existe()
    import routers.metricas as metricas_router
    monkeypatch.setattr(metricas_router, "get_client", lambda: mock_sb)
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-ger-1", "ger@citi.com", "gerente")

    resp = tc.get("/metricas/proj-1/spi", cookies={"docudata_session": token})

    assert resp.status_code == 200


def test_require_project_access_operacional_sem_vinculo_403(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    mock_sb = _mock_client_operacional_vinculado(vinculado=False)
    import services.auth as auth_service
    import routers.tasks as tasks_router
    monkeypatch.setattr(auth_service, "get_client", lambda: mock_sb)
    monkeypatch.setattr(tasks_router, "get_client", lambda: mock_sb)
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-op-1", "op@citi.com", "operacional")

    resp = tc.get("/tasks", params={"project_id": "proj-1"}, cookies={"docudata_session": token})

    assert resp.status_code == 403


def test_require_project_access_operacional_com_vinculo_200(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    mock_sb = _mock_client_operacional_vinculado(vinculado=True)
    import services.auth as auth_service
    import routers.tasks as tasks_router
    monkeypatch.setattr(auth_service, "get_client", lambda: mock_sb)
    monkeypatch.setattr(tasks_router, "get_client", lambda: mock_sb)
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-op-1", "op@citi.com", "operacional")

    resp = tc.get("/tasks", params={"project_id": "proj-1"}, cookies={"docudata_session": token})

    assert resp.status_code == 200


def test_require_project_access_gerente_sem_operacionais_bypass_200(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    mock_sb = _mock_client_operacional_vinculado(vinculado=False)
    import services.auth as auth_service
    import routers.tasks as tasks_router
    monkeypatch.setattr(auth_service, "get_client", lambda: mock_sb)
    monkeypatch.setattr(tasks_router, "get_client", lambda: mock_sb)
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-ger-1", "ger@citi.com", "gerente")

    resp = tc.get("/tasks", params={"project_id": "proj-1"}, cookies={"docudata_session": token})

    assert resp.status_code == 200
```
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python3 -c "import ast; ast.parse(open('main.py').read()); ast.parse(open('routers/sprints.py').read()); ast.parse(open('routers/tasks.py').read()); print('syntax OK')"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_rbac_gate.py -q</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest -q</automated>
    <automated>grep -c "allow_origins=\[_frontend_url\]" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/main.py"</automated>
  </verify>
  <done>
    Toda rota do backend, exceto `auth.router` e os 3 endpoints diretos em `app` (`/health`, `/notifications/check`, `/tasks/travamento/check`), exige cookie de sessão válido; `metricas.router`/`painel.router`/`operacionais.router` e os 3 endpoints administrativos de `sprints.py` rejeitam cargo=operacional com 403 sem afetar gerente/lider; `GET /tasks` e os 2 endpoints de listagem/criação de sprint por projeto verificam vínculo real em `operacionais` para cargo=operacional; CORS não usa mais `allow_origins=["*"]`; toda a suíte de testes (incluindo Phase 13/14/15) continua passando.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: audit_log genérico + GET /performance protegido</name>
  <files>
    docudata-backend/services/audit.py,
    docudata-backend/routers/performance.py,
    docudata-backend/main.py,
    docudata-backend/tests/test_performance_stub.py
  </files>
  <read_first>
    - docudata-backend/services/auth.py (resultado da Task 2) — `get_current_pessoa`, `require_role` já existem; `require_role("lider")` retorna um dependency callable
    - docudata-backend/main.py (resultado da Task 3) — padrão de `app.include_router(X.router, dependencies=[Depends(...)])` a seguir para `performance.router`, mas desta vez a dependency vai direto no decorator da rota (não no `include_router`), já que o router só tem essa uma rota
  </read_first>
  <behavior>
    - `GET /performance` sem cookie -> 401
    - `GET /performance` com cargo=gerente -> 403
    - `GET /performance` com cargo=lider -> 200, body `{"status": "ok", ...}`, e uma linha é inserida em `audit_log` com `pessoa_email` do líder, `rota="/performance"`, `acao="acesso"`
  </behavior>
  <action>
Service (`docudata-backend/services/audit.py`, novo arquivo):

```python
"""
Log de auditoria genérico (RBAC-05) — pronto para as Phases 17/18
reutilizarem quando as rotas de avaliação do gerente e score existirem.
Nesta phase, só GET /performance chama isso.
"""
from datetime import datetime, timezone

from services.supabase_client import get_client


def registrar_auditoria(pessoa: dict, rota: str, acao: str) -> None:
    client = get_client()
    client.table("audit_log").insert({
        "pessoa_id": pessoa["id"],
        "pessoa_email": pessoa["email"],
        "rota": rota,
        "acao": acao,
        "created_at": datetime.now(timezone.utc).isoformat(),
    }).execute()
```

Router (`docudata-backend/routers/performance.py`, novo arquivo):

```python
from fastapi import APIRouter, Depends

from services.audit import registrar_auditoria
from services.auth import get_current_pessoa, require_role

router = APIRouter(tags=["performance"])


@router.get("/performance", dependencies=[Depends(require_role("lider"))])
async def performance(pessoa: dict = Depends(get_current_pessoa)):
    registrar_auditoria(pessoa, "/performance", "acesso")
    return {"status": "ok", "message": "Ranking ainda não implementado — Phase 19"}
```

`main.py`: adicione `performance` ao import de `routers` (mesma linha do `auth`) e `app.include_router(performance.router)` — sem `dependencies=` no `include_router` (a proteção já está no decorator da própria rota via `require_role("lider")`, que implica login válido).

Testes (`docudata-backend/tests/test_performance_stub.py`, novo):

```python
"""Testes para GET /performance (RBAC-04, RBAC-05)."""
from unittest.mock import MagicMock
from fastapi.testclient import TestClient

from services.auth import criar_jwt


def _mock_audit_client():
    client = MagicMock()
    calls = {"audit_insert": []}

    def table_side_effect(name):
        tbl = MagicMock()
        if name == "audit_log":
            def insert_side_effect(payload):
                calls["audit_insert"].append(payload)
                q = MagicMock()
                resp = MagicMock()
                resp.data = [dict(payload, id="audit-1")]
                q.execute = MagicMock(return_value=resp)
                return q
            tbl.insert = MagicMock(side_effect=insert_side_effect)
        return tbl

    client.table = MagicMock(side_effect=table_side_effect)
    return client, calls


def test_performance_sem_cookie_401(monkeypatch):
    from main import app
    tc = TestClient(app)

    resp = tc.get("/performance")

    assert resp.status_code == 401


def test_performance_nao_lider_retorna_403(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-ger-1", "ger@citi.com", "gerente")

    resp = tc.get("/performance", cookies={"docudata_session": token})

    assert resp.status_code == 403


def test_performance_lider_200_grava_audit_log(monkeypatch):
    monkeypatch.setenv("JWT_SECRET", "test-secret-nao-usar-em-producao")
    mock_sb, calls = _mock_audit_client()
    import services.audit as audit_service
    monkeypatch.setattr(audit_service, "get_client", lambda: mock_sb)
    from main import app
    tc = TestClient(app)
    token = criar_jwt("pessoa-lid-1", "lider@citi.com", "lider")

    resp = tc.get("/performance", cookies={"docudata_session": token})

    assert resp.status_code == 200
    assert resp.json()["status"] == "ok"
    assert len(calls["audit_insert"]) == 1
    assert calls["audit_insert"][0]["pessoa_email"] == "lider@citi.com"
    assert calls["audit_insert"][0]["rota"] == "/performance"
    assert calls["audit_insert"][0]["acao"] == "acesso"
```
  </action>
  <verify>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python3 -c "import ast; ast.parse(open('services/audit.py').read()); ast.parse(open('routers/performance.py').read()); ast.parse(open('main.py').read()); print('syntax OK')"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest tests/test_performance_stub.py -q</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && .venv/bin/pytest -q</automated>
  </verify>
  <done>
    `GET /performance` retorna 401 sem sessão, 403 pra cargo != lider, 200 pra lider e grava uma linha em `audit_log` a cada acesso; `registrar_auditoria` é genérico e reutilizável, sem nenhum acoplamento com dado de score que ainda não existe.
  </done>
</task>

<task type="auto">
  <name>Task 5: Frontend — login, cadastro, guarda de rota, abas por papel (Onda 2)</name>
  <files>
    docudata-frontend/app/lib/api.ts,
    docudata-frontend/app/components/AuthGuard.tsx,
    docudata-frontend/app/login/page.tsx,
    docudata-frontend/app/cadastro/page.tsx,
    docudata-frontend/app/layout.tsx,
    docudata-frontend/app/projects/[id]/page.tsx
  </files>
  <read_first>
    - docudata-frontend/app/lib/api.ts linhas 1-17 (const `API`, `ValidationError`) — 66 chamadas `fetch(` existentes no arquivo (confirmado via grep), todas no formato `await fetch(...)` ou `return fetch(...)`, nenhuma passa `credentials`
    - docudata-frontend/app/layout.tsx inteiro (Server Component, sem "use client") — vai importar e renderizar `AuthGuard` envolvendo `{children}`
    - docudata-frontend/app/projects/[id]/page.tsx linha 59 (`type TabId = ...`), linha 202 (`const [activeTab, setActiveTab] = useState<TabId>("sprints")`), linhas 592-605 (array de tabs passado pro componente `Tabs`)
    - docudata-frontend/app/components/Tabs.tsx inteiro — `TabItem[]` já suporta filtrar o array antes de passar, nenhuma mudança necessária nesse componente
  </read_first>
  <action>
`api.ts` — passo 1, adicione logo após a linha `const API = ...` (linha 1):

```typescript
function apiFetch(input: string, init?: RequestInit): Promise<Response> {
  return globalThis.fetch(input, { ...init, credentials: "include" });
}

export interface MeResponse {
  nome: string;
  email: string;
  cargo: "lider" | "gerente" | "operacional";
}

export interface LoginResponse {
  nome: string;
  cargo: "lider" | "gerente" | "operacional";
}

export interface OperacionalSemConta {
  operacional_id: string;
  nome: string;
  project_id: string;
  project_name: string;
}

export async function login(email: string, senha: string): Promise<LoginResponse> {
  const res = await apiFetch(`${API}/auth/login`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ email, senha }),
  });
  if (res.status === 401) throw new Error("Email ou senha inválidos");
  if (!res.ok) throw new Error("Erro ao fazer login");
  return res.json();
}

export async function logout(): Promise<void> {
  await apiFetch(`${API}/auth/logout`, { method: "POST" });
}

export async function getMe(): Promise<MeResponse> {
  const res = await apiFetch(`${API}/auth/me`);
  if (!res.ok) throw new Error("Não autenticado");
  return res.json();
}

export async function listOperacionaisSemConta(): Promise<OperacionalSemConta[]> {
  const res = await apiFetch(`${API}/auth/operacionais-sem-conta`);
  if (!res.ok) throw new Error("Erro ao buscar operacionais");
  return res.json();
}

export async function signupClaim(operacional_id: string, email: string, senha: string): Promise<LoginResponse> {
  const res = await apiFetch(`${API}/auth/signup/claim`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ operacional_id, email, senha }),
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error((err as { detail?: string }).detail ?? "Erro ao criar conta");
  }
  return res.json();
}

export async function signupNovo(nome: string, email: string, senha: string): Promise<LoginResponse> {
  const res = await apiFetch(`${API}/auth/signup/novo`, {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ nome, email, senha }),
  });
  if (!res.ok) {
    const err = await res.json().catch(() => ({}));
    throw new Error((err as { detail?: string }).detail ?? "Erro ao criar conta");
  }
  return res.json();
}
```

`api.ts` — passo 2, mecânico: troque toda ocorrência de `await fetch(` por `await apiFetch(` e toda ocorrência de `return fetch(` por `return apiFetch(` no resto do arquivo (as 66 chamadas já existentes). Rode:
```bash
cd docudata-frontend && sed -i '' 's/await fetch(/await apiFetch(/g; s/return fetch(/return apiFetch(/g' app/lib/api.ts
```
A função `apiFetch` adicionada no passo 1 usa `globalThis.fetch(...)` (não `fetch(...)` bare), então o `sed` não a afeta — confirme com o grep do `<verify>` abaixo.

`AuthGuard.tsx` (`docudata-frontend/app/components/AuthGuard.tsx`, novo arquivo):

```tsx
"use client";

import { createContext, useContext, useEffect, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import { getMe, type MeResponse } from "../lib/api";

const PUBLIC_PATHS = new Set(["/login", "/cadastro"]);

const AuthContext = createContext<MeResponse | null>(null);

export function useAuth(): MeResponse | null {
  return useContext(AuthContext);
}

export default function AuthGuard({ children }: { children: React.ReactNode }) {
  const pathname = usePathname();
  const router = useRouter();
  const [pessoa, setPessoa] = useState<MeResponse | null>(null);
  const [checked, setChecked] = useState(false);

  useEffect(() => {
    if (PUBLIC_PATHS.has(pathname)) {
      setChecked(true);
      return;
    }
    getMe()
      .then((me) => {
        setPessoa(me);
        setChecked(true);
      })
      .catch(() => {
        router.replace("/login");
      });
  }, [pathname, router]);

  if (PUBLIC_PATHS.has(pathname)) {
    return <>{children}</>;
  }

  if (!checked) {
    return null;
  }

  return <AuthContext.Provider value={pessoa}>{children}</AuthContext.Provider>;
}
```

`layout.tsx` — importe `AuthGuard` e envolva `{children}`:

```tsx
import type { Metadata } from "next";
import "./globals.css";
import AuthGuard from "./components/AuthGuard";

export const metadata: Metadata = {
  title: "Agente Documentador",
  description: "Documentação automática de projetos de dados",
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="pt-BR">
      <body>
        <AuthGuard>{children}</AuthGuard>
      </body>
    </html>
  );
}
```

`login/page.tsx` (`docudata-frontend/app/login/page.tsx`, novo arquivo):

```tsx
"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { login } from "../lib/api";

export default function LoginPage() {
  const router = useRouter();
  const [email, setEmail] = useState("");
  const [senha, setSenha] = useState("");
  const [err, setErr] = useState("");
  const [loading, setLoading] = useState(false);

  async function handleSubmit(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setErr("");
    try {
      await login(email, senha);
      router.push("/");
      router.refresh();
    } catch (e) {
      setErr(e instanceof Error ? e.message : "Erro ao fazer login");
    } finally {
      setLoading(false);
    }
  }

  const inputSt: React.CSSProperties = { padding: "10px 12px", border: "1px solid #e4e4ea", borderRadius: 8, fontSize: 14 };

  return (
    <div style={{ maxWidth: 360, margin: "80px auto", padding: 24 }}>
      <h1 style={{ fontSize: 20, fontWeight: 700, marginBottom: 24 }}>Entrar — DocuData</h1>
      <form onSubmit={handleSubmit} style={{ display: "flex", flexDirection: "column", gap: 12 }}>
        <input type="email" placeholder="Email" value={email} onChange={(e) => setEmail(e.target.value)} required style={inputSt} />
        <input type="password" placeholder="Senha" value={senha} onChange={(e) => setSenha(e.target.value)} required style={inputSt} />
        {err && <p style={{ color: "#dc2626", fontSize: 13 }}>{err}</p>}
        <button type="submit" disabled={loading} style={{ padding: "10px 12px", borderRadius: 8, background: "#111116", color: "#fff", border: "none", fontWeight: 600 }}>
          {loading ? "Entrando..." : "Entrar"}
        </button>
      </form>
      <p style={{ marginTop: 16, fontSize: 13 }}>
        Ainda não tem conta? <a href="/cadastro">Cadastre-se</a>
      </p>
    </div>
  );
}
```

`cadastro/page.tsx` (`docudata-frontend/app/cadastro/page.tsx`, novo arquivo):

```tsx
"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { listOperacionaisSemConta, signupClaim, signupNovo, type OperacionalSemConta } from "../lib/api";

export default function CadastroPage() {
  const router = useRouter();
  const [modo, setModo] = useState<"lista" | "claim" | "novo">("lista");
  const [operacionais, setOperacionais] = useState<OperacionalSemConta[]>([]);
  const [selecionado, setSelecionado] = useState<OperacionalSemConta | null>(null);
  const [nome, setNome] = useState("");
  const [email, setEmail] = useState("");
  const [senha, setSenha] = useState("");
  const [err, setErr] = useState("");
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    listOperacionaisSemConta().then(setOperacionais).catch(() => setOperacionais([]));
  }, []);

  const inputSt: React.CSSProperties = { padding: "10px 12px", border: "1px solid #e4e4ea", borderRadius: 8, fontSize: 14 };
  const btnPrimary: React.CSSProperties = { padding: "10px 12px", borderRadius: 8, background: "#111116", color: "#fff", border: "none", fontWeight: 600 };
  const btnGhost: React.CSSProperties = { fontSize: 13, background: "none", border: "none", color: "#9696a0", cursor: "pointer" };

  async function handleClaim(e: React.FormEvent) {
    e.preventDefault();
    if (!selecionado) return;
    setLoading(true);
    setErr("");
    try {
      await signupClaim(selecionado.operacional_id, email, senha);
      router.push("/");
      router.refresh();
    } catch (e) {
      setErr(e instanceof Error ? e.message : "Erro ao criar conta");
    } finally {
      setLoading(false);
    }
  }

  async function handleNovo(e: React.FormEvent) {
    e.preventDefault();
    setLoading(true);
    setErr("");
    try {
      await signupNovo(nome, email, senha);
      router.push("/");
      router.refresh();
    } catch (e) {
      setErr(e instanceof Error ? e.message : "Erro ao criar conta");
    } finally {
      setLoading(false);
    }
  }

  return (
    <div style={{ maxWidth: 420, margin: "60px auto", padding: 24 }}>
      <h1 style={{ fontSize: 20, fontWeight: 700, marginBottom: 24 }}>Criar conta — DocuData</h1>

      {modo === "lista" && (
        <div>
          <p style={{ fontSize: 13, color: "#64748b", marginBottom: 12 }}>Você já é operacional em algum projeto?</p>
          <div style={{ display: "flex", flexDirection: "column", gap: 6, maxHeight: 240, overflowY: "auto" }}>
            {operacionais.map((op) => (
              <button
                key={op.operacional_id}
                onClick={() => { setSelecionado(op); setModo("claim"); }}
                style={{ textAlign: "left", padding: "8px 12px", border: "1px solid #e4e4ea", borderRadius: 8, background: "#fff", cursor: "pointer" }}
              >
                {op.nome} <span style={{ color: "#9696a0", fontSize: 12 }}>— {op.project_name}</span>
              </button>
            ))}
          </div>
          <button onClick={() => setModo("novo")} style={{ ...btnGhost, marginTop: 16 }}>
            Não me encontrei na lista — sou novo
          </button>
        </div>
      )}

      {modo === "claim" && selecionado && (
        <form onSubmit={handleClaim} style={{ display: "flex", flexDirection: "column", gap: 12 }}>
          <p style={{ fontSize: 13 }}>Criando conta para <strong>{selecionado.nome}</strong> ({selecionado.project_name})</p>
          <input type="email" placeholder="Seu email" value={email} onChange={(e) => setEmail(e.target.value)} required style={inputSt} />
          <input type="password" placeholder="Crie uma senha" value={senha} onChange={(e) => setSenha(e.target.value)} required minLength={6} style={inputSt} />
          {err && <p style={{ color: "#dc2626", fontSize: 13 }}>{err}</p>}
          <button type="submit" disabled={loading} style={btnPrimary}>{loading ? "Criando..." : "Criar conta"}</button>
          <button type="button" onClick={() => setModo("lista")} style={btnGhost}>Voltar</button>
        </form>
      )}

      {modo === "novo" && (
        <form onSubmit={handleNovo} style={{ display: "flex", flexDirection: "column", gap: 12 }}>
          <input placeholder="Seu nome" value={nome} onChange={(e) => setNome(e.target.value)} required style={inputSt} />
          <input type="email" placeholder="Seu email" value={email} onChange={(e) => setEmail(e.target.value)} required style={inputSt} />
          <input type="password" placeholder="Crie uma senha" value={senha} onChange={(e) => setSenha(e.target.value)} required minLength={6} style={inputSt} />
          {err && <p style={{ color: "#dc2626", fontSize: 13 }}>{err}</p>}
          <button type="submit" disabled={loading} style={btnPrimary}>{loading ? "Criando..." : "Criar conta"}</button>
          <button type="button" onClick={() => setModo("lista")} style={btnGhost}>Voltar</button>
        </form>
      )}
    </div>
  );
}
```

`projects/[id]/page.tsx` — importe `useAuth` (linha do bloco de imports, junto de `Tabs`):
```tsx
import { useAuth } from "../../components/AuthGuard";
```
Logo após `const [activeTab, setActiveTab] = useState<TabId>("sprints");` (linha 202), adicione:
```tsx
const auth = useAuth();
const cargo = auth?.cargo ?? "lider";
const OPERACIONAL_TABS = new Set(["sprints", "tasks", "tecnologias", "documentos"]);
```
No JSX do componente `Tabs` (linhas 592-605), encadeie `.filter(...)` no array antes de fechar o array literal:
```tsx
      <Tabs
        tabs={[
          { id: "sprints", label: "Sprints", badge: totalPendencias > 0 ? `${totalPendencias} pend.` : undefined },
          { id: "escopo", label: "Escopo", badge: funcionalidades.length || undefined },
          { id: "painel", label: "Painel" },
          { id: "tasks", label: "Tasks" },
          { id: "metricas", label: "Métricas" },
          { id: "tecnologias", label: "Tecnologias" },
          { id: "documentos", label: "Documentos", badge: docs.length || undefined },
          { id: "config", label: "Configurações" },
        ].filter((t) => cargo !== "operacional" || OPERACIONAL_TABS.has(t.id))}
        active={activeTab}
        onChange={(t) => setActiveTab(t as TabId)}
      />
```
Não altere nenhum outro bloco condicional `{activeTab === "..." && (...)}` — a filtragem do array de tabs já impede o Operacional de clicar nelas; o backend (Task 3) é quem garante que mesmo trocando `activeTab` manualmente via devtools, as chamadas de API dessas abas retornam 403.
  </action>
  <verify>
    <automated>grep -c "await apiFetch(\|return apiFetch(" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/lib/api.ts"</automated>
    <automated>grep -c "await fetch(\|return fetch(" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/lib/api.ts" || echo 0</automated>
    <automated>grep -c "globalThis.fetch" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/lib/api.ts"</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend" && npx tsc --noEmit</automated>
    <automated>cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend" && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    `api.ts` manda `credentials: "include"` em todas as chamadas (as 66 originais renomeadas + as 6 novas de auth); `AuthGuard` redireciona pra `/login` quando `GET /auth/me` retorna 401, exceto nas próprias páginas `/login`/`/cadastro`; `/login` e `/cadastro` funcionam ponta a ponta contra os endpoints da Task 2; abas `escopo`/`painel`/`metricas`/`config` somem da UI pra `cargo=operacional`; `npx tsc --noEmit` e `npm run build` sem erros novos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| client -> POST /auth/login, /auth/signup/* | credenciais em texto puro no body (HTTPS em produção); sem rate limiting nesta phase |
| client -> cookie `docudata_session` | JWT httpOnly+Secure+SameSite=None; cliente não lê nem manipula o conteúdo, só o servidor decodifica |
| operacional (cargo) -> rotas escopadas por projeto | `require_project_access` casa `pessoa.email` com `operacionais.email`+`project_id` — cobertura parcial, documentada em `<objective>` (task_id-scoped e tecnologias/documentos routers ficam de fora) |
| qualquer pessoa autenticada -> `/auth/signup/claim`, `/auth/signup/novo` | criam só `cargo=operacional`; `cargo=gerente`/`lider` nunca são alcançáveis pela API |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-16-01 | Spoofing | `POST /auth/login` sem rate limiting/lockout | medium | accept | MVP interno, poucos usuários conhecidos; rate limiting fica para quando o volume de usuários justificar |
| T-16-02 | Tampering | `pessoa.cargo` nunca é aceito em nenhum payload de request (`SignupClaimRequest`/`SignupNovoRequest` não têm campo `cargo`) — sempre hardcoded `"operacional"` no server | low | mitigate | Cliente não pode se auto-promover a gerente/lider por nenhum caminho da API |
| T-16-03 | Information Disclosure | `require_project_access`/`require_not_operacional` não cobrem `task_id`-scoped endpoints (`PATCH /tasks/{id}`, `POST /tasks/{id}/mover`), os routers de tecnologias/documentos, `funcionalidades.router` (dados de Escopo) nem `projects.router` (configurações) | medium | accept | Documentado explicitamente em `<objective>` como gap desta plan; exige que o Operacional autenticado chame a API diretamente (não exposto por nenhuma tela sua) ou já conheça um UUID de outro projeto; sem superfície de enumeração na UI |
| T-16-04 | Tampering | Cookie `Secure`+`SameSite=None` não funciona em dev local via `http://localhost` no browser (só via `TestClient`/pytest, que não aplica essa restrição) | low | accept | Fora de escopo: dev local do fluxo de login via browser real exige HTTPS local ou testar contra o backend já deployado no Railway; não bloqueia a suíte automatizada |
| T-16-05 | Elevation of Privilege | `require_not_operacional` e `require_project_access` dependem de `pessoa["cargo"]`/`pessoa["email"]` extraídos do JWT, nunca de uma nova consulta ao banco por request | low | mitigate | JWT é assinado com `JWT_SECRET` (HS256) — impossível forjar sem a chave; token expira em 8h, sem renovação silenciosa |
| T-16-06 | Denial of Service | `GET /auth/operacionais-sem-conta` é pública e faz 3 selects sem paginação | low | accept | Volume do MVP é baixo (poucos projetos/operacionais); paginação fica para quando o volume justificar |

</threat_model>

<verification>
- `python3 -c "import ast; ast.parse(...)"` em todos os arquivos `.py` novos/modificados: sem `SyntaxError`
- `docudata-backend/.venv/bin/pytest -q` (suíte completa, incluindo Phase 13/14/15 já existentes): todos passam, nenhuma regressão
- `grep -c "CREATE TABLE IF NOT EXISTS pessoa\|CREATE TABLE IF NOT EXISTS audit_log"` em `supabase_schema.sql`: 2
- `grep -c "allow_origins=\[_frontend_url\]"` em `main.py`: >= 1 (CORS não usa mais `["*"]`)
- `grep -c "await fetch(\|return fetch("` em `api.ts`: 0 (todas as chamadas passam por `apiFetch`)
- `npx tsc --noEmit` (frontend): sem erros novos
- `npm run build` (frontend): compila sem erros
- Manual spot-check (não automatizado): aplicar a migration no Supabase; inserir manualmente um `pessoa` com `cargo=lider` via SQL Editor; fazer login em `/login`; confirmar que as abas Métricas/Painel/Escopo/Configurações aparecem; criar um `operacional` de teste, deslogar, ir em `/cadastro`, clicar "sou eu", criar senha; logar como esse operacional e confirmar que só vê Sprints/Tasks/Tecnologias/Documentos e que `GET /performance` retorna 403 pra ele
</verification>

<success_criteria>
- [ ] Tabela `pessoa` existe; login por email/senha resolve o cargo da sessão via cookie JWT httpOnly — RBAC-01
- [ ] cargo=gerente/lider acessam qualquer projeto sem restrição; cargo=operacional só passa em `require_project_access` quando existe `operacionais.email` == `pessoa.email` no `project_id` da rota (cobertura documentada como parcial — só `GET /tasks` e os 2 endpoints de sprint por projeto) — RBAC-02
- [ ] Nenhuma rota de `metricas.py`/`painel.py`/`operacionais.py` nem os 3 endpoints administrativos de `sprints.py` retornam 200 para cargo=operacional — RBAC-02, RBAC-03 (decisão explícita: dados interinos de Phase 13 continuam abertos pra Gerente, RBAC-03 "score real" fica pra Phase 18/19)
- [ ] `GET /performance` retorna 403 pra não-Líder via `require_role("lider")` dedicado (não reaproveita `require_not_operacional`) — RBAC-04
- [ ] Toda leitura de `/performance` grava uma linha em `audit_log`; mecanismo `registrar_auditoria` genérico e pronto pra Phase 17/18 — RBAC-05
- [ ] Nenhuma rota de auto-cadastro aceita ou grava `cargo` diferente de `operacional`
- [ ] `docudata-frontend`: `/login` e `/cadastro` funcionam ponta a ponta; abas restritas somem da UI pra cargo=operacional; `credentials:"include"` em toda chamada de `api.ts`
- [ ] Toda a suíte de testes de Phase 13/14/15 continua passando sem alteração de comportamento
- [ ] Gaps documentados em `<objective>` (task_id-scoped endpoints, tecnologias/documentos routers, recuperação de senha, convite de gerente/lider, refresh token) permanecem não implementados nesta plan, conforme decisão explícita de escopo
</success_criteria>

<output>
Create `.planning/quick/260904-rb1-implementar-phase-16-rbac-login-leve/260904-rb1-SUMMARY.md` when done
</output>