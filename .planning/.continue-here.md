---
context: feature
phase: kanban-tasks-spi
task: 5
total_tasks: 6
status: in_progress
last_updated: 2026-09-02T04:00:00Z
---

# Kanban de Tasks, Operacionais, Pontos e SPI — Handoff

## Plano aprovado
`.claude/plans/eu-quero-implementa-essas-cached-tome.md`

<current_state>
Wave 1 (Fundação de dados e CRUD) foi concluída e commitada.
Wave 2 (WIP limits + task events) foi concluída nesta sessão.
Wave 3 (Frontend Kanban de tasks) foi concluída nesta sessão — build TypeScript limpo.
Wave 4 backend foi concluída nesta sessão (kanban block na geração + backlog tasks no rascunho).
Wave 4b (task_sugestoes + banner na UI) ainda não implementada.
Migrations SQL rodadas no Supabase (exceto DROP/CREATE VIEW que foi corrigida na sessão).
</current_state>

<completed_work>

**Wave 1 — CONCLUÍDA:**
- `models/schemas.py` — schemas novos: `OperacionalCreate/Update/Response`, `TaskCreate/Update/Response`, `TaskReordenarItem`, `TaskTransicaoResponse`, `SprintBaselineUpdate/Response`
- `routers/operacionais.py` — CRUD completo (POST, GET /projects/{id}, PATCH, DELETE com guard de tasks vinculadas)
- `routers/tasks.py` — CRUD + PATCH com `_registrar_task_transicao` automático (espelha padrão de `funcionalidades.py:371-458`), endpoint `/mover`, `/reordenar/batch`
- `routers/sprints.py` — endpoint `PATCH /sprints/{id}/baseline` com lock imutável
- `main.py` — routers `operacionais` e `tasks` registrados
- `supabase_schema.sql` — migrations: tabelas `operacionais`, `tasks`, `task_transicoes`, `task_sugestoes`; colunas novas em `sprints` (pontos_previstos, faturamento_previsto, baseline_locked_at, data_inicio, data_fim) e `projects` (wip_config, valor_por_ponto); view `sprint_spi`
- Imports verificados com `.venv/bin/python` — OK
</completed_work>

<completed_work>

**Wave 2 — CONCLUÍDA:**
- `services/wip_check.py` — `check_wip(client, project_id, operacional_id, coluna_destino) -> (ok, motivo)`: lê `projects.wip_config`, verifica `por_coluna_em_andamento` e `por_pessoa`, retorna 409 se ultrapassado
- `services/task_events.py` — `on_task_transition(client, task, campo, de, para) -> bool`: loga `file_type=task_event` em `ingestions` (só se a task tem sprint_id), detecta funcionalidade 100% concluída
- `routers/tasks.py` — WIP check antes de aceitar mudança de coluna; `on_task_transition` chamado após update bem-sucedido
</completed_work>

<remaining_work>

<completed_work>

**Wave 3 — CONCLUÍDA:**
- `app/lib/api.ts` — tipos e funções: `OperacionalResponse`, `TaskKanbanResponse`, `TaskTransicaoKanban`; `listOperacionais`, `createOperacional`, `updateOperacional`, `deleteOperacional`, `listTasksKanban`, `createTaskKanban`, `patchTaskKanban`, `moverTaskKanban`, `deleteTaskKanban`, `listTaskTransicoesKanban`
- `app/components/TasksKanbanTab.tsx` — kanban 3 colunas, drag-and-drop nativo, filtros sprint/operacional/funcionalidade, modal Nova/Editar task (checklist inline, histórico de transições, formatação de duração), WIP error banner com 409
- `app/projects/[id]/page.tsx` — aba "Tasks" adicionada ao TabId + Tabs; estado `operacionais` carregado no bootstrap; `OperacionaisSection` inline na aba Config (CRUD: add/desativar/excluir)
- `app/components/PainelTab.tsx` — chip "N/M tasks ✓" no KanbanCard de funcionalidade (verde quando 100%, cinza caso contrário); tasks carregadas uma vez via `listTasksKanban`
- Build TypeScript: zero erros, next build OK
</completed_work>

<completed_work>

**Wave 4 backend — CONCLUÍDA:**
- `graphs/generation_graph.py` — `_bloco_kanban_tasks()` busca tasks da sprint e formata como bloco de texto; `compilar_contexto` o injeta para tipos sprint-scoped (`repasse_semanal`, `retrospectiva`, `review`)
- `routers/composer.py` — `RascunhoResponse` ganhou campo `backlog_tasks: list[dict]`; `GET /rascunho` busca tasks em `planejado`/`em_andamento` da sprint e as retorna
</completed_work>

**Wave 4b — task_sugestoes + banner (pendente):**
- Handler `on_review_ingested`: após ingerir review, fazer match fuzzy tasks ↔ conteúdo e criar registros em `task_sugestoes`
- Banner na aba Tasks quando há sugestões pendentes (aceita/rejeita por task)

**Wave 5 — Métricas:**
- `routers/metricas.py`: SPI, cycle-time, throughput, CFD, performance por operacional
- `MetricasTab.tsx` com Recharts

**Wave 6 — Opcional:**
- Ganchos daily/commit/retrospectiva
- DoR/DoD bloqueante
- Auto-derivação de status_saude a partir de SPI
</remaining_work>

<decisions_made>

- Task é first-class (tabela própria), não jsonb — para individualizar pontos por operacional
- Dois Kanbans separados: funcionalidades (já existe, não muda) + tasks (novo)
- Só 3 colunas (planejado/em_andamento/concluida) — sem Em Review/Bloqueado como coluna
- Bloqueado = flag booleana na task, não coluna separada
- Operacional é entidade por projeto (não global) — sem tabela pessoas global no MVP
- SPI por sprint via pontos (EVM): Σ pontos_realizados / Σ pontos_previstos
- Sem importador de planilha CCE — gerentes cadastram tasks manualmente
- Sem campo `tipo` (dev/design/etc) nas tasks
- Backlog → planning: Kanban alimenta o rascunho do composer automaticamente
- Review → Kanban: match fuzzy propõe sugestões, gerente aceita/rejeita (nunca aplica automaticamente)
- `_registrar_task_transicao` calculado em Python no handler PATCH (mesmo padrão Phase 7 de transicoes_status)
</decisions_made>

<blockers>

- **Migrations não rodadas no Supabase**: as queries em `supabase_schema.sql` (seção "Wave: Kanban de Tasks") precisam ser executadas no dashboard do Supabase antes de qualquer teste E2E.
- **Sem testes escritos**: Wave 1 foi implementada mas sem testes unitários/integração. Cenário de verificação está no plano aprovado.
</blockers>

## Required Reading (em ordem)
1. `.claude/plans/eu-quero-implementa-essas-cached-tome.md` — plano completo com 6 waves, modelo de dados e cenários de verificação
2. `docudata-backend/routers/tasks.py` — implementação Wave 1 (padrão a ser seguido nas waves 2+)
3. `docudata-backend/routers/funcionalidades.py:371-458` — padrão de PATCH com transição automática (referência)

## Infrastructure State
- Backend: FastAPI em `/docudata-backend`, venv em `.venv`
- Frontend: Next.js em `/docudata-frontend`
- Migrations pendentes: seção `-- Wave: Kanban de Tasks, Operacionais, SPI` ao fim de `supabase_schema.sql`

<next_action>
1. Wave 4b: handler `on_review_ingested` + tabela `task_sugestoes` + banner na UI
2. Wave 5: Métricas (SPI, cycle-time, throughput, CFD) — `routers/metricas.py` + `MetricasTab.tsx`
</next_action>
