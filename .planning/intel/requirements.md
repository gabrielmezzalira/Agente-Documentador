# Requirements (from PRD-type documents)

No PRD-type documents were present in this ingestion batch (`CLASSIFICATIONS_DIR` contained one classified document, type `SPEC`). No entries extracted.

Note: `SDD-hub-de-projetos-v3.md` contains a "Critérios de aceite (Definition of Done)" section, but since the document is classified `SPEC` (not `PRD`), that content is preserved verbatim in `constraints.md` instead, per the type→file routing rule.

---
*Synthesized by gsd-doc-synthesizer — merge run (re-synthesis, v3 supersedes v2).*
