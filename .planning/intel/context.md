# Context (from DOC-type documents)

No DOC-type documents were present in this ingestion batch (`CLASSIFICATIONS_DIR` contained one classified document, type `SPEC`). No entries extracted.

---
*Synthesized by gsd-doc-synthesizer — merge run (re-synthesis, v3 supersedes v2).*
