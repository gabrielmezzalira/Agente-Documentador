---
phase: 10-composer-de-planning
plan: 02
type: execute
wave: 2
depends_on:
  - "10-01"
files_modified:
  - docudata-backend/routers/composer.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/components/PlanningTab.tsx
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - M4 (§5)

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "POST /composer/gerar chama Gemini com gemini-3.5-flash-lite e retorna {markdown} sem persistir — D-05/D-06"
    - "Aba 'Planning' aparece em Tabs.tsx e renderiza PlanningTab.tsx — D-08"
    - "Tela de boas-vindas exibe throughput_ref das últimas 3 sprints e botão 'Iniciar Planning da Sprint N' — D-10"
    - "Wizard tem 4 steps horizontais no topo com conteúdo abaixo; zero className em PlanningTab.tsx — D-09"
    - "Step 1 lista funcionalidades; transbordos no topo com badge laranja 'Transbordado' (borderLeft laranja)"
    - "Step 2 exibe checkboxes de criterios_aceite por índice; botão 'Próximo' bloqueado se alguma funcionalidade sem índice marcado — D-04/D-03"
    - "Step 3 permite definir responsável por funcionalidade selecionada"
    - "Step 4 chama POST /composer/gerar ao entrar no step, exibe preview via react-markdown, botão 'Confirmar Planning' chama POST /composer/confirmar — D-06/D-07"
    - "Rascunho nunca se torna planning oficial sem confirmação explícita — D-06"
    - "Gerente pode sair e voltar: ao entrar na aba Planning, GET rascunho retoma o step salvo — D-01"
  artifacts:
    - docudata-frontend/app/components/PlanningTab.tsx
    - docudata-frontend/app/lib/api.ts (funções getRascunho, patchRascunho, gerarPlanning, confirmarPlanning)
  key_links:
    - "page.tsx TabId type → inclui 'planning'"
    - "page.tsx tabs array → inclui {id: 'planning', label: 'Planning'}"
    - "page.tsx renderização condicional → {activeTab === 'planning' && <PlanningTab projectId={id} sprints={sprints} />}"
    - "POST /composer/gerar chama GET /composer/rascunho internamente para buscar dados antes de invocar Gemini"
    - "PlanningTab usa getRascunho ao montar + patchRascunho ao avançar/retroceder steps"
---

<objective>
Expansão: endpoint POST /composer/gerar com chamada Gemini + wizard de 4 passos PlanningTab.tsx completo com tela de boas-vindas, os 4 steps, validação D-04, preview de markdown e confirmação. Aba "Planning" wired no page.tsx.

Purpose: Completar o fluxo ponta-a-ponta do composer — o gerente compõe o planning em 4 passos, vê o preview gerado pelo Gemini e confirma para oficializar. O rascunho persiste entre sessões via backend (Plan 01).

Output:
- docudata-backend/routers/composer.py — POST /composer/gerar adicionado
- docudata-frontend/app/lib/api.ts — 4 funções composer (getRascunho, patchRascunho, gerarPlanning, confirmarPlanning) + tipos TypeScript
- docudata-frontend/app/components/PlanningTab.tsx — wizard completo, zero className
- docudata-frontend/app/projects/[id]/page.tsx — TabId estendido, tab adicionada, renderização condicional
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/ROADMAP.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/10-composer-de-planning/10-CONTEXT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/10-composer-de-planning/10-RESEARCH.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/10-composer-de-planning/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: POST /composer/gerar — Gemini call retorna markdown sem persistir</name>
  <files>docudata-backend/routers/composer.py</files>

  <read_first>
    - docudata-backend/routers/composer.py — ler o arquivo completo (criado no Plan 01) para ver o estado atual antes de adicionar o novo endpoint
    - docudata-backend/routers/commit_ingest.py — linhas 129-146: padrão exato de ChatGoogleGenerativeAI(model="gemini-3.5-flash-lite", max_tokens=2048, google_api_key=api_key) + await llm.ainvoke([SystemMessage(...), HumanMessage(...)]) + try/except HTTPException 502
    - docudata-backend/models/schemas.py — linhas 248-262: FuncionalidadeResponse para verificar campos criterios_aceite e sprint_alvo
    - docudata-backend/routers/painel.py — linha 147 em diante: lógica calcular_bloco_c (referência para entender como as funcionalidades e transições são tratadas — não copiar, apenas entender o padrão)
  </read_first>

  <action>
    Adicionar ao composer.py (não reescrever o arquivo — Edit para adicionar após o POST /confirmar):

    1. Pydantic body model GerarBody com campos: project_id: str, sprint_numero: int.

    2. Endpoint POST /gerar (async): buscar o rascunho no Supabase (404 se não existe). Buscar o projeto para obter gemini_api_key (422 se chave ausente/vazia — mesmo padrão de commit_ingest.py). Buscar todas as funcionalidades do projeto (select * where project_id=?). Montar funcs_map: dict[str, dict] = {f["id"]: f for f in funcs_resp.data}. Construir o contexto de texto chamando a função auxiliar _montar_contexto_gerar(rascunho, funcs_map, projeto) conforme RESEARCH.md §Prompt do Gemini. IMPORTANTE: ao selecionar critérios por índice, filtrar out-of-bounds: indices = [i for i in indices if i < len(criterios)] — Pitfall 5 do RESEARCH.md. Incluir throughput_ref (se disponível no rascunho ou calcular na hora) no bloco de contexto.

    3. Chamar Gemini com _PLANNING_SYSTEM_PROMPT (conforme RESEARCH.md §Prompt do Gemini — prompt em português pedindo retorno em markdown puro sem texto extra antes ou depois) e o contexto como HumanMessage. Usar ChatGoogleGenerativeAI(model="gemini-3.5-flash-lite", max_tokens=2048, google_api_key=api_key) + await llm.ainvoke(...). Acessar result.content (não usar with_structured_output — queremos texto livre/markdown). try/except → HTTPException 502.

    4. Retornar {"markdown": result.content} — NÃO persistir em nenhuma tabela. Esta é a separação central entre /gerar (preview) e /confirmar (oficialização per D-06/D-07).

    Adicionar constante de módulo _PLANNING_SYSTEM_PROMPT com o prompt em português conforme RESEARCH.md §Prompt do Gemini: assistente especializado em documentação de projetos de dados do CITi, gera documento de Planning em markdown com seções (Objetivo da Sprint, Funcionalidades Selecionadas com critérios recortados, Responsabilidades, Transbordos da Sprint Anterior, Throughput de Referência), escreve em português de forma objetiva, retorna APENAS o markdown sem texto antes ou depois.
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python -c "from routers import composer; routes = [r.path for r in composer.router.routes]; print(routes); assert any('/gerar' in r for r in routes), 'rota /gerar ausente'"
    </automated>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python -c "import main; routes = [r.path for r in main.app.routes]; print([r for r in routes if 'composer' in r])"
    </automated>
  </verify>

  <acceptance_criteria>
    - composer.py define POST /gerar com body GerarBody(project_id, sprint_numero)
    - /gerar retorna HTTPException 404 se rascunho não existe
    - /gerar retorna HTTPException 422 se gemini_api_key do projeto está ausente/vazia
    - /gerar retorna HTTPException 502 se chamada Gemini falhar (try/except)
    - /gerar retorna {"markdown": str} sem chamar .insert() em nenhuma tabela
    - Filtragem de índices out-of-bounds presente: [i for i in indices if i < len(criterios)]
    - _PLANNING_SYSTEM_PROMPT está definido como constante de módulo em português
    - python -c "import main" passa sem erros após a adição
  </acceptance_criteria>

  <done>
    POST /composer/gerar importa sem erro, retorna {"markdown": "..."} de forma determinística dada entrada válida, e nunca persiste dados. Rota aparece em python -c "import main".
  </done>
</task>

<task type="auto">
  <name>Task 2: PlanningTab.tsx wizard completo + api.ts funções + page.tsx tab wiring</name>
  <files>
    docudata-frontend/app/lib/api.ts,
    docudata-frontend/app/components/PlanningTab.tsx,
    docudata-frontend/app/projects/[id]/page.tsx
  </files>

  <read_first>
    - docudata-frontend/app/components/PainelTab.tsx — ler completo: paleta de cores exata, padrão de constantes de estilo fora do componente, loading/error states, padrão de useState, useEffect com fetch inicial; PlanningTab.tsx deve seguir byte-a-byte o mesmo estilo
    - docudata-frontend/app/lib/api.ts — ler completo: padrão de funções fetch (const API, fetch com res.ok check, throw new Error(err.detail)), interfaces TypeScript existentes (FuncionalidadeResponse, SprintWithStatus)
    - docudata-frontend/app/projects/[id]/page.tsx — linhas 47 (TabId type), 40-46 (imports de componentes), 469-481 (Tabs array e renderização condicional) — verificar padrão exato de como PainelTab é importado e renderizado
    - docudata-frontend/package.json — confirmar "react-markdown" presente (sem instalação adicional)
    - docudata-backend/routers/composer.py — verificar os shapes de response dos 4 endpoints para construir as interfaces TypeScript corretamente
  </read_first>

  <action>
    ARQUIVO 1 — api.ts: adicionar ao final do arquivo (não reescrever):

    Interfaces TypeScript: DadosJson { funcionalidades_selecionadas: string[]; recortes: Record<string, number[]>; alocacoes: Record<string, string>; transbordos: string[]; }. RascunhoData { id: string; project_id: string; sprint_numero: number; step_atual: number; dados_json: DadosJson; created_at: string; updated_at: string; }. GetRascunhoResponse { rascunho: RascunhoData; throughput_ref: number | null; transbordos: TransbordoItem[]; }. TransbordoItem { id: string; titulo: string; sprint_alvo: string; status: string; criterios_aceite: string[]; }. GerarResponse { markdown: string; }. ConfirmarResponse { doc_id: string; content: string; created_at: string; }.

    Funções async: getRascunho(projectId: string, sprintNumero: number): Promise<GetRascunhoResponse> — GET ${API}/composer/rascunho/${projectId}/${sprintNumero}. patchRascunho(projectId: string, sprintNumero: number, payload: { step_atual: number; dados_json: DadosJson }): Promise<RascunhoData> — PATCH com JSON body. gerarPlanning(projectId: string, sprintNumero: number): Promise<GerarResponse> — POST ${API}/composer/gerar com JSON {project_id, sprint_numero}. confirmarPlanning(projectId: string, sprintNumero: number, markdown: string): Promise<ConfirmarResponse> — POST ${API}/composer/confirmar com JSON {project_id, sprint_numero, markdown}.

    ARQUIVO 2 — PlanningTab.tsx: criar componente novo seguindo RIGOROSAMENTE o padrão de PainelTab.tsx — zero className, todos os estilos como constantes React.CSSProperties declaradas fora do componente.

    Props: interface Props { projectId: string; sprints: SprintWithStatus[]; }. O componente recebe sprints para inferir a sprint mais recente (sprint atual = Math.max dos sprints.map(s => s.numero) + 1, ou 1 se sem sprints).

    Estados locais: loading: boolean, error: string|null, funcionalidades: FuncionalidadeResponse[], rascunhoData: GetRascunhoResponse|null, wizardStep: 1|2|3|4, selecionadas: string[], recortes: Record<string,number[]>, alocacoes: Record<string,string>, transbordos: string[], markdownGerado: string, gerandoMarkdown: boolean, confirmando: boolean, confirmado: boolean.

    useEffect inicial: chamar getRascunho(projectId, sprintAlvo) + listFuncionalidades(projectId). Se rascunhoData retornar rascunho com step_atual > 1, retomar o step e popular selecionadas/recortes/alocacoes/transbordos do dados_json.

    TELA DE BOAS-VINDAS (quando rascunhoData === null ou loading): exibir throughput_ref das últimas 3 sprints ("Throughput médio das últimas 3 sprints: X funcionalidades/sprint" ou "Sem dados de throughput" se null) — D-10. Botão "Iniciar Planning da Sprint N" chama getRascunho (que já faz upsert no backend) e transita para o wizard.

    BARRA DE PROGRESSO HORIZONTAL (steps 1-4): 4 círculos/itens horizontais com labels "1. Seleção", "2. Recorte", "3. Alocação", "4. Composição". Step ativo com cor #4ade80 (verde primário do projeto), steps anteriores em cinza claro, step futuro em #e8e8ed. Implementar com style={{display:"flex", gap:8}} sem className — D-09.

    STEP 1 — Seleção: listar todas as funcionalidades do projeto. Funcionalidades com id em transbordos do backend aparecem NO TOPO separadas com borderLeft: "3px solid #c2410c" e badge inline "Transbordado" (background:"#ffedd5", color:"#c2410c", borderRadius:6, padding:"2px 8px", fontSize:12, fontWeight:700). Cada funcionalidade tem checkbox para incluir/excluir da seleção. Exibir throughput_ref como informação ("Throughput de referência: X func/sprint — selecionar além desse número pode comprometer a sprint"). Ao avançar: patchRascunho com step_atual=1, dados_json atualizado com funcionalidades_selecionadas e transbordos (IDs com id in transbordos E selecionadas).

    STEP 2 — Recorte (por D-03/D-04): para cada funcionalidade selecionada, exibir título e lista de checkboxes dos criterios_aceite (um por índice). Usuário marca quais critérios entram nesta sprint. Se alguma funcionalidade selecionada não tiver nenhum índice marcado: exibir aviso inline por funcionalidade ("Selecione ao menos um critério de aceite") em vermelho (#dc2626). Botão "Próximo" recebe disabled quando passo2Valido === false (passo2Valido = selecionadas.every(id => (recortes[id]??[]).length > 0)) — D-04. Ao avançar: patchRascunho com step_atual=2 e dados_json.recortes atualizado.

    STEP 3 — Alocação: para cada funcionalidade selecionada exibir título e input de texto para nome do responsável (não obrigatório — campo opcional, alocacoes pode ter entradas vazias). Ao avançar: patchRascunho com step_atual=3 e dados_json.alocacoes atualizado.

    STEP 4 — Composição: ao entrar neste step (useEffect ou na transição), disparar gerarPlanning automaticamente (sem botão intermediário — D-06 implica preview já visível). Exibir spinner/loading enquanto gerandoMarkdown=true ("Gerando planning com IA…"). Se erro: exibir mensagem + botão "Tentar novamente". Quando markdownGerado disponível: renderizar via ReactMarkdown. Botão "Confirmar Planning" (cor #16a34a, fundo #dcfce7) — ao clicar: confirmarPlanning(projectId, sprintAlvo, markdownGerado) → se sucesso: exibir tela de confirmação ("Planning da Sprint N salvo com sucesso!") e limpar rascunhoData (gerente pode iniciar novo planning) — D-02. Botão "Anterior" retorna ao step 3 sem perder o rascunho.

    Paleta de cores: seguir exatamente RESEARCH.md §Pattern 6 — background #f7f7fa, cards #ffffff border #e8e8ed borderRadius 14, texto primário #111116, secundário #9696a0, verde #4ade80/#16a34a/#dcfce7, laranja #ffedd5/#c2410c para badge Transbordado.

    ARQUIVO 3 — page.tsx: 3 edições cirúrgicas via Edit:
    (a) Linha 47: estender TabId de "...| 'config'" para "...| 'config' | 'planning'"
    (b) Dentro do array de tabs (após {id:"config",label:"Configurações"}): adicionar {id:"planning",label:"Planning"}
    (c) Adicionar import de PlanningTab no bloco de imports de componentes (linha ~44)
    (d) Após o bloco de renderização da aba config: adicionar {activeTab === "planning" && (<PlanningTab projectId={id} sprints={sprints} />)}
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend" && npx tsc --noEmit 2>&1 | head -30
    </automated>
    <automated>
      grep -c "planning" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/projects/[id]/page.tsx"
    </automated>
    <automated>
      grep -v "^//" "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/components/PlanningTab.tsx" | grep -c "className"
    </automated>
  </verify>

  <acceptance_criteria>
    - api.ts contém funções getRascunho, patchRascunho, gerarPlanning, confirmarPlanning exportadas
    - api.ts contém interfaces DadosJson, RascunhoData, GetRascunhoResponse, TransbordoItem
    - PlanningTab.tsx existe e não contém "className" fora de comentários (grep -v "^//" | grep -c "className" retorna 0)
    - PlanningTab.tsx importa ReactMarkdown de "react-markdown" e usa para renderizar markdownGerado no step 4
    - PlanningTab.tsx: passo2Valido verifica selecionadas.every(id => (recortes[id]??[]).length > 0) e botão "Próximo" do step 2 tem disabled={!passo2Valido}
    - PlanningTab.tsx: step 4 chama gerarPlanning ao entrar no step (não ao clicar botão)
    - PlanningTab.tsx: botão "Confirmar Planning" chama confirmarPlanning com markdownGerado
    - page.tsx: TabId inclui "planning" como literal type
    - page.tsx: array de tabs inclui {id:"planning", label:"Planning"}
    - page.tsx: renderização condicional {activeTab === "planning" && <PlanningTab ...>} presente
    - npx tsc --noEmit retorna sem erros de tipo relevantes para os arquivos desta fase
    - PlanningTab.tsx: transbordos aparecem no topo com borderLeft laranja e badge "Transbordado"
    - PlanningTab.tsx: tela de boas-vindas exibe throughput_ref e botão "Iniciar Planning da Sprint N"
  </acceptance_criteria>

  <done>
    aba "Planning" aparece no Tabs do dashboard. Tela de boas-vindas exibe throughput. Wizard de 4 steps navega corretamente, salva rascunho a cada avanço, step 2 bloqueia sem critérios selecionados, step 4 gera preview via Gemini e confirmação salva em generated_docs. Zero className em PlanningTab.tsx. TypeScript compila sem erros.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /composer/gerar → Gemini API | Contexto do planning (títulos de funcionalidades, critérios, nomes de responsáveis) sai para Gemini API externa |
| Gemini response → react-markdown | Markdown gerado por LLM renderizado no browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-10-05 | Spoofing | react-markdown no preview do step 4 | low | mitigate | react-markdown ^9.x renderiza safe por default — não usa dangerouslySetInnerHTML; XSS não aplicável |
| T-10-06 | Information Disclosure | POST /gerar — gemini_api_key por projeto | medium | mitigate | api_key buscada no backend por project_id, nunca retornada no response; padrão idêntico a commit_ingest.py |
| T-10-07 | Denial of Service | POST /gerar chamado repetidamente no step 4 | low | mitigate | gerandoMarkdown state previne chamadas duplicadas enquanto em progresso; botão "Tentar novamente" só aparece em erro |
| T-10-08 | Tampering | markdown editado no cliente antes de POST /confirmar | low | accept | O gerente é o autor do planning — MVP sem isolamento de usuário; markdown é conteúdo de trabalho, não credencial |
</threat_model>

<verification>
Verificação end-to-end do wizard completo:

1. `npx tsc --noEmit` no frontend passa sem erros de tipo para os arquivos desta fase
2. `grep -v "^//" PlanningTab.tsx | grep -c "className"` retorna 0
3. Browser: navegar para /projects/{id} → aba "Planning" visível
4. Aba Planning: tela de boas-vindas exibe throughput_ref ou "Sem dados" + botão "Iniciar Planning"
5. Step 1: funcionalidades listadas, transbordos no topo com badge laranja
6. Step 2: botão "Próximo" desabilitado sem critérios selecionados, habilitado após selecionar ≥1 critério por funcionalidade
7. Step 4: ao entrar, spinner aparece, após resposta do Gemini markdown renderizado, botão "Confirmar Planning" disponível
8. Confirmação: mensagem de sucesso exibida, rascunho não persiste após confirmar
</verification>

<success_criteria>
- POST /composer/gerar retorna {markdown: str} sem persistir — D-05/D-06
- PlanningTab.tsx tem zero className (inline style apenas) — D-09
- Tela de boas-vindas com throughput das últimas 3 sprints e botão de iniciar — D-10
- 4 steps horizontais com conteúdo abaixo, navegação Anterior/Próximo — D-09
- Transbordos no topo do step 1 com badge laranja — CONTEXT.md §Specifics
- Recorte por índice obrigatório; "Próximo" bloqueado sem seleção — D-03/D-04
- Responsável opcional por funcionalidade no step 3
- Preview via react-markdown no step 4, gerado automaticamente ao entrar no step — D-06
- Confirmação via POST /confirmar salva em generated_docs e limpa rascunho — D-07/D-02
</success_criteria>

<output>
Criar `.planning/phases/10-composer-de-planning/10-02-SUMMARY.md` quando concluído.
</output>

## Artifacts This Phase Produces (Plan 02)

| Symbol | Type | File |
|--------|------|------|
| `POST /composer/gerar` | endpoint | docudata-backend/routers/composer.py |
| `GerarBody` | Pydantic model | docudata-backend/routers/composer.py |
| `_PLANNING_SYSTEM_PROMPT` | constant | docudata-backend/routers/composer.py |
| `_montar_contexto_gerar` | function | docudata-backend/routers/composer.py |
| `getRascunho` | async function | docudata-frontend/app/lib/api.ts |
| `patchRascunho` | async function | docudata-frontend/app/lib/api.ts |
| `gerarPlanning` | async function | docudata-frontend/app/lib/api.ts |
| `confirmarPlanning` | async function | docudata-frontend/app/lib/api.ts |
| `DadosJson` | TypeScript interface | docudata-frontend/app/lib/api.ts |
| `RascunhoData` | TypeScript interface | docudata-frontend/app/lib/api.ts |
| `GetRascunhoResponse` | TypeScript interface | docudata-frontend/app/lib/api.ts |
| `TransbordoItem` | TypeScript interface | docudata-frontend/app/lib/api.ts |
| `PlanningTab` | React component | docudata-frontend/app/components/PlanningTab.tsx |
| `TabId` (extended) | TypeScript union type | docudata-frontend/app/projects/[id]/page.tsx |
