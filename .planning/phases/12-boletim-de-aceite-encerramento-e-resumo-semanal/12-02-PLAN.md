---
phase: 12-boletim-de-aceite-encerramento-e-resumo-semanal
plan: 02
type: execute
wave: 2
depends_on:
  - "12-01"
files_modified:
  - docudata-backend/routers/boletins.py
autonomous: true
requirements:
  - "M6 (§5)"
  - "M8 (§5)"
  - "§4.5 (RevisaoDiaria)"

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "PATCH /boletins/{id} com status=enviado atualiza boletim e funcionalidades para status_cliente=enviado, com TransicaoStatus registrado para cada uma (D-05)"
    - "PATCH /boletins/{id} com status=ajuste sem retorno_tipo retorna 422 (D-06)"
    - "Transição rascunho→aprovado (pulando enviado) retorna 422 — sequência enforced (RESEARCH Pitfall 2)"
    - "POST /boletins/resumo_semanal retorna markdown determinístico sem chamar Gemini (D-08)"
    - "Resumo semanal cobre dom–sáb da semana atual usando cálculo correto com weekday()+1)%7 (D-09)"
    - "Resumo semanal salvo em generated_docs com doc_type=resumo_semanal e sprint_number=None (D-10)"
    - "Resumo semanal inclui achados críticos de revisoes_diarias via calcular_bloco_b (§4.5)"
    - "Quando não há anomalia, o resumo declara explicitamente que não há"
  artifacts:
    - "docudata-backend/routers/boletins.py (atualizado com PATCH + POST /resumo_semanal)"
  key_links:
    - "PATCH /boletins/{id} → TRANSICOES_VALIDAS dict → batch UPDATE funcionalidades.status_cliente → INSERT transicoes_status"
    - "POST /boletins/resumo_semanal → calcular_bloco_a + calcular_bloco_b (painel.py) → formatted markdown → generated_docs INSERT"
    - "calcular_bloco_b recebe revisoes_diarias → achados_criticos no resumo semanal (§4.5)"
---

<objective>
Expansion: adicionar PATCH /boletins/{id} (fluxo de status com validação de sequência, classificação obrigatória retorno_tipo, batch update de status_cliente com TransicaoStatus) e POST /boletins/resumo_semanal (listagem determinística sem Gemini, reutilizando calcular_bloco_a/calcular_bloco_b de painel.py, salva em generated_docs).

Purpose: Completar o backend da fase — o PATCH fecha o ciclo de vida do boletim; o resumo semanal entrega M8 sem custo de token.
Output: boletins.py com todos os 4 endpoints funcionais (POST, GET, PATCH, POST resumo_semanal).
</objective>

<execution_context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-02-SUMMARY.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-CONTEXT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-RESEARCH.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-PATTERNS.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: PATCH /boletins/{id} — status transitions + retorno_tipo validation + batch status_cliente update with TransicaoStatus</name>
  <files>
    docudata-backend/routers/boletins.py
  </files>
  <read_first>
    - docudata-backend/routers/boletins.py — ler inteiro: ver estado atual do arquivo após Plan 01 para saber onde adicionar o PATCH
    - docudata-backend/routers/funcionalidades.py — ler linhas 270-320: padrão TransicaoStatus (campos, cálculo de duracao_fase_anterior_segundos, insert pattern) para replicar em batch no boletins.py
    - .planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-PATTERNS.md — seção "TransicaoStatus batch pattern" e "Status transition validation"
    - .planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-RESEARCH.md — seções Common Pitfalls 2, 3, 4
  </read_first>
  <action>
Adicionar ao final de boletins.py (após GET /boletins/{project_id}):

**Constante de transições válidas** (definir antes do handler):

    TRANSICOES_VALIDAS: dict[str, set[str]] = {
        "rascunho": {"enviado"},
        "enviado": {"aprovado", "ajuste"},
    }

**Função auxiliar `_registrar_transicao_status_cliente`** — replicar o padrão de funcionalidades.py:270-307 para atualizar status_cliente em batch (per RESEARCH Pattern 2 e Pitfall 4):
- Recebe `client`, `func_id: str`, `status_anterior: str`, `novo_status: str`, `agora: datetime`
- Busca última transição de status_cliente para essa funcionalidade em `transicoes_status` (campo="status_cliente")
- Calcula `duracao_fase_anterior_segundos` = diferença em segundos entre agora e a data da última transição (ou created_at se não houver transição anterior)
- Insere em `transicoes_status`: `{funcionalidade_id: func_id, campo: "status_cliente", de: status_anterior, para: novo_status, autor: None, timestamp: agora.isoformat(), motivo: None, duracao_fase_anterior_segundos: duracao}`
- Atualiza `funcionalidades` SET `status_cliente = novo_status` WHERE `id = func_id`

**Endpoint PATCH /boletins/{id}** (per D-05, D-06):

- Recebe `id: str` (path) e `BoletimPatch` body
- Buscar boletim: `client.table("boletins_aceite").select("*").eq("id", id).execute()` — 404 se não encontrado
- Validar transição: se `body.status` não está em `TRANSICOES_VALIDAS.get(boletim["status"], set())`, retornar 422 com mensagem "Transição inválida: {status_atual} → {body.status}. Transições válidas: rascunho→enviado, enviado→aprovado, enviado→ajuste"
- Validar retorno_tipo: se `body.status == "ajuste"` e `body.retorno_tipo` não está em `{"bug", "mudanca_escopo"}`, retornar 422 com mensagem "retorno_tipo é obrigatório quando status = ajuste (valores aceitos: bug, mudanca_escopo)"
- Derivar novo `status_cliente` das funcionalidades (per D-05):
  - `enviado` → `status_cliente = "enviado"`
  - `aprovado` → `status_cliente = "aprovado"`
  - `ajuste` → `status_cliente = "ajuste_pedido"`
- Para cada `func_id` em `boletim["funcionalidade_ids"]`:
  - Buscar funcionalidade atual para obter `status_cliente` anterior
  - Chamar `_registrar_transicao_status_cliente(client, func_id, status_anterior, novo_status_cliente, agora)` (registra transicao E atualiza a funcionalidade)
- Montar campos de update do boletim:
  - Sempre: `{"status": body.status}`
  - Se `body.status == "enviado"`: adicionar `{"enviado_em": agora.isoformat()}`
  - Se `body.status in {"aprovado", "ajuste"}`: adicionar `{"retorno_em": agora.isoformat(), "retorno_tipo": body.retorno_tipo}`
- Executar `client.table("boletins_aceite").update(campos_update).eq("id", id).execute()`
- Retornar `BoletimResponse` com os dados atualizados
  </action>
  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && python3 -c "import ast; ast.parse(open('routers/boletins.py').read()); print('boletins.py syntax OK after PATCH')"</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && python3 -c "from routers import boletins; routes = [r.path for r in boletins.router.routes]; print('routes:', routes); assert any('{id}' in r for r in routes), 'PATCH /{id} missing'"</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "TRANSICOES_VALIDAS" routers/boletins.py</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "retorno_tipo.*obrigat" routers/boletins.py</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "ajuste_pedido" routers/boletins.py</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "_registrar_transicao_status_cliente" routers/boletins.py</automated>
  </verify>
  <done>
    - PATCH /boletins/{id} adicionado ao router com validação de sequência de status via TRANSICOES_VALIDAS
    - Transição inválida (ex: rascunho→aprovado) retorna 422 com mensagem descritiva
    - PATCH com status=ajuste e sem retorno_tipo retorna 422 com mensagem "retorno_tipo é obrigatório..."
    - _registrar_transicao_status_cliente registra transição em transicoes_status E atualiza funcionalidades.status_cliente para cada funcionalidade do lote
    - status=enviado define enviado_em; status=aprovado|ajuste define retorno_em + retorno_tipo
    - grep ajuste_pedido em boletins.py retorna count >= 1 (derivação de status_cliente)
  </done>
</task>

<task type="auto">
  <name>Task 2: POST /boletins/resumo_semanal — deterministic markdown + generated_docs save (M8, §4.5)</name>
  <files>
    docudata-backend/routers/boletins.py
  </files>
  <read_first>
    - docudata-backend/routers/boletins.py — ler inteiro (estado pós-Task 1) para ver onde adicionar o novo endpoint
    - docudata-backend/routers/painel.py — ler linhas 1-170: ver signatures completas de calcular_bloco_a e calcular_bloco_b (parâmetros, retorno esperado), e como os dados são buscados do Supabase (quais tabelas, quais campos) para replicar as mesmas queries
    - docudata-backend/routers/generate.py — ler linhas 1-30: ver o padrão de INSERT em generated_docs (campos obrigatórios: project_id, doc_type, sprint_number, content, input_tokens, output_tokens, cost_usd)
    - .planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-RESEARCH.md — seção Pattern 3 (resumo semanal) e Pitfall 5 (cálculo dom–sáb) e Pitfall 6 (não usar generate.py router)
  </read_first>
  <action>
Adicionar ao final de boletins.py (após PATCH /boletins/{id}):

**Imports adicionais necessários** no topo do arquivo (se não presentes):
- `from datetime import date, timedelta` (além de datetime, timezone já presentes)
- `from routers.painel import calcular_bloco_a, calcular_bloco_b` — importar as funções de cálculo diretamente

**Endpoint POST /boletins/resumo_semanal** — ATENÇÃO: esta rota deve ser registrada ANTES de PATCH /boletins/{id} no arquivo, ou pelo menos antes de qualquer rota com parâmetro de path que possa capturar "resumo_semanal" como {id}. Verificar se FastAPI interpretaria /boletins/resumo_semanal antes de /boletins/{id} — se houver conflito, registrar a rota estática antes da parametrizada:

- Recebe `ResumoSemanalRequest` body com `project_id: str`
- Buscar projeto: 404 se não existir

**Cálculo do período da semana (per D-09 e RESEARCH Pitfall 5):**

    hoje = date.today()
    dias_desde_domingo = (hoje.weekday() + 1) % 7
    inicio_semana = hoje - timedelta(days=dias_desde_domingo)
    fim_semana = inicio_semana + timedelta(days=6)

Comentar: "weekday() retorna 0=seg, 6=dom; (weekday+1)%7 calcula dias desde o domingo"

**Busca dos dados para anomalias (replicar o que painel.py faz para calcular_bloco_a e calcular_bloco_b):**
- Buscar todas as funcionalidades do projeto: `client.table("funcionalidades").select("*").eq("project_id", project_id).execute()`
- Buscar transições de status: `client.table("transicoes_status").select("*").in_("funcionalidade_id", [f["id"] for f in funcs]).execute()`
- Buscar revisão diária recente: `client.table("revisoes_diarias").select("*").eq("project_id", project_id).order("criado_em", desc=True).limit(1).execute()` — `revisao_recente = result.data[0] if result.data else None`
- Buscar execuções de aceite: `client.table("execucoes_aceite").select("*").in_("funcionalidade_id", [f["id"] for f in funcs]).execute()`

**Chamar as funções importadas de painel.py:**

    bloco_a = calcular_bloco_a(projeto, funcs)
    bloco_b = calcular_bloco_b(funcs, transicoes, revisao_recente, execucoes_aceite)

**Montar markdown estruturado (per D-08 — sem Gemini, determinístico):**

Estrutura do markdown:
- Linha 1: `# Resumo Semanal — {project_name} — {inicio_semana.strftime("%d/%m/%Y")} a {fim_semana.strftime("%d/%m/%Y")}`
- Linha 2: `_Gerado em: {datetime.now(timezone.utc).strftime("%d/%m/%Y %H:%M UTC")}_`
- Verificar se há QUALQUER anomalia. Se nenhuma lista estiver populada E não houver achados críticos E não houver funcionalidades com aceite falhando: adicionar seção `## Status\nNenhuma anomalia identificada nesta semana.` e PARAR
- Se houver anomalias:
  - `## Funcionalidades Travadas` — listar `bloco_b["travadas"]` (status=em_andamento > 7 dias): para cada uma, linha com `- **{titulo}** — em andamento há {dias} dias`
  - `## Aguardando Cliente` — listar `bloco_b["aguardando_cliente"]` (status_cliente=enviado > 5 dias úteis): para cada uma, linha com `- **{titulo}** — aguardando há {dias} dias úteis`
  - `## Concluídas com Suíte Falhando` — listar `bloco_b["funcionalidades_com_aceite_falhando"]`: para cada uma, linha com `- **{titulo}**`
  - `## Achados Críticos (Revisão de Código)` — se `bloco_b.get("achados_criticos")`, listar itens; se lista vazia ou None, omitir seção
  - `## Leitura Tempo × Escopo` — se `bloco_a` tiver dados (data_inicio e data_fim_contratada preenchidos), exibir: `% prazo consumido: {bloco_a.get("pct_prazo_consumido", "N/A")}%`, `% escopo concluído: {bloco_a.get("pct_escopo_concluido", "N/A")}%`, `% escopo aprovado: {bloco_a.get("pct_escopo_aprovado", "N/A")}%`; se sem dados, escrever `Datas de contrato não configuradas — sem leitura de tempo × escopo`

**Salvar em generated_docs (per D-10):**

    client.table("generated_docs").insert({
        "project_id": project_id,
        "doc_type": "resumo_semanal",
        "sprint_number": None,
        "content": markdown_content,
        "input_tokens": 0,
        "output_tokens": 0,
        "cost_usd": 0.0,
    }).execute()

Retornar `{"content": markdown_content}` (mesmo shape de GenerateResponse ou similar).

**IMPORTANTE (RESEARCH Pitfall 6):** NÃO usar o router `/generate` nem o `generation_graph` para o resumo semanal. O endpoint é dedicado em `/boletins/resumo_semanal` e não chama Gemini em nenhuma hipótese.
  </action>
  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && python3 -c "import ast; ast.parse(open('routers/boletins.py').read()); print('boletins.py syntax OK after resumo_semanal')"</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && python3 -c "from routers import boletins; routes = [r.path for r in boletins.router.routes]; print('routes:', routes)"</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "resumo_semanal" routers/boletins.py</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "calcular_bloco" routers/boletins.py</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && python3 -v -c "from routers.painel import calcular_bloco_a, calcular_bloco_b" 2>&1 | tail -3</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "Gemini\|ainvoke\|ChatGoogleGenerativeAI" routers/boletins.py | python3 -c "import sys; count_before=int(sys.stdin.read()); from routers.boletins import _BOLETIM_SYSTEM_PROMPT; print('Gemini present in module (expected for POST /boletins):', count_before)"</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "weekday.*%.*7\|dias_desde_domingo" routers/boletins.py</automated>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança\ Dados/Agentes/Documentador/docudata-backend && grep -c "Nenhuma anomalia" routers/boletins.py</automated>
  </verify>
  <done>
    - POST /boletins/resumo_semanal adicionado ao router boletins.py
    - Importa calcular_bloco_a e calcular_bloco_b de routers.painel
    - Cálculo de período dom–sáb usa (hoje.weekday() + 1) % 7 — conforme Pitfall 5
    - Markdown estruturado gerado sem chamar Gemini ou ainvoke (grep não encontra chamada Gemini dentro da função resumo_semanal)
    - Quando não há anomalia, texto "Nenhuma anomalia identificada nesta semana." presente
    - Insere em generated_docs com doc_type="resumo_semanal" e sprint_number=None
    - Sintaxe válida (ast.parse pass)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→PATCH /boletins/{id} | Status payload pode tentar transição inválida ou omitir retorno_tipo obrigatório |
| boletins.py→painel.py | Importação circular possível se painel.py importar boletins.py (verificar) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-05 | Tampering | PATCH /boletins/{id} — status sequence | high | mitigate | TRANSICOES_VALIDAS dict + 422 em transição inválida; verificado antes de qualquer update |
| T-12-06 | Tampering | PATCH /boletins/{id} — retorno_tipo nulo | medium | mitigate | 422 obrigatório se status=ajuste e retorno_tipo ausente/inválido |
| T-12-07 | Information Disclosure | POST /boletins/resumo_semanal | low | accept | MVP sem autenticação — espaço compartilhado; resumo contém dados de trabalho, não credenciais |
| T-12-08 | Elevation of Privilege | PATCH funcionalidades batch sem verificar project ownership | medium | mitigate | Boletim só pode ser PATCH-ado se pertencer ao project_id — funcionalidades já foram verificadas no POST /boletins; ids armazenados são confiáveis |
</threat_model>

<verification>
- python3 ast.parse em boletins.py após todas as adições: PASS
- routes do router incluem "{id}" para PATCH e "resumo_semanal" para POST
- grep TRANSICOES_VALIDAS: count >= 1
- grep retorno_tipo.*obrigat: count >= 1
- grep ajuste_pedido: count >= 1
- grep calcular_bloco: count >= 1 (importação de painel.py)
- grep weekday.*%.*7|dias_desde_domingo: count >= 1
- grep Nenhuma anomalia: count >= 1 (caso sem anomalias)
- grep resumo_semanal: count >= 2 (endpoint path + doc_type)
</verification>

<success_criteria>
- [ ] PATCH /boletins/{id} com sequência inválida retorna 422 com mensagem descritiva
- [ ] PATCH /boletins/{id} com status=ajuste e retorno_tipo=None retorna 422
- [ ] PATCH status=enviado dispara _registrar_transicao_status_cliente para cada funcionalidade do lote
- [ ] POST /boletins/resumo_semanal não faz nenhuma chamada Gemini (função determinística)
- [ ] Resumo semanal cobre período dom–sáb calculado corretamente via (weekday()+1)%7
- [ ] Resumo semanal inclui achados de revisoes_diarias via calcular_bloco_b (§4.5 RevisaoDiaria)
- [ ] Resumo semanal salvo em generated_docs com doc_type=resumo_semanal, sprint_number=None
- [ ] Caso sem anomalia: markdown declara "Nenhuma anomalia identificada nesta semana."
</success_criteria>

<output>
Criar `/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/12-boletim-de-aceite-encerramento-e-resumo-semanal/12-02-SUMMARY.md` ao concluir.
</output>

## Artifacts this phase produces

### Modified Files
- `docudata-backend/routers/boletins.py` — adiciona:
  - `TRANSICOES_VALIDAS: dict[str, set[str]]` — constante de módulo
  - `_registrar_transicao_status_cliente(client, func_id, status_anterior, novo_status, agora)` — função auxiliar
  - `PATCH /boletins/{id}` — endpoint com validação de sequência + batch status_cliente update
  - `POST /boletins/resumo_semanal` — endpoint determinístico sem Gemini
