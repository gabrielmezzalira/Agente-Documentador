---
phase: 06-token-usage-panel
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - docudata-backend/routers/projects.py
  - docudata-backend/models/schemas.py
  - docudata-backend/tests/test_project_usage.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - USAGE-01
  - USAGE-03

estimate:
  tokens: 65000
  raw_tokens: 32500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - GET /projects/{id}/usage?month=YYYY-MM retorna, alem dos totais, um breakdown com custo/tokens agrupados por tipo (ingestao commit, ingestao planning/daily/ata/review/retro/upload, geracao repasse_semanal/retrospectiva/log_decisoes/documentacao_final/onboarding/ata_reuniao/planning/daily/review, doc manual)
    - A resposta traz uma lista cronologica items (default ate 100) das acoes do mes, cada uma com id origem (ingestions.id ou generated_docs.id), source ("ingestion" ou "generated_doc"), label (tipo_documentacao ou doc_type), created_at, input_tokens, output_tokens, cost_usd (per USAGE-01)
    - Items com cost_usd == 0 (docs manuais e uploads sem tokens) aparecem na lista com custo zerado, nao sao omitidos (per SC 4)
    - No dashboard, a aba "Custos" mostra: (a) total_usd do mes; (b) tabela/cards de breakdown por tipo; (c) lista cronologica das acoes; (d) seletor de mes que troca o conteudo sem recarregar a pagina (per SC 3 e SC 5)
  artifacts:
    - "Campos breakdown (dict[str, {input_tokens, output_tokens, cost_usd, count}]) e items (list) adicionados a ProjectUsageResponse em docudata-backend/models/schemas.py"
    - "Handler get_project_usage em docudata-backend/routers/projects.py estendido para calcular breakdown e items sem tocar em cost_usd/input_tokens/output_tokens salvos"
    - "Componente inline (ou componente novo) na aba 'Custos' de docudata-frontend/app/projects/[id]/page.tsx: seletor de mes + summary + breakdown + tabela cronologica"
    - "Funcao getProjectUsage em docudata-frontend/app/lib/api.ts atualizada para retornar breakdown + items"
  key_links:
    - Breakdown backend agrupa por: ingestions.tipo_documentacao (fallback "upload" se null) e generated_docs.doc_type — chaves distintas com prefixo para nao colidirem (ex "ingestion:commit", "generation:repasse_semanal", "generation:manual")
    - Doc manual e identificado no generated_docs como linha com cost_usd=0 AND input_tokens=0 AND output_tokens=0 (nao ha coluna is_manual) — categorizada em "generation:manual"
    - Seletor de mes no frontend chama getProjectUsage(id, novoMes) e atualiza state; scroll da lista permanece independente
---

<objective>
Expandir o slice provado no Plan 01 para o painel completo pedido pelo goal da Fase 6: breakdown por tipo de acao, lista cronologica das acoes com tokens/custo individuais e navegacao entre meses. Continua lendo somente os valores ja salvos nas colunas cost_usd, input_tokens e output_tokens.

Purpose: fecha o valor de usuario do MVP — o gerente consegue responder "quanto gastei este mes, com o que gastei e quais foram as ultimas acoes" numa unica tela. Preserva a arquitetura do Plan 01 (contrato JSON + aba Tabs) sem introduzir tabelas nem colunas novas.
Output: endpoint enriquecido, tipagem TS atualizada, painel completo renderizado na aba "Custos".
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@docudata-backend/routers/projects.py
@docudata-backend/models/schemas.py
@docudata-backend/graphs/generation_graph.py
@docudata-frontend/app/lib/api.ts
@docudata-frontend/app/projects/[id]/page.tsx
@.planning/phases/06-token-usage-panel/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — breakdown por tipo + lista cronologica no /projects/{id}/usage</name>
  <files>docudata-backend/routers/projects.py, docudata-backend/models/schemas.py, docudata-backend/tests/test_project_usage.py</files>
  <read_first>
    - docudata-backend/routers/projects.py (handler get_project_usage criado no Plan 01 — reaproveitar validacao de month e computo de intervalo)
    - docudata-backend/models/schemas.py (ProjectUsageResponse criado no Plan 01 — sera estendido)
    - docudata-backend/routers/generate.py (constante _VALID_DOC_TYPES e conjuntos _SPRINT_REQUIRED, _INGESTION_REQUIRED — referencia dos rotulos validos de doc_type; ver create_manual_doc / create_manual_doc_from_pdf para confirmar que docs manuais ficam com cost_usd=0)
    - docudata-backend/routers/commit_ingest.py (tipo_documentacao="commit" nas ingestions)
    - docudata-backend/routers/sprint_docs.py (rapidamente: identificar quais valores de tipo_documentacao sao emitidos — planning, daily, ata, review, retrospectiva)
    - .planning/phases/06-token-usage-panel/06-01-SUMMARY.md
  </read_first>
  <behavior>
    - GET /projects/{id}/usage?month=YYYY-MM continua retornando month, total_usd, input_tokens, output_tokens (compat com Plan 01) e passa a retornar ADICIONALMENTE: breakdown (dict de chave string -> {input_tokens, output_tokens, cost_usd, count}) e items (lista ordenada por created_at desc, ate 100 registros)
    - Chaves do breakdown: "ingestion:<tipo_documentacao>" para cada linha de ingestions (usando "upload" quando tipo_documentacao IS NULL); "generation:<doc_type>" para cada linha de generated_docs onde algum token > 0; "generation:manual" para linhas de generated_docs com input_tokens=0 AND output_tokens=0 AND cost_usd=0
    - Cada item da lista: {source: "ingestion"|"generated_doc", id: str, label: str (mesma string usada como chave no breakdown), created_at: ISO string, input_tokens: int, output_tokens: int, cost_usd: float}
    - Somatorios de breakdown fecham EXATAMENTE com os totais (assertivel: soma dos cost_usd em breakdown == total_usd; idem input_tokens e output_tokens)
    - Ordenacao dos items por created_at desc — item mais recente primeiro. Limit 100 (linhas mais antigas do mes sao truncadas sem erro; adicionar campo truncated: bool = True se cortou)
    - month invalido continua retornando 422 (comportamento herdado do Plan 01)
    - Projeto sem ingestion/generation no mes: total_usd=0, breakdown={}, items=[], truncated=False
  </behavior>
  <action>
    1) Em docudata-backend/models/schemas.py, estender ProjectUsageResponse: adicionar (a) classe UsageBucket(BaseModel) com {input_tokens: int, output_tokens: int, cost_usd: float, count: int}; (b) classe UsageItem(BaseModel) com {source: str, id: str, label: str, created_at: datetime, input_tokens: int, output_tokens: int, cost_usd: float}; (c) em ProjectUsageResponse adicionar breakdown: dict[str, UsageBucket] = {}, items: list[UsageItem] = [], truncated: bool = False.
    2) Em docudata-backend/routers/projects.py, dentro de get_project_usage, alterar os dois selects para incluir colunas necessarias: ingestions => "id, created_at, tipo_documentacao, input_tokens, output_tokens, cost_usd"; generated_docs => "id, created_at, doc_type, input_tokens, output_tokens, cost_usd". Manter ordenacao/nao-ordenacao vinda do Plan 01 apenas para totais.
    3) Construir dois helpers locais (ou funcoes privadas no proprio arquivo, para nao inflar services/): _label_ingestion(row) -> f"ingestion:{row.get('tipo_documentacao') or 'upload'}"; _label_generation(row) -> "generation:manual" if (row.get('input_tokens') or 0)==0 and (row.get('output_tokens') or 0)==0 and float(row.get('cost_usd') or 0)==0.0 else f"generation:{row.get('doc_type')}". Percorrer as linhas UMA vez agregando dentro de um dict[str, UsageBucket] incrementando input_tokens, output_tokens, cost_usd, count. Nao recalcular custos — usar os valores brutos.
    4) Construir items: para cada linha das duas tabelas, montar UsageItem com source correspondente ("ingestion" ou "generated_doc"), id = row["id"], label = mesma string do bucket, created_at = row["created_at"], tokens/cost = row lido direto. Concatenar as duas listas, ordenar por created_at desc, aplicar slice [:100]. truncated = True se combined_len > 100 else False.
    5) Somatorios totais permanecem sendo calculados como no Plan 01 (para nao mudar contrato dos 3 campos raiz). Assercao interna opcional (dev-only): sum(bucket.cost_usd for bucket in breakdown.values()) == total_usd — nao usar assert em prod; usar em teste.
    6) Estender docudata-backend/tests/test_project_usage.py com 3 novos casos: (i) breakdown_by_type: supabase mock retorna 1 ingestion commit (0.001) + 1 ingestion planning (0.002) + 1 generated_doc repasse_semanal (0.005) + 1 generated_doc manual (0) => breakdown tem 4 chaves corretas com count=1 cada e somatorio bate com total_usd 0.008; (ii) items_ordered_and_zero_shown: mesma fixture, items retorna len==4, ordem eh created_at desc, o item manual com cost_usd=0 esta presente (nao filtrado); (iii) truncation: mock retorna 105 linhas => len(items)==100 e truncated is True.
  </action>
  <acceptance_criteria>
    - Response do endpoint contem chaves: month, total_usd, input_tokens, output_tokens, breakdown, items, truncated
    - Somatorio de breakdown[*].cost_usd == total_usd (assertivel em teste ii)
    - Somatorio de breakdown[*].input_tokens == input_tokens e idem para output_tokens
    - Linhas com cost_usd=0 aparecem em items (nao sao omitidas)
    - `grep -n "_COST_PER" docudata-backend/routers/projects.py` continua nao aparecendo dentro de get_project_usage (nada de recalculo)
    - pytest passa nos 3 casos novos + 3 casos herdados do Plan 01 (total 6)
    - Rastreamento: USAGE-01 estendida (breakdown + items) e USAGE-03 satisfeita no lado backend com os dados necessarios para o painel
  </acceptance_criteria>
  <verify>
    <automated>cd docudata-backend && pytest tests/test_project_usage.py -x -q</automated>
  </verify>
  <done>
    Endpoint retorna breakdown e items alinhados aos totais, doc manual/upload sem token aparecem na lista, truncation acima de 100 registros funciona, e todos os testes passam sem introduzir recalculo de tarifa.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend — painel completo na aba Custos (breakdown + tabela + seletor de mes)</name>
  <files>docudata-frontend/app/lib/api.ts, docudata-frontend/app/projects/[id]/page.tsx</files>
  <read_first>
    - docudata-frontend/app/lib/api.ts (interface ProjectUsage criada no Plan 01 — sera estendida; padrao ProjectCost tambem serve de referencia)
    - docudata-frontend/app/projects/[id]/page.tsx (bloco activeTab === "custos" adicionado no Plan 01; TabId; sectionStyle/sectionTitle; formato de exibicao de valores em USD ja usado na section "Custo de IA")
    - .planning/phases/06-token-usage-panel/06-01-SUMMARY.md (confirmar como o mes atual esta sendo computado no Plan 01)
  </read_first>
  <behavior>
    - Ao entrar na aba "Custos", o painel carrega automaticamente o mes atual (compat com Plan 01)
    - Seletor de mes: dois botoes de navegacao ("<" mes anterior e ">" mes seguinte) + label do mes atual (formato "mai/2026" em pt-BR); alterar o mes dispara nova chamada getProjectUsage(id, novoMes) SEM recarregar a pagina (usa state)
    - Botao ">" fica desabilitado quando o proximo mes seria futuro em relacao a hoje (evita navegar para meses sem dados possiveis)
    - Painel exibe 3 blocos: (a) SUMMARY — total_usd em destaque + "X in / Y out tokens"; (b) BREAKDOWN — cards ou linhas mostrando cada label do breakdown com count, tokens e cost_usd, ordenados por cost_usd desc; (c) HISTORICO — tabela com colunas Data (created_at formatado), Tipo (label — pode ser humanizado), Tokens (in/out) e Custo (USD); mostra ate 100 linhas. Se truncated=true, exibir aviso "Mostrando as 100 acoes mais recentes deste mes."
    - Linhas com cost_usd=0 aparecem na tabela com "$0.0000" — nao sao ocultadas (per SC 4)
    - Loading state entre trocas de mes (spinner ou "Carregando..." dentro do painel)
  </behavior>
  <action>
    1) Em docudata-frontend/app/lib/api.ts: estender ProjectUsage adicionando breakdown: Record<string, { input_tokens: number; output_tokens: number; cost_usd: number; count: number }>, items: Array<{ source: "ingestion" | "generated_doc"; id: string; label: string; created_at: string; input_tokens: number; output_tokens: number; cost_usd: number }>, truncated: boolean. Nenhuma outra funcao muda (getProjectUsage ja aceita month).
    2) Em docudata-frontend/app/projects/[id]/page.tsx: (a) adicionar helpers puros no topo do modulo (fora do componente): shiftMonth(YYYYMM: string, delta: number): string — soma delta meses tratando virada de ano; humanizeLabel(label: string): string — mapeia strings como "ingestion:commit"->"Ingestao: commit", "generation:repasse_semanal"->"Geracao: Repasse Semanal", "generation:manual"->"Doc manual", etc, com fallback para o proprio label capitalizado; formatMonthLabel(YYYYMM: string): string — usa Intl.DateTimeFormat("pt-BR", {timeZone:"America/Sao_Paulo", month:"short", year:"numeric"}); (b) adicionar state useState<boolean>(false) para usageLoading; (c) alterar o useEffect que reage a currentMonth para chamar setUsageLoading(true); getProjectUsage(id, currentMonth).then(setUsage).finally(() => setUsageLoading(false)); (d) reescrever o bloco de renderizacao da aba Custos com os 3 subblocos SUMMARY/BREAKDOWN/HISTORICO usando sectionStyle. Botoes de navegacao usam shiftMonth(currentMonth, -1) e +1; ">" fica disabled quando shiftMonth(currentMonth, 1) > mesAtualYYYYMM.
    3) SUMMARY: um bloco similar ao "Custo de IA" da aba config, mas mostrando total_usd e tokens do mes selecionado. Format: `$0.0000` (ou `$0.000000` para valores < 0.001) — mesmo helper usado ja em cost.total_usd. Reuse mental — se ficar muito repetitivo, aceitar duplicacao no MVP (nao criar componente reutilizavel novo).
    4) BREAKDOWN: `Object.entries(usage.breakdown).sort(([,a],[,b]) => b.cost_usd - a.cost_usd)` renderiza cards com humanizeLabel(k), count acoes, tokens (in/out) e custo USD.
    5) HISTORICO: `<table>` simples estilizada inline (a base ja usa React.CSSProperties inline). Colunas Data (new Date(item.created_at).toLocaleString("pt-BR", { timeZone:"America/Sao_Paulo" })), Tipo (humanizeLabel), Tokens (in/out), Custo. Se truncated exibe rodape azul com o aviso.
    6) Loading: se usageLoading => "Carregando dados do mes..."; se !usage e nao esta loading (edge de erro) => "Nao foi possivel carregar os dados". Erros do fetch entram em um state useState<string>("") de usageError, exibido dentro do painel.
    7) `npx tsc --noEmit` no docudata-frontend precisa passar.
  </action>
  <acceptance_criteria>
    - `cd docudata-frontend && npx tsc --noEmit` retorna 0 erros
    - Rodando `npm run dev` (frontend) + `uvicorn main:app` (backend), abrir /projects/{id}, clicar em "Custos": SUMMARY, BREAKDOWN e HISTORICO estao visiveis
    - Clicar em "<" na aba Custos troca o mes exibido para mes anterior sem reload; a URL do browser nao muda; getProjectUsage e chamado com o novo mes (verificavel no devtools network)
    - Botao ">" fica desabilitado quando o proximo mes seria mais recente que o mes atual real
    - Uma linha de doc manual (cost_usd=0) do mes selecionado aparece na tabela HISTORICO com "$0.0000"
    - Rastreamento: USAGE-03 completa (dashboard exibindo total do mes, breakdown por acao, lista cronologica); USAGE-01 mantida (endpoint continua o data source unico)
  </acceptance_criteria>
  <verify>
    <automated>cd docudata-frontend && npx tsc --noEmit</automated>
    <human-check>Rodar backend + frontend, abrir /projects/{id-com-dados} e verificar: (a) aba Custos mostra os 3 blocos; (b) botoes < e > trocam o mes sem reload; (c) botao > desabilita quando alvo eh mes futuro; (d) uma acao manual com cost_usd=0 aparece na tabela HISTORICO com "$0.0000".</human-check>
  </verify>
  <done>
    A aba "Custos" mostra os 3 blocos, seletor de mes navega para meses anteriores sem reload, futuros ficam bloqueados, docs manuais com custo zero aparecem na lista, e o build TS passa.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> FastAPI | month query param continua sendo entrada nao confiavel (mesma superficie do Plan 01) |
| FastAPI -> Supabase | selects passam a incluir mais colunas, mas continuam filtrados por project_id + intervalo mensal |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-04 | Denial of Service | list items sem limite | medium | mitigate | Slice [:100] no backend + campo truncated para UI comunicar; frontend nao pagina alem disso no MVP |
| T-06-05 | Information Disclosure | ids de ingestions/generated_docs vazando | low | accept | Ja expostos por outros endpoints (list_docs, listIngestions); consistente com decisao "sem auth v1" |
| T-06-06 | Tampering | frontend shiftMonth para meses futuros | low | mitigate | Botao ">" desabilitado na UI + backend continua aceitando qualquer month valido (nao ha dados retornados => resultado vazio inofensivo) |
</threat_model>

<verification>
1. Testes backend: `cd docudata-backend && pytest tests/test_project_usage.py -x`
2. Contrato: `curl -s "http://localhost:8000/projects/<id>/usage?month=2026-08" | jq '{keys: keys, buckets: (.breakdown | keys), items_count: (.items | length), truncated}'`
3. Somatorio: `curl -s ... | jq '(.breakdown | to_entries | map(.value.cost_usd) | add) == .total_usd'` -> `true`
4. Frontend build: `cd docudata-frontend && npx tsc --noEmit`
5. UI smoke: rodar backend+frontend local, abrir aba Custos, navegar entre meses, confirmar doc manual com $0 aparece
</verification>

<success_criteria>
- Endpoint retorna breakdown + items + truncated alem dos totais herdados do Plan 01
- Somatorio do breakdown bate com os totais (assertivel em teste)
- Painel "Custos" no dashboard exibe SUMMARY, BREAKDOWN e HISTORICO
- Seletor de mes troca conteudo sem reload; botao ">" bloqueia meses futuros
- Linhas com cost_usd=0 aparecem na tabela HISTORICO
- 6 testes pytest passam (3 do Plan 01 + 3 novos)
- Build TS do frontend passa
</success_criteria>

<output>
Criar `.planning/phases/06-token-usage-panel/06-02-SUMMARY.md` quando terminar, listando: mudancas na resposta do endpoint (com exemplo de payload), componente da aba Custos (estrutura visual), e USAGE-01/USAGE-02/USAGE-03 marcadas como completas.
</output>
