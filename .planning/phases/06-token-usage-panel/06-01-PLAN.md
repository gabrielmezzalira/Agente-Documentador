---
phase: 06-token-usage-panel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-backend/routers/projects.py
  - docudata-backend/models/schemas.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - USAGE-01
  - USAGE-02
  - USAGE-03

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - Ao chamar GET /projects/{id}/usage?month=YYYY-MM o backend retorna soma de cost_usd, input_tokens e output_tokens vindos exclusivamente de ingestions + generated_docs do projeto no mês pedido
    - Ao abrir o dashboard do projeto, o gerente vê o total_usd e tokens do MES ATUAL exibidos numa aba nova de custos, alimentados por esse endpoint
    - Se month for omitido, o backend usa o mes atual em America/Sao_Paulo (o mesmo mes que o frontend vai mostrar por default)
  artifacts:
    - "GET /projects/{project_id}/usage endpoint em docudata-backend/routers/projects.py aceitando query param month=YYYY-MM opcional"
    - "Modelo ProjectUsageResponse em docudata-backend/models/schemas.py com campos month, total_usd, input_tokens, output_tokens"
    - "Funcao getProjectUsage em docudata-frontend/app/lib/api.ts que chama /usage?month=YYYY-MM e retorna ProjectUsage"
    - "Nova aba 'Custos' registrada no Tabs do dashboard (docudata-frontend/app/projects[id]/page.tsx) exibindo total do mes atual"
  key_links:
    - Consulta Supabase filtra por project_id + created_at >= inicio_mes AND created_at < inicio_prox_mes tanto em ingestions quanto em generated_docs
    - Frontend calcula mes atual (America/Sao_Paulo) como YYYY-MM e passa como query param
    - Valores exibidos vem 1:1 do payload do endpoint (sem recalculo no cliente)
---

<objective>
Provar o pipeline BD -> API -> UI de uso mensal de tokens fim-a-fim, entregando o slice minimo: novo endpoint GET /projects/{id}/usage?month=YYYY-MM que agrega tokens/custo direto das colunas ja salvas em ingestions e generated_docs, e uma nova aba "Custos" no dashboard que carrega o mes atual e mostra total_usd + tokens.

Purpose (por que esse tracer): o risco arquitetural desta fase esta na fronteira BD-API-UI (filtro de mes por timezone, agregacao correta, tipagem no frontend). Uma vez que uma unica leitura do mes atual atravessa todas as camadas com verify real, o Plan 02 so precisa expandir o payload (breakdown + lista) e o componente da aba (seletor de mes + tabela), sem tocar contrato.
Output: endpoint funcional, contrato tipado, aba "Custos" renderizando o total do mes atual usando exclusivamente valores ja gravados nas colunas cost_usd/input_tokens/output_tokens.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docudata-backend/routers/projects.py
@docudata-backend/models/schemas.py
@docudata-backend/routers/commit_ingest.py
@docudata-backend/graphs/extraction_graph.py
@docudata-backend/graphs/generation_graph.py
@docudata-frontend/app/lib/api.ts
@docudata-frontend/app/projects/[id]/page.tsx
</context>

**User story (MVP):**
- **As a** gerente de projeto de dados do CITi
- **I want to** ver, ja na abertura do dashboard, quanto o meu projeto gastou em Gemini neste mes
- **so that** eu consiga responder rapidamente "quanto esse projeto esta custando agora?" sem abrir tabela nenhuma nem ligar planilha por fora

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "custo do mes atual" — GET /projects/{id}/usage + aba Custos wireada</name>
  <files>docudata-backend/routers/projects.py, docudata-backend/models/schemas.py, docudata-backend/tests/test_project_usage.py, docudata-frontend/app/lib/api.ts, docudata-frontend/app/projects/[id]/page.tsx</files>
  <read_first>
    - docudata-backend/routers/projects.py (padrao de handler async, uso de get_client, endpoint /cost ja existente como analogo mais proximo)
    - docudata-backend/models/schemas.py (padrao Pydantic — ver ProjectCostResponse como modelo mais proximo do que criar)
    - docudata-backend/graphs/extraction_graph.py (confirmar que ingestions.created_at e ingestions.cost_usd/input_tokens/output_tokens existem e ja sao gravados)
    - docudata-backend/graphs/generation_graph.py (mesma confirmacao para generated_docs)
    - docudata-backend/routers/commit_ingest.py (ingestion type=commit tambem grava cost_usd/tokens)
    - docudata-frontend/app/lib/api.ts (padrao das outras funcoes fetch, forma de ProjectCost como analogo mais proximo)
    - docudata-frontend/app/projects/[id]/page.tsx (como TabId, tabs e as sections do dashboard sao renderizadas — ver activeTab === "config" como referencia estrutural)
  </read_first>
  <behavior>
    - GET /projects/{id}/usage sem query param retorna month = mes atual em America/Sao_Paulo no formato YYYY-MM
    - GET /projects/{id}/usage?month=YYYY-MM retorna {month, total_usd, input_tokens, output_tokens} agregando SOMENTE linhas de ingestions e generated_docs do project_id no intervalo [inicio do mes, inicio do mes seguinte)
    - total_usd = soma(ingestions.cost_usd) + soma(generated_docs.cost_usd) do periodo, arredondado em 6 casas
    - input_tokens/output_tokens sao inteiros, soma direta das duas tabelas no periodo
    - Se project_id nao existir, retorna 404 (mesmo padrao de /projects/{id}/cost)
    - Se month tiver formato invalido (ex "2026-13", "abc"), retorna 422 com detail explicando o formato esperado YYYY-MM
    - Projeto sem nenhuma ingestion/generation no mes: retorna 200 com total_usd=0, input_tokens=0, output_tokens=0 (nao 404)
  </behavior>
  <action>
    Backend (implementa USAGE-01 e USAGE-02):
    1) Em docudata-backend/models/schemas.py adicionar Pydantic model ProjectUsageResponse com campos: project_id (str), month (str no formato YYYY-MM), total_usd (float), input_tokens (int), output_tokens (int). Colocar logo apos ProjectCostResponse para agrupar familia de modelos de custo.
    2) Em docudata-backend/routers/projects.py adicionar handler async GET /{project_id}/usage aceitando query param opcional month: Optional[str] = None. Importar ProjectUsageResponse e usar como response_model. Importar datetime, timezone e zoneinfo.ZoneInfo (Python 3.9+).
    3) Logica do handler (nao recalcular custos — apenas agregar valores ja salvos, per USAGE-02): (a) validar existencia do projeto via client.table("projects").select("id").eq("id", project_id) — 404 se ausente; (b) se month=None, computar YYYY-MM atual em ZoneInfo("America/Sao_Paulo"); (c) validar formato do month recebido (regex ^\d{4}-(0[1-9]|1[0-2])$) — 422 com mensagem "Formato invalido para 'month'. Use YYYY-MM." se falhar; (d) computar start_iso e end_iso: start = primeiro instante do mes em America/Sao_Paulo convertido para UTC ISO 8601, end = primeiro instante do mes seguinte no mesmo formato; (e) rodar dois selects filtrando por project_id + created_at >= start_iso + created_at < end_iso, um em ingestions selecionando "input_tokens, output_tokens, cost_usd", outro identico em generated_docs; (f) somar rows das duas tabelas — total_usd = round(sum(cost_usd or 0.0), 6), input_tokens = sum(input_tokens or 0), output_tokens = sum(output_tokens or 0); (g) retornar ProjectUsageResponse(project_id=project_id, month=month, total_usd=..., input_tokens=..., output_tokens=...).
    4) Criar docudata-backend/tests/test_project_usage.py com 3 casos pytest usando FastAPI TestClient e um cliente Supabase mockado (mesmo padrao usado em outros testes existentes se houver, senao pode usar monkeypatch no services.supabase_client.get_client): (i) usuario chama /usage sem month => response.month == mes atual America/Sao_Paulo e status 200; (ii) chama com month="2026-05" e supabase mock retorna 2 ingestions (cost 0.001 + 0.002, tokens 100/50 + 200/80) + 1 generated_doc (cost 0.005, tokens 300/120) => total_usd == 0.008, input_tokens == 600, output_tokens == 250; (iii) month="2026-13" => 422 com detail contendo "Formato invalido".

    Frontend (implementa USAGE-03 minimamente — a expansao com breakdown + lista fica no Plan 02):
    5) Em docudata-frontend/app/lib/api.ts adicionar interface ProjectUsage com { project_id: string; month: string; total_usd: number; input_tokens: number; output_tokens: number } e funcao async getProjectUsage(projectId: string, month?: string): Promise<ProjectUsage> que chama fetch(`${API}/projects/${projectId}/usage${month ? `?month=${month}` : ''}`). Reusa padrao das outras funcoes (throw new Error em !res.ok).
    6) Em docudata-frontend/app/projects/[id]/page.tsx: (a) estender TabId adicionando "custos" (na linha do type alias); (b) registrar nova entrada { id: "custos", label: "Custos" } no array de tabs entre "documentos" e "config"; (c) adicionar state useState<ProjectUsage | null>(null) para usage e state useState<string>(mesAtualYYYYMM) para currentMonth — computar mesAtualYYYYMM usando Intl.DateTimeFormat("pt-BR", { timeZone: "America/Sao_Paulo", year: "numeric", month: "2-digit" }) e reformatar para YYYY-MM; (d) adicionar useEffect que quando activeTab === "custos" chama getProjectUsage(id, currentMonth).then(setUsage); (e) renderizar bloco {activeTab === "custos" && usage && ...} usando sectionStyle existente, mostrando titulo "Custo do mes {currentMonth}", total formatado em USD e "input_tokens in + output_tokens out tokens" — visualmente similar a section "Custo de IA" que ja existe em activeTab === "config" (nao remover a de config; esta e um bloco novo dedicado ao mes).
    7) Verificar que o build TS passa (tsc --noEmit) e que nenhuma outra referencia a TabId precisa mudar.
  </action>
  <acceptance_criteria>
    - Backend: `curl -s "http://localhost:8000/projects/{id}/usage"` retorna 200 com JSON contendo os 5 campos definidos e month == mes atual YYYY-MM
    - Backend: `curl -s "http://localhost:8000/projects/{id}/usage?month=2026-13"` retorna 422 com detail sobre formato invalido
    - Backend: pytest docudata-backend/tests/test_project_usage.py passa nos 3 casos (mes default, agregacao 2+1 rows, month invalido)
    - Backend: total_usd, input_tokens e output_tokens sao lidos das colunas cost_usd/input_tokens/output_tokens de ingestions + generated_docs (nenhuma multiplicacao por tarifa acontece dentro do handler — grep no arquivo nao encontra "_COST_PER_INPUT_TOKEN" nem "* 0.15" dentro do novo handler)
    - Frontend: `cd docudata-frontend && npx tsc --noEmit` retorna 0 erros
    - Frontend: ao abrir /projects/{id} e clicar em "Custos" no Tabs, aparece uma section renderizando total_usd e tokens do mes atual (verificacao via app rodando local ja e feita no Plan 02; aqui o gate e o tsc + os testes de API)
    - Rastreamento D-USAGE-01: USAGE-01 satisfeita pelo endpoint + response model; USAGE-02 satisfeita pela ausencia de recalculo (grep negativo acima); USAGE-03 iniciada pela aba minima
  </acceptance_criteria>
  <verify>
    <automated>cd docudata-backend && pytest tests/test_project_usage.py -x -q && grep -c "_COST_PER_INPUT_TOKEN\|_COST_PER_OUTPUT_TOKEN" routers/projects.py | grep -q "^0$" && cd ../docudata-frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    Endpoint GET /projects/{id}/usage responde em 3 modos (sem month, month valido, month invalido), o handler agrega apenas colunas pre-existentes (nao recalcula), a aba "Custos" carrega o total do mes atual chamando esse endpoint, e tsc + pytest passam.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> FastAPI | project_id vem do path (UUID) e month vem de query string — ambos entradas nao confiaveis |
| FastAPI -> Supabase | queries usam PostgREST via supabase-py com filtros parametrizados (nao ha SQL raw) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | query param month | medium | mitigate | Regex ^\d{4}-(0[1-9]|1[0-2])$ + 422 antes de tocar Supabase; sem month = default calculado no servidor |
| T-06-02 | Information Disclosure | GET /projects/{id}/usage | low | accept | MVP nao tem auth (decisao consciente em CLAUDE.md — "sem auth v1"); mesmo modelo de /projects/{id}/cost ja aceito |
| T-06-03 | Denial of Service | agregacao no handler | low | accept | Sao 2 selects filtrados por project_id + intervalo de 1 mes — volume por projeto e da ordem de dezenas de linhas/mes; sem paginacao adicional necessaria |
</threat_model>

<verification>
1. Backend tests: `cd docudata-backend && pytest tests/test_project_usage.py -x`
2. Backend endpoint (contrato): `curl -s "http://localhost:8000/projects/<id-existente>/usage" | jq '{month, total_usd, input_tokens, output_tokens}'`
3. Backend endpoint (invalido): `curl -s -o /dev/null -w "%{http_code}\n" "http://localhost:8000/projects/<id>/usage?month=2026-13"` deve imprimir 422
4. Backend nao recalcula: `grep -n "_COST_PER" docudata-backend/routers/projects.py` nao pode aparecer no novo handler get_project_usage
5. Frontend tipos: `cd docudata-frontend && npx tsc --noEmit`
6. Frontend integracao smoke: rodar `npm run dev` no frontend + uvicorn no backend, abrir /projects/{id}, clicar na aba "Custos" — total do mes atual aparece
</verification>

<success_criteria>
- Endpoint GET /projects/{id}/usage retorna agregacao mensal usando exclusivamente colunas ja salvas em ingestions + generated_docs
- Nenhuma tarifa hardcoded (_COST_PER_INPUT_TOKEN / _COST_PER_OUTPUT_TOKEN) aparece dentro do handler novo
- ProjectUsageResponse tipado e exportado como response_model
- Aba "Custos" existe no dashboard e mostra o mes atual sem recarregar a pagina
- 3 testes pytest passam
- tsc --noEmit passa no frontend
</success_criteria>

<output>
Criar `.planning/phases/06-token-usage-panel/06-01-SUMMARY.md` quando terminar, listando: endpoint criado, testes escritos, aba adicionada, e o que fica pendente para o Plan 02 (breakdown por tipo + lista cronologica + seletor de mes).
</output>
