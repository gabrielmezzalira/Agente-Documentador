---
phase: 01-backend-foundation-extraction-proof
plan: 03
type: tdd
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - docudata-backend/evals/__init__.py
  - docudata-backend/evals/conftest.py
  - docudata-backend/evals/test_schema_validity.py
  - docudata-backend/evals/test_retry_edge.py
  - docudata-backend/evals/test_supabase_write.py
  - docudata-backend/requirements-dev.txt
autonomous: true
requirements: [EXTR-01, EXTR-02]

must_haves:
  truths:
    - "pytest evals/ runs and the code-based eval gates (schema validity, retry edge, supabase write) pass"
    - "test_retry_edge proves _roteador stops at exactly 2 retries (max 3 attempts) and never loops past tentativas=2"
    - "test_schema_validity proves ConteudoEstruturado enforces all 6 fields with correct list/str types"
  artifacts:
    - path: "docudata-backend/evals/test_retry_edge.py"
      provides: "Unit tests for _roteador conditional edge at tentativas 0/1/2 (AI-SPEC eval dim 5)"
      contains: "_roteador"
    - path: "docudata-backend/evals/test_schema_validity.py"
      provides: "Pydantic schema validation tests (AI-SPEC eval dim 1)"
      contains: "ConteudoEstruturado"
    - path: "docudata-backend/evals/test_supabase_write.py"
      provides: "salvar node write-integrity test with a stubbed Supabase client (AI-SPEC eval dim 6)"
      contains: "salvar"
  key_links:
    - from: "docudata-backend/evals/test_retry_edge.py"
      to: "graphs.extraction_graph._roteador"
      via: "import + assert routing"
      pattern: "from graphs.extraction_graph import"
    - from: "docudata-backend/evals/test_supabase_write.py"
      to: "graphs.extraction_graph.salvar"
      via: "monkeypatch get_client, call salvar"
      pattern: "salvar"
---

<objective>
Lock the AI-SPEC critical code-based eval dimensions (section 5: dim 1 schema validity, dim 5 retry edge correctness, dim 6 Supabase write integrity) as a pytest suite. These three dimensions are marked Critical/High and are the CI merge gates per AI-SPEC section 5 CI/CD Integration. They protect against the highest-stakes silent failures: structural drift, runaway/incorrect retry, and silent save failure (failure modes #2, #4 from AI-SPEC section 1).

Purpose: Without these tests, the retry edge and write integrity can regress invisibly. The LLM-judge dimensions (2, 3, 4) require a labeled reference dataset and are deferred to Phase 2 per AI-SPEC; this plan covers only the deterministic code-based gates that need no Gemini call.
Output: A passing `pytest evals/` suite that gates the extraction graph's structural and routing correctness without any live LLM or live DB dependency.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.MD
@.planning/phases/01-backend-foundation-extraction-proof/01-AI-SPEC.md
@.planning/phases/01-backend-foundation-extraction-proof/01-PATTERNS.md
@.planning/phases/01-backend-foundation-extraction-proof/01-02-SUMMARY.md

<interfaces>
<!-- Contracts under test — imported from Plans 01 and 02. -->
From models/schemas.py:
  class ConteudoEstruturado(BaseModel)  # 6 fields
From graphs/extraction_graph.py:
  class ExtractionState(TypedDict)
  def _roteador(state) -> "salvar" | "extrair_conteudo" | END
  async def salvar(state) -> dict   # writes to ingestions via get_client()
From services/supabase_client.py:
  def get_client() -> Client   # monkeypatch target for the write test
</interfaces>
</context>

<feature>
  <name>Code-based eval gates for the extraction graph (AI-SPEC dimensions 1, 5, 6)</name>
  <files>docudata-backend/evals/test_schema_validity.py, docudata-backend/evals/test_retry_edge.py, docudata-backend/evals/test_supabase_write.py, docudata-backend/evals/conftest.py, docudata-backend/evals/__init__.py, docudata-backend/requirements-dev.txt</files>
  <behavior>
    Dimension 1 (schema validity): ConteudoEstruturado rejects a payload missing any of the 6 fields; accepts a fully-populated payload; tarefas/decisoes/problemas/proximos_passos must be list[str] (a bare string or dict fails validation); resumo/contexto_cliente must be str.
    Dimension 5 (retry edge): _roteador({valido:True, tentativas:0}) == "salvar"; _roteador({valido:False, tentativas:0}) == "extrair_conteudo"; _roteador({valido:False, tentativas:1}) == "extrair_conteudo"; _roteador({valido:False, tentativas:2}) == END; never returns "extrair_conteudo" once tentativas >= 2 (no infinite loop).
    Dimension 6 (write integrity): with get_client monkeypatched to a fake whose insert().execute() returns an object with .data == [{...inserted row...}], salvar returns {} (success) and the insert payload contains project_id, sprint_number, file_name, file_type, extracted_content equal to the input state; when the fake returns .data == [], salvar returns a dict with valido False and an erro string (does NOT report success) — closing failure mode #4.
  </behavior>
  <implementation>
    Create `requirements-dev.txt` with `pytest>=8` (and pytest-asyncio>=0.23 for the async salvar test). Create `evals/__init__.py` (empty) and `evals/conftest.py` ensuring the backend root is importable (insert the docudata-backend dir on sys.path so `from graphs...` / `from models...` resolve when pytest runs from the backend dir) and registering asyncio mode (set `asyncio_mode = "auto"` via a pytest config or an `@pytest.mark.asyncio` decorator on the async test).

    `test_schema_validity.py`: import ConteudoEstruturado; use pytest.raises(ValidationError) for a payload missing a field and for wrong types (e.g., tarefas=123, resumo=None); assert a valid 6-field payload constructs and model_dump() yields all 6 keys. This is AI-SPEC dim 1 — pure Pydantic, no LLM.

    `test_retry_edge.py`: import _roteador and END from graphs.extraction_graph; parametrize the four (valido, tentativas) -> expected-target cases from the behavior block; add an assertion that for tentativas in {2,3} with valido=False the result is END (proves the loop terminates — AI-SPEC dim 5, ROADMAP success criterion #5).

    `test_supabase_write.py`: import salvar from graphs.extraction_graph. Build a fake client object whose `.table(name).insert(payload).execute()` records the payload and returns a stub with a configurable `.data`. Monkeypatch `services.supabase_client.get_client` (and/or the name salvar imports) to return the fake. Run salvar via asyncio (pytest-asyncio). Case A: data non-empty -> salvar returns {} and recorded payload keys == {project_id, sprint_number, file_name, file_type, extracted_content} with values matching the input state. Case B: data == [] -> salvar return dict has valido False and a non-empty erro (AI-SPEC dim 6 / failure mode #4). No live Supabase, no network.

    Do NOT add LLM-judge tests (dims 2, 3, 4) here — they need the labeled reference dataset (12 examples) and live Gemini, deferred to Phase 2 per AI-SPEC section 5.
  </implementation>
</feature>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Schema-validity and retry-edge eval tests (RED -> GREEN)</name>
  <files>docudata-backend/requirements-dev.txt, docudata-backend/evals/__init__.py, docudata-backend/evals/conftest.py, docudata-backend/evals/test_schema_validity.py, docudata-backend/evals/test_retry_edge.py</files>
  <read_first>
    - docudata-backend/models/schemas.py (ConteudoEstruturado under test)
    - docudata-backend/graphs/extraction_graph.py (_roteador + END under test)
    - .planning/phases/01-backend-foundation-extraction-proof/01-AI-SPEC.md (section 5 Dimensions table dims 1 and 5; CI/CD Integration block)
  </read_first>
  <behavior>
    - ConteudoEstruturado(missing field) raises ValidationError; valid 6-field payload constructs and model_dump has 6 keys; tarefas=123 and resumo=None raise ValidationError
    - _roteador returns "salvar" / "extrair_conteudo" / END exactly per the (valido, tentativas) matrix; tentativas>=2 with valido=False always returns END
  </behavior>
  <action>
    Create `requirements-dev.txt` (pytest>=8, pytest-asyncio>=0.23), `evals/__init__.py` (empty), and `evals/conftest.py` (put the backend root on sys.path; set asyncio auto mode). Write `evals/test_schema_validity.py` and `evals/test_retry_edge.py` per the feature implementation block. Since the production code already exists (Plans 01-02), these tests should pass on first run (GREEN) — if any fails, it has found a real regression in the extraction graph or schema; fix the production file, do not weaken the test.
  </action>
  <verify>
    <automated>cd docudata-backend && pip install -r requirements-dev.txt -q && python -m pytest evals/test_schema_validity.py evals/test_retry_edge.py -v</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest evals/test_schema_validity.py evals/test_retry_edge.py` exits 0 with all tests passing
    - test_retry_edge.py imports `from graphs.extraction_graph import _roteador` and asserts the END sentinel (not the string "END") for the exhausted-retry cases
    - test_retry_edge.py includes an assertion that valido=False with tentativas in {2,3} returns END (loop termination)
    - test_schema_validity.py uses pytest.raises for at least one missing-field and one wrong-type case
  </acceptance_criteria>
  <done>Schema and retry-edge tests pass; AI-SPEC dims 1 and 5 are gated by code.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Supabase write-integrity eval test with stubbed client (RED -> GREEN)</name>
  <files>docudata-backend/evals/test_supabase_write.py</files>
  <read_first>
    - docudata-backend/graphs/extraction_graph.py (salvar node under test)
    - docudata-backend/services/supabase_client.py (get_client — monkeypatch target)
    - docudata-backend/evals/conftest.py (from Task 1 — asyncio mode + sys.path)
    - .planning/phases/01-backend-foundation-extraction-proof/01-AI-SPEC.md (section 5 dim 6 Supabase write integrity; section 1 failure mode #4; section 6 Supabase write confirmation guardrail)
    - .planning/phases/01-backend-foundation-extraction-proof/01-RESEARCH.md (Pitfall 5 response.data)
  </read_first>
  <behavior>
    - With a fake client returning .data non-empty: salvar returns {} and the recorded insert payload keys are exactly {project_id, sprint_number, file_name, file_type, extracted_content} matching the input ExtractionState values
    - With a fake client returning .data == []: salvar returns a dict where valido is False and erro is a non-empty string (no false success) — AI-SPEC failure mode #4
  </behavior>
  <action>
    Write `evals/test_supabase_write.py` per the feature implementation block. Build a FakeClient with chainable `.table().insert().execute()` capturing the payload and returning a stub whose `.data` is parametrizable. Monkeypatch the get_client symbol that `salvar` resolves at call time (patch `services.supabase_client.get_client`; if salvar imports get_client into the graphs module namespace, patch `graphs.extraction_graph.get_client` instead — verify which by reading the import in extraction_graph.py). Drive salvar with `@pytest.mark.asyncio` (or asyncio auto mode). Assert Case A (success -> {} + correct payload) and Case B (empty data -> valido False + erro). No network, no real Supabase.
  </action>
  <verify>
    <automated>cd docudata-backend && python -m pytest evals/test_supabase_write.py -v && python -m pytest evals/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest evals/test_supabase_write.py` exits 0
    - The test monkeypatches get_client (no live Supabase connection is opened — confirmed by the test running without SUPABASE_* env vars set)
    - Case A asserts the recorded payload has exactly the 5 ingestion column keys with values equal to the input state
    - Case B asserts salvar's return has valido False and a non-empty erro when response.data is empty
    - `python -m pytest evals/ -q` exits 0 (full suite green)
  </acceptance_criteria>
  <done>Write-integrity test passes with a stubbed client; the full evals/ suite is green; AI-SPEC dim 6 / failure mode #4 is gated by code.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code -> production modules | Tests import and exercise extraction_graph/schemas; no external boundary crossed (stubbed client, no live LLM/DB) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Retry loop regressing to infinite | mitigate | test_retry_edge asserts END at tentativas>=2 — CI gate blocks merge of a non-terminating router (AI-SPEC dim 5) |
| T-03-02 | Information Disclosure | Tests requiring real SUPABASE_SERVICE_KEY | mitigate | salvar test uses a stubbed client — no real credentials needed, no key in CI logs |
| T-03-03 | Denial of Service | Eval suite calling live Gemini on every CI run | accept | Code-based dims (1,5,6) use no LLM; LLM-judge dims deferred to Phase 2 — keeps CI fast and free |
| T-03-SC | Tampering | pytest/pytest-asyncio installs | mitigate | Both are flagship test-ecosystem packages on PyPI (multi-year history); pinned floor in requirements-dev.txt; no [SUS]/[SLOP] |
</threat_model>

<verification>
- `pytest evals/` exits 0 with all schema-validity, retry-edge, and write-integrity tests passing.
- Retry edge terminates at tentativas>=2 (no infinite loop).
- salvar reports failure (not success) when response.data is empty.
- No live Gemini or live Supabase dependency in the suite.
</verification>

<success_criteria>
- AI-SPEC section 5 code-based eval dimensions 1, 5, 6 are implemented as passing pytest gates.
- ROADMAP Phase 1 success criterion #5 (retry up to 2 times) is independently test-verified.
- The suite runs offline (no credentials, no network), suitable as a CI merge gate.
</success_criteria>

<output>
Create `.planning/phases/01-backend-foundation-extraction-proof/01-03-SUMMARY.md` when done. Note any production-code regression the tests surfaced and how it was fixed.
</output>
