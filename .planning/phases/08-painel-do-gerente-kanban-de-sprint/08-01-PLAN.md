---
id: "08-01"
phase: 08-painel-do-gerente-kanban-de-sprint
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-backend/routers/painel.py
  - docudata-backend/main.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/components/PainelTab.tsx
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - "M2 Blocos A, B, C, D (§5)"
  - "M3 (§5)"

estimate:
  tokens: 90000
  raw_tokens: 45000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "GET /projects/{id}/painel returns HTTP 200 with JSON containing keys bloco_a, bloco_b, bloco_c, bloco_d"
    - "Bloco A returns sem_dados=true when data_inicio or data_fim_contratada is null, sem_dados=false with numeric fields when both are set"
    - "Bloco B lists funcionalidades where status=em_andamento and last status transition >7 days ago under travadas key"
    - "Bloco C returns wip, throughput_por_semana, cycle_time_p50_dias, cycle_time_p85_dias (null when <1 concluida)"
    - "Bloco D returns fases_resumo dict keyed by phase name, eficiencia_fluxo_pct, detalhe_por_funcionalidade list"
    - "Frontend aba Painel is the 2nd tab (after Sprints, before Tecnologias) and renders the raw JSON data when active"
    - "No 500 errors when project has zero funcionalidades"
  artifacts:
    - "docudata-backend/routers/painel.py — GET /projects/{id}/painel endpoint with full calculation logic"
    - "docudata-backend/main.py — updated import and include_router for painel"
    - "docudata-frontend/app/lib/api.ts — PainelData, BlocoA/B/C/D, FuncionalidadeResponse interfaces + getPainel() function"
    - "docudata-frontend/app/components/PainelTab.tsx — component that fetches and displays painel data"
    - "docudata-frontend/app/projects/[id]/page.tsx — TabId union includes painel, Tabs array has Painel entry, PainelTab rendered"
  key_links:
    - "painel.py imports and uses get_client() from services.supabase_client — same pattern as funcionalidades.py"
    - "PainelTab.tsx calls getPainel(projectId) from api.ts on mount"
    - "page.tsx renders <PainelTab projectId={id} sprints={sprints} /> when activeTab === painel"
---

<objective>
Tracer: wire the full end-to-end path for the Painel tab — backend endpoint calculates all 4 metric blocks (A/B/C/D) from existing Supabase tables, frontend tab fetches and displays the result.

Purpose: Prove the architecture works before expanding into full visual polish. A manager opening the Painel tab should see real metric data from the database by the end of this plan.

Output: routers/painel.py (all calculation logic), updated main.py, api.ts interfaces + getPainel(), PainelTab.tsx (data fetch + raw display), page.tsx wiring.

User story: As a project manager, I want to open the Painel tab and see calculated metrics from my project data, so that I can understand health at a glance without manual calculation.
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/PROJECT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/ROADMAP.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-CONTEXT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Backend GET /projects/{id}/painel — all 4 blocks calculated end-to-end</name>

  <read_first>
    - docudata-backend/routers/funcionalidades.py — read lines 1-50 (router setup, get_client usage) and lines 140-200 (transicoes_status query pattern with .in_() / filter logic)
    - docudata-backend/routers/projects.py — lines 1-40 (APIRouter prefix pattern, HTTPException 404, supabase query)
    - docudata-backend/main.py — full file (current import line and include_router calls)
    - docudata-backend/models/schemas.py — lines 28-42 (ProjectResponse fields: data_inicio, data_fim_contratada, tolerancia_desvio_pontos), lines 234-259 (FuncionalidadeResponse, TransicaoStatusResponse including duracao_fase_anterior_segundos)
    - docudata-backend/services/supabase_client.py — full file (get_client() signature)
  </read_first>

  <action>
Create docudata-backend/routers/painel.py. This file must NOT contain any fenced code blocks — pure Python.

Router setup (per D-10):
- APIRouter with prefix="/projects", tags=["painel"]
- Single endpoint: GET /{project_id}/painel

Imports needed: fastapi (APIRouter, HTTPException), datetime (date, datetime, timezone), statistics, collections (defaultdict), services.supabase_client (get_client)

Endpoint body — 3 Supabase queries:
1. Fetch project row: client.table("projects").select("*").eq("id", project_id).single().execute() — raise HTTPException(404) if .data is None
2. Fetch all funcionalidades: client.table("funcionalidades").select("*").eq("project_id", project_id).execute() — store as func_list = result.data (may be empty list)
3. Fetch transicoes_status only if func_list is non-empty: func_ids = [f["id"] for f in func_list]; if func_ids is empty, set trans_list = []; otherwise client.table("transicoes_status").select("*").in_("funcionalidade_id", func_ids).order("timestamp", desc=False).execute() — store as trans_list = result.data (per pitfall 2 from RESEARCH.md: never call .in_() with empty list)

After fetching, call four pure-Python helper functions and return their combined result as a dict:
- calcular_bloco_a(proj, func_list) — per D-05: no DB write, only calculation
- calcular_bloco_b(func_list, trans_list) — per D-11, D-12
- calcular_bloco_c(func_list, trans_list) — per D-13, D-14, D-15
- calcular_bloco_d(func_list, trans_list)

Return: {"bloco_a": bloco_a, "bloco_b": bloco_b, "bloco_c": bloco_c, "bloco_d": bloco_d}

Helper function specifications (implement as module-level functions, not nested):

calcular_bloco_a(proj, funcs) — per D-03, D-05:
  - If proj.get("data_inicio") is falsy OR proj.get("data_fim_contratada") is falsy → return {"sem_dados": True}
  - Convert string dates with date.fromisoformat() if they are strings (pitfall 3)
  - hoje = date.today()
  - total_dias = (data_fim - data_inicio).days; if total_dias <= 0, pct_prazo = 0
  - dias_consumidos = max((hoje - data_inicio).days, 0)
  - pct_prazo = round(min(dias_consumidos / total_dias * 100, 100), 1) (pitfall 6: cap at 100)
  - total_funcs = len(funcs); concluidas = count where status=="concluida"; aprovadas = count where status_cliente=="aprovado"
  - pct_escopo = round(concluidas/total_funcs*100, 1) if total_funcs>0 else 0
  - pct_aprovado = round(aprovadas/total_funcs*100, 1) if total_funcs>0 else 0
  - tolerancia = proj.get("tolerancia_desvio_pontos") or 0
  - desvio = pct_prazo - pct_aprovado; desvio_detectado = desvio > tolerancia (per D-05: bool only, no DB write)
  - Return dict with all fields: sem_dados=False, pct_prazo_consumido, pct_escopo_concluido, pct_aprovado_cliente, desvio_detectado, desvio_pontos=round(desvio,1)

calcular_bloco_b(funcs, transicoes) — per D-11, D-12:
  - hoje = datetime.now(timezone.utc)
  - Build ultima_transicao_status: dict[func_id, datetime] — iterate transicoes where campo=="status", parse timestamp with datetime.fromisoformat().replace(tzinfo=timezone.utc), last wins (list is ASC)
  - Build ultima_ts_enviado: dict[func_id, datetime] — iterate transicoes where campo=="status_cliente" AND para=="enviado", keep the most recent
  - travadas list: funcs where status=="em_andamento" AND (hoje - ultima_transicao_status.get(fid, created_at_dt)).days > 7 → {id, titulo, dias}
  - aguardando_cliente list: funcs where status_cliente=="enviado" AND ultima_ts_enviado exists AND dias_corridos*5/7 > 5 → {id, titulo, dias_uteis: round(x,1)} (per D-12)
  - em_ajuste list: funcs where status=="em_ajuste" → {id, titulo}
  - Return {"travadas": [...], "aguardando_cliente": [...], "em_ajuste": [...]}

calcular_bloco_c(funcs, transicoes) — per D-13, D-14, D-15:
  - Group transicoes by funcionalidade_id into dict
  - Cycle time calculation (per D-15): for each func with status=="concluida", find first transition to "em_andamento" (campo=="status", para=="em_andamento") and last transition to "concluida" (campo=="status", para=="concluida"); if both found and ts_fim > ts_inicio: cycle_time_days = (ts_fim - ts_inicio).total_seconds() / 86400
  - WIP: count funcs with status in ("em_andamento", "em_ajuste")
  - Throughput (per discretion note): total_concluidas / max(weeks_since_first_func, 1) where weeks = (now - min(created_at)).days / 7; round to 2
  - Percentiles (pitfall 1): if len>=2 use statistics.quantiles(cycle_times, n=100)[49] and [84]; elif len==1 p50=p85=cycle_times[0]; else None
  - Return {"throughput_por_semana": throughput, "wip": wip, "cycle_time_p50_dias": p50, "cycle_time_p85_dias": p85, "total_concluidas": len(cycle_times)}

calcular_bloco_d(funcs, transicoes) — per D-04 (accordion data) and discretion formula:
  - Collect duracao_fase_anterior_segundos per (campo=="status", de) — each row: duracoes_por_fase[t["de"]].append(dur/86400); also accumulate per func
  - Guard: skip rows where duracao_fase_anterior_segundos is None (pitfall 4)
  - Eficiencia de fluxo per each func: tempo_em_andamento_dias / tempo_total_de_vida_dias * 100; tempo total = sum of all status transition durations (or hoje - created_at if none); average across funcs with tempo_total > 0
  - fases_resumo: for each phase seen in duracoes_por_fase: media_dias=round(mean,1); p85_dias via statistics.quantiles if len>=2 else single value or None; amostras=len
  - detalhe_por_funcionalidade: list of {id, titulo, tempos_por_fase: {fase: total_dias}} — only funcs that have at least one status transition with duracao recorded
  - Return {"fases_resumo": {...}, "eficiencia_fluxo_pct": eficiencia_media, "detalhe_por_funcionalidade": [...]}

After creating painel.py, update docudata-backend/main.py:
  - Add "painel" to the existing import: from routers import ..., funcionalidades, painel
  - Add app.include_router(painel.router) after app.include_router(funcionalidades.router)
  </action>

  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend && python -c "from routers.painel import router; print('import ok')" && python -c "from routers.painel import calcular_bloco_a, calcular_bloco_b, calcular_bloco_c, calcular_bloco_d; print('functions ok')" && grep -c "include_router(painel.router)" main.py</automated>
  </verify>

  <acceptance_criteria>
    - python -c "from routers.painel import router" exits 0 with no ImportError
    - python -c "from routers.painel import calcular_bloco_a, calcular_bloco_b, calcular_bloco_c, calcular_bloco_d; print('functions ok')" exits 0
    - grep -c "include_router(painel.router)" main.py returns 1 (router is wired)
    - main.py contains the string "painel" in the routers import line
    - main.py contains "app.include_router(painel.router)"
    - routers/painel.py defines GET /{project_id}/painel endpoint (grep "project_id}/painel" finds a match)
    - calcular_bloco_a, calcular_bloco_b, calcular_bloco_c, calcular_bloco_d are defined as module-level functions in painel.py
    - calcular_bloco_a returns {"sem_dados": True} for proj with data_inicio=None (verify by unit-calling the function with {"data_inicio": None, "data_fim_contratada": None} and empty list)
    - statistics module is imported in painel.py and the quantiles guard (len >= 2 check) is present
    - The .in_() query on transicoes_status is guarded by "if func_ids:" or "if not func_ids" early return
  </acceptance_criteria>

  <done>routers/painel.py exists, imports cleanly, and main.py is wired with the new router. All 4 calculation functions are defined and importable. IMPORTANT: 08-02 depends on 08-01-SUMMARY.md — the executor must create .planning/phases/08-painel-do-gerente-kanban-de-sprint/08-01-SUMMARY.md before 08-02 can start.</done>
</task>

<task type="auto">
  <name>Tracer: Frontend PainelTab.tsx + page.tsx wiring + api.ts types</name>

  <read_first>
    - docudata-frontend/app/components/TechnologiesTab.tsx — full file (padrão de aba isolada: useEffect on mount, loading/error/data state, inline styles only)
    - docudata-frontend/app/projects/[id]/page.tsx — lines 1-50 (imports, TabId union at line 46), lines 460-480 (Tabs array), lines 553-556 (TechnologiesTab render pattern)
    - docudata-frontend/app/lib/api.ts — lines 1-30 (API constant, existing imports/exports) to understand base URL pattern; also check if getPainel or FuncionalidadeResponse already exist
  </read_first>

  <action>
Step 1 — Update docudata-frontend/app/lib/api.ts:

Append at the end of the file (do not modify existing functions). Add:

Interface FuncionalidadeResponse:
  id, project_id, id_funcional, titulo, descricao (optional string), criterios_aceite (string[]), prioridade (string), status typed as "nao_iniciada" | "em_andamento" | "em_ajuste" | "concluida", status_cliente typed as "nao_enviado" | "enviado" | "aprovado" | "rejeitado", data_aprovacao_cliente (optional string or null), responsavel (optional string or null), sprint_alvo (optional string or null), created_at (string)

Interface BlocoA:
  sem_dados: boolean; pct_prazo_consumido?: number; pct_escopo_concluido?: number; pct_aprovado_cliente?: number; desvio_detectado?: boolean; desvio_pontos?: number

Interface BlocoB:
  travadas: Array of {id: string; titulo: string; dias: number}; aguardando_cliente: Array of {id: string; titulo: string; dias_uteis: number}; em_ajuste: Array of {id: string; titulo: string}

Interface BlocoC:
  throughput_por_semana: number | null; wip: number; cycle_time_p50_dias: number | null; cycle_time_p85_dias: number | null; total_concluidas: number

Interface FaseResumo:
  media_dias: number; p85_dias: number | null; amostras: number

Interface BlocoD:
  fases_resumo: Record<string, FaseResumo>; eficiencia_fluxo_pct: number | null; detalhe_por_funcionalidade: Array of {id: string; titulo: string; tempos_por_fase: Record<string, number>}

Interface PainelData:
  bloco_a: BlocoA; bloco_b: BlocoB; bloco_c: BlocoC; bloco_d: BlocoD

Function getPainel(projectId: string): Promise<PainelData> — fetch `${API}/projects/${projectId}/painel`, throw Error("Erro ao buscar painel") if !res.ok, return res.json()

Function listFuncionalidades(projectId: string): Promise<FuncionalidadeResponse[]> — fetch `${API}/funcionalidades?project_id=${projectId}`, throw Error on !res.ok, return res.json()

Export all new interfaces and functions.

Step 2 — Create docudata-frontend/app/components/PainelTab.tsx:

"use client" directive at top.
Imports: useEffect, useState from "react"; getPainel, listFuncionalidades, PainelData, FuncionalidadeResponse, SprintWithStatus from "../lib/api"

Props interface: { projectId: string; sprints: SprintWithStatus[] }

Component state:
  - data: PainelData | null (useState null)
  - funcionalidades: FuncionalidadeResponse[] (useState [])
  - loading: boolean (useState true)
  - error: string | null (useState null)
  - sprintSelecionada: number (useState initialized to sprints.length > 0 ? Math.max(...sprints.map(s => s.numero)) : 1)
  - expandedFase: string | null (useState null) — for Bloco D accordion (per D-04)

useEffect on mount (projectId as dep):
  setLoading(true)
  Promise.all([getPainel(projectId), listFuncionalidades(projectId)])
    .then(([d, funcs]) => { setData(d); setFuncionalidades(funcs); setError(null) })
    .catch(e => setError(e instanceof Error ? e.message : "Erro"))
    .finally(() => setLoading(false))

Render loading state: a section div with inline styles matching TechnologiesTab.tsx sectionStyle (background:#ffffff, border:1px solid #e8e8ed, borderRadius:14, padding:"20px 22px") containing <p style={{color:"#9696a0", fontSize:14, margin:0}}>Carregando painel…</p>

Render error state: same section with <p style={{color:"#dc2626", fontSize:14, margin:0}}>{error}</p>

Render data: In this tracer task, render a simple but complete structure:

1. Header: <h2 style={{fontSize:16, fontWeight:700, color:"#111116", marginBottom:16}}>Painel do Projeto</h2>

2. Grid 2x2 container (per D-02): div with style={{display:"grid", gridTemplateColumns:"1fr 1fr", gap:14, marginBottom:24}}

   - Bloco A card: div with sectionStyle-like border. Show title "Tempo × Escopo". If data.bloco_a.sem_dados, show "Sem dados de contrato" text in gray (per D-03). Otherwise show pct_prazo_consumido%, pct_escopo_concluido%, pct_aprovado_cliente%. If desvio_detectado, show a small orange badge "Desvio detectado" (per D-05: visual only).

   - Bloco B card: title "Itens Travados". Show counts: travadas.length, aguardando_cliente.length, em_ajuste.length as three labeled numbers.

   - Bloco C card: title "Métricas de Fluxo". Show WIP: {wip}, Throughput: {throughput_por_semana ?? "—"}/semana, Cycle time p50: {cycle_time_p50_dias ?? "—"} dias, p85: {cycle_time_p85_dias ?? "—"} dias.

   - Bloco D card: title "Tempo por Fase". Show eficiencia_fluxo_pct ?? "—" % efficiency. For each entry in Object.entries(data.bloco_d.fases_resumo): show a row with fase name, media_dias dias. Accordion toggle (per D-04): clicking "ver detalhe" on a fase sets expandedFase to that fase key (or null to close); when expanded, render the list of funcionalidades from detalhe_por_funcionalidade that have that fase key in tempos_por_fase.

3. Kanban section (per D-06, D-07, D-09):

   Sprint dropdown: <select value={sprintSelecionada} onChange={e => setSprintSelecionada(Number(e.target.value))}> — options from sprints array sorted by numero descending (per D-06: default = most recent)

   Filter funcionalidades per D-09: funcsDaSprint = funcionalidades.filter(f => f.sprint_alvo !== null && f.sprint_alvo !== undefined && f.sprint_alvo === String(sprintSelecionada))

   3-column grid (per D-07): display:grid, gridTemplateColumns:"1fr 1fr 1fr", gap:14
   - Planejado column: funcsDaSprint.filter(f => f.status === "nao_iniciada")
   - Em andamento column: funcsDaSprint.filter(f => ["em_andamento","em_ajuste"].includes(f.status))
   - Concluido column: funcsDaSprint.filter(f => f.status === "concluida")

   Each column: header label + count badge. Each funcionalidade card: titulo, prioridade chip, sprint_alvo badge chip (per D-08: chip with sprint_alvo value — NOT multi-sprint, single chip).

   All styling via inline styles only — no Tailwind, no className.

Step 3 — Update docudata-frontend/app/projects/[id]/page.tsx:

a) Line 46: Update TabId union. Current: type TabId = "sprints" | "tecnologias" | "cross_sprint" | "documentos" | "custos" | "config"
   New: type TabId = "sprints" | "painel" | "tecnologias" | "cross_sprint" | "documentos" | "custos" | "config"

b) Add PainelTab to imports: import PainelTab from "../../components/PainelTab"

c) In the Tabs array (around line 469-478), add { id: "painel", label: "Painel" } as the second entry, after the "sprints" entry and before "tecnologias":
   { id: "sprints", label: "Sprints", badge: ... },
   { id: "painel", label: "Painel" },   // NEW
   { id: "tecnologias", label: "Tecnologias" },
   ...

d) After the {activeTab === "tecnologias" && <TechnologiesTab projectId={id} />} block, add:
   {activeTab === "painel" && <PainelTab projectId={id} sprints={sprints} />}

   Place this BEFORE the "tecnologias" render (per D-01: Painel is between Sprints and Tecnologias).
  </action>

  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - api.ts exports PainelData, BlocoA, BlocoB, BlocoC, BlocoD, FaseResumo, FuncionalidadeResponse, getPainel, listFuncionalidades (grep each export in the file)
    - PainelTab.tsx exists and exports default function PainelTab
    - PainelTab.tsx imports getPainel and listFuncionalidades from "../lib/api"
    - page.tsx TabId union string contains "painel"
    - page.tsx Tabs array contains the string "Painel" as a label entry
    - page.tsx renders PainelTab when activeTab === "painel" (grep "activeTab === \"painel\"" finds a match)
    - No Tailwind class names (className=) appear in PainelTab.tsx — all styling is inline style={{...}} (grep -c "className=" PainelTab.tsx returns 0)
    - The sprint dropdown in PainelTab defaults to the highest sprint numero (Math.max pattern visible in the code)
  </acceptance_criteria>

  <done>PainelTab.tsx renders all 4 blocks and the kanban section. page.tsx shows the Painel tab as the 2nd tab. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → FastAPI | project_id path param is untrusted input from URL |
| FastAPI → Supabase | Queries use user-supplied project_id — must validate project exists |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-01 | Tampering | GET /{project_id}/painel — path param | medium | mitigate | Validate project exists via projects table lookup, raise 404 if not found — prevents data leakage across projects |
| T-08-02 | Information Disclosure | bloco_d.detalhe_por_funcionalidade | low | accept | Endpoint is read-only; data returned belongs to the project being queried; no cross-project leakage if 404 guard is in place |
| T-08-03 | Denial of Service | statistics.quantiles() on large datasets | low | accept | N < 10000 funcionalidades per project is reasonable bound; stdlib is efficient enough at this scale |
| T-08-SC | Tampering | npm/pip installs | low | accept | No new packages installed in this phase — statistics is Python stdlib, no new npm deps |
</threat_model>

<verification>
End-to-end smoke test (requires backend running on port 8000):

```bash
# Start backend: cd docudata-backend && uvicorn main:app --port 8000
# Then:
curl -s http://localhost:8000/projects/{any_valid_project_id}/painel | python3 -m json.tool | grep -E "bloco_[abcd]"
# Expected: 4 keys printed

curl -s http://localhost:8000/projects/nonexistent-uuid/painel
# Expected: {"detail": "Projeto não encontrado"} with HTTP 404
```

Frontend smoke test (requires `npm run dev` in docudata-frontend):
1. Open the project dashboard in browser
2. Verify "Painel" tab appears between "Sprints" and "Tecnologias" in the tab bar
3. Click "Painel" tab — should show loading indicator then the 4 block cards
4. Bloco A should show "Sem dados de contrato" if project has no data_inicio/data_fim_contratada
5. No console errors
</verification>

<success_criteria>
- curl GET /projects/{id}/painel returns 200 with JSON containing bloco_a, bloco_b, bloco_c, bloco_d
- curl GET /projects/bad-uuid/painel returns 404
- curl GET /projects/{id-with-zero-funcionalidades}/painel returns 200 with empty lists in bloco_b, null cycle times in bloco_c, empty fases_resumo in bloco_d
- TypeScript build passes with no errors
- Painel tab appears 2nd in the tab bar and renders PainelTab on click
</success_criteria>

<output>
Create `.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-01-SUMMARY.md` when done
</output>
