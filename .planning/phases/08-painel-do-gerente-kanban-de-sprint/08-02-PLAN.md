---
id: "08-02"
phase: 08-painel-do-gerente-kanban-de-sprint
plan: 02
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - docudata-frontend/app/components/PainelTab.tsx
autonomous: true
requirements:
  - "M2 Blocos A, B, C, D (§5)"
  - "M3 (§5)"

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 2
  confidence: med

must_haves:
  truths:
    - "Bloco A renders grey placeholder card with Sem dados de contrato text and link to Configuracoes tab when sem_dados is true"
    - "Bloco A renders three percentage bars (prazo, escopo, aprovado) and an orange Desvio detectado badge when desvio_detectado is true"
    - "Bloco B renders three sub-sections: Travadas, Aguardando cliente, Em ajuste — each listing funcionalidade titles and counts"
    - "Bloco C renders WIP, throughput and cycle time p50/p85 with null displayed as dash character"
    - "Bloco D accordion: clicking ver detalhe on a phase row expands the per-funcionalidade time list inline; clicking again collapses it"
    - "Kanban shows 3 column headers (Planejado, Em andamento, Concluido) with funcionalidade counts and each card shows titulo plus sprint_alvo chip"
    - "Kanban sprint dropdown lists all sprints by numero and defaults to the highest one on tab open"
    - "All styling uses inline style objects only — zero className attributes in PainelTab.tsx"
  artifacts:
    - "docudata-frontend/app/components/PainelTab.tsx — fully polished component with all visual states, accordion, and kanban"
  key_links:
    - "Bloco D expandedFase useState controls which phase row is expanded in the accordion"
    - "sprintSelecionada useState filters funcionalidades for the kanban columns"
    - "Bloco A sem_dados branch links to Configuracoes tab — must use a button with onClick that propagates up or use a callback prop"
---

<objective>
Expansion: Replace the raw-data tracer display in PainelTab.tsx with the full polished layout — grid 2×2 with all four blocks properly rendered, the Bloco D accordion (D-04), the visual desvio alert (D-05), and the three-column kanban with sprint dropdown (D-06, D-07, D-08, D-09).

Purpose: Deliver the full visual experience defined in CONTEXT.md decisions so a manager can actually read and act on the metrics.

Output: PainelTab.tsx fully implemented. No new backend files, no new routes.
</objective>

<execution_context>
@/Users/gabrielmezzalira/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-CONTEXT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-RESEARCH.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Expand Bloco A, B, C, D visual rendering in PainelTab.tsx</name>

  <read_first>
    - docudata-frontend/app/components/PainelTab.tsx — full file (understand current tracer render structure before replacing it)
    - docudata-frontend/app/components/TechnologiesTab.tsx — lines 10-61 (inline style constants: sectionStyle, sectionTitle, chipStyle — use same color palette)
    - docudata-frontend/app/projects/[id]/page.tsx — lines 1-50 (look for any setActiveTab exposed or onTabChange pattern; needed if Bloco A "Sem dados" link should switch to "config" tab)
  </read_first>

  <action>
Rewrite the main data-render section of PainelTab.tsx to produce fully polished blocks. Keep the existing state declarations, useEffect, and loading/error renders unchanged. Only replace the JSX returned when data is non-null and loading is false.

IMPORTANT: no fenced code blocks in this action. All instructions are prose.

Props update: Add onNavigateToConfig?: () => void to Props interface — this is called when the user clicks the "Ir para Configuracoes" link in Bloco A sem_dados state (per D-03). Caller (page.tsx) will wire setActiveTab("config") to this prop.

Add to page.tsx: update the PainelTab render to pass onNavigateToConfig={() => setActiveTab("config" as TabId)}.

Inline style constants to define at top of PainelTab.tsx (before the component):
- cardStyle: background #ffffff, border 1px solid #e8e8ed, borderRadius 14, padding "20px 22px"
- cardTitleStyle: fontSize 11, fontWeight 700, letterSpacing "0.1em", textTransform "uppercase", color "#9696a0", marginBottom 14, margin 0 for bottom, display "block"
- metricLabelStyle: fontSize 12, color "#6a6a7a", marginBottom 2
- metricValueStyle: fontSize 22, fontWeight 800, color "#111116", letterSpacing "-0.02em"
- chipBaseStyle: display "inline-flex", alignItems "center", padding "4px 10px", borderRadius 999, fontSize 12, fontWeight 600
- subSectionTitleStyle: fontSize 12, fontWeight 700, color "#374151", marginBottom 8, marginTop 12

Outer layout container: div with display grid, gridTemplateColumns "1fr 1fr", gap 14, marginBottom 24 — contains the 4 block cards.

BLOCO A — "Tempo x Escopo" (per D-02, D-03, D-05):

Card div with cardStyle.

If data.bloco_a.sem_dados:
  - Title "TEMPO x ESCOPO" in cardTitleStyle
  - Gray text "Sem dados de contrato" (fontSize 13, color "#9696a0")
  - A button styled as inline link (background none, border none, color "#4ade80", fontSize 13, cursor pointer, padding 0, textDecoration underline) with text "Ir para Configuracoes" that calls onNavigateToConfig?.()
  - Per D-03: the grid remains 2x2 — the card stays in its position, just showing the placeholder content

If data.bloco_a.sem_dados is false:
  - Title "TEMPO x ESCOPO"
  - Three metric rows in a flex column with gap 10:
    Each row: label (metricLabelStyle) + value (metricValueStyle + "%" suffix)
    Labels: "Prazo consumido", "Escopo concluido", "Aprovado pelo cliente"
    Values: pct_prazo_consumido, pct_escopo_concluido, pct_aprovado_cliente
  - If data.bloco_a.desvio_detectado (per D-05): show an orange alert badge below the metrics:
    div with display flex, alignItems center, gap 6, marginTop 10, padding "8px 12px", background "#fff7ed", borderRadius 8, border "1px solid #fed7aa"
    Icon span "⚠" color "#c2410c" fontSize 14
    Text span "Desvio de {data.bloco_a.desvio_pontos} pts acima da tolerancia" fontSize 12 color "#c2410c"
    Per D-05: this is visual only, no DB write, the bool comes from the backend response

BLOCO B — "Itens Travados" (per D-11, D-12, CONTEXT.md specifics section):

Card div with cardStyle. Title "ITENS TRAVADOS".

Three sub-sections (per CONTEXT.md specifics: 3 visual sub-sections):

1. "Travadas" sub-section: label with count badge. If travadas.length === 0, show "Nenhuma funcionalidade travada" in muted gray. Else show a list: each item as a row with titulo (fontWeight 600, fontSize 13) and dias badge (background "#fee2e2", color "#dc2626", borderRadius 4, padding "2px 6px", fontSize 11) showing "{dias} dias".

2. "Aguardando cliente" sub-section: same pattern. Badge shows "{dias_uteis} d.u." in yellow tones (background "#fef9c3", color "#a16207").

3. "Em ajuste" sub-section: list of titulo names, badge shows count in orange (background "#ffedd5", color "#c2410c"). If empty, show "Nenhuma em ajuste" in muted gray.

Sub-section separator: a thin horizontal rule (border none, borderTop "1px solid #f0f0f4", margin "10px 0") between the three sub-sections.

BLOCO C — "Metricas de Fluxo" (per D-13, D-14, D-15):

Card div with cardStyle. Title "METRICAS DE FLUXO".

Four metric rows in a flex column with gap 10:
- "WIP agora": wip value (large bold), sub-label "funcionalidades em andamento ou em ajuste"
- "Throughput": (throughput_por_semana ?? "—") + " / semana" in a sub-span (fontSize 12, color "#6a6a7a")
- "Cycle time (p50)": (cycle_time_p50_dias ?? "—") + " dias"
- "Cycle time (p85)": (cycle_time_p85_dias ?? "—") + " dias"

If total_concluidas === 0: show a muted note "Nenhuma funcionalidade concluida — cycle time indisponivel" in place of the cycle time rows.

BLOCO D — "Tempo por Fase" with accordion (per D-04):

Card div with cardStyle. Title "TEMPO POR FASE".

Show eficiencia_fluxo_pct ?? "—" % as a metric with label "Eficiencia de fluxo".

Then iterate Object.entries(data.bloco_d.fases_resumo). For each [fase, resumo]:
  Row div (display flex, justifyContent space-between, alignItems center, cursor pointer, padding "8px 0", borderBottom "1px solid #f0f0f4"):
    Left: fase name (fontWeight 600, fontSize 13, color "#111116")
    Center: "{resumo.media_dias} dias (media)" in fontSize 12 color "#6a6a7a"
    Right: "{resumo.p85_dias ?? '—'} p85" + accordion toggle button

  Accordion toggle: a button (background none, border none, cursor pointer, fontSize 12, color "#9696a0") with text expandedFase === fase ? "▲ fechar" : "▼ ver detalhe". Clicking it calls setExpandedFase(expandedFase === fase ? null : fase).

  If expandedFase === fase: render an expansion div below that row (background "#f7f7fa", borderRadius 8, padding "10px 12px", marginTop 4). Inside, list entries from data.bloco_d.detalhe_por_funcionalidade that have a non-zero value for that fase key in tempos_por_fase:
    Each as a line: span with titulo (fontSize 12, color "#374151") + span with "{tempos_por_fase[fase]} dias" (fontSize 12, color "#6a6a7a", marginLeft 8)
    If none, show "Sem dados de duracao para esta fase" in muted gray.

If Object.keys(fases_resumo).length === 0: show "Sem transicoes de status registradas ainda" in muted gray.
  </action>

  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0 — no TypeScript errors
    - grep -c "className=" /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/components/PainelTab.tsx returns 0 (no Tailwind classes)
    - PainelTab.tsx contains the string "Sem dados de contrato" for the D-03 empty state branch
    - PainelTab.tsx contains "Desvio" text and checks "desvio_detectado" for the D-05 alert branch
    - PainelTab.tsx contains "Travadas" and "Aguardando cliente" and "Em ajuste" as sub-section labels for Bloco B
    - PainelTab.tsx contains "expandedFase" state and "ver detalhe" toggle text for D-04 accordion
    - PainelTab.tsx contains "onNavigateToConfig" prop and it is called when the sem_dados button is clicked
    - page.tsx passes onNavigateToConfig prop to PainelTab (grep finds onNavigateToConfig in page.tsx)
  </acceptance_criteria>

  <done>All 4 blocks render their full polished UI including the Bloco D accordion and the Bloco A sem_dados placeholder with config link.</done>
</task>

<task type="auto">
  <name>Expand Kanban section — sprint dropdown, 3 columns, funcionalidade cards with badge</name>

  <read_first>
    - docudata-frontend/app/components/PainelTab.tsx — full file after previous task completes (understand current kanban tracer section to replace it)
    - docudata-frontend/app/components/TechnologiesTab.tsx — lines 26-37 (chipStyle pattern for status badges)
    - docudata-frontend/app/lib/api.ts — lines containing SprintWithStatus interface definition (check the shape of sprints.numero field)
  </read_first>

  <action>
Replace the kanban section in PainelTab.tsx (the section below the 2x2 grid) with the full polished kanban implementation. The kanban renders below the 4-block grid as a full-width section (per D-02).

Do not use Tailwind. All styles inline.

SPRINT SELECTOR — per D-06:

A section div with cardStyle (same as the block cards, or slightly lighter background "#f7f7fa"). Inside:
- Row with display flex, alignItems center, justifyContent space-between, marginBottom 16
- Left: h3 "Kanban de Sprint" (fontSize 16, fontWeight 700, color "#111116", margin 0)
- Right: a select element:
  - value={String(sprintSelecionada)}
  - onChange: e => setSprintSelecionada(Number(e.target.value))
  - style: border "1px solid #e4e4ea", borderRadius 8, padding "6px 12px", fontSize 13, background "#ffffff", color "#111116"
  - options: sprints sorted descending by numero, each option with value={String(s.numero)} and label "Sprint {s.numero}"
  - If sprints.length === 0: a disabled option "Sem sprints"

Empty state: if sprints.length === 0, show a muted paragraph "Nenhuma sprint cadastrada." below the selector.

If sprints.length > 0, compute:
  funcsDaSprint = funcionalidades.filter(f => f.sprint_alvo !== null && f.sprint_alvo !== undefined && f.sprint_alvo === String(sprintSelecionada))

  planejado = funcsDaSprint.filter(f => f.status === "nao_iniciada")
  emAndamento = funcsDaSprint.filter(f => f.status === "em_andamento" || f.status === "em_ajuste")
  concluido = funcsDaSprint.filter(f => f.status === "concluida")

THREE COLUMN GRID — per D-07:

div with display grid, gridTemplateColumns "1fr 1fr 1fr", gap 12, marginTop 16.

Column 1 — "Planejado" (nao_iniciada):
  Column header: div with display flex, alignItems center, gap 8, marginBottom 12.
    span "Planejado" (fontSize 13, fontWeight 700, color "#374151")
    span with planejado.length count (chipBaseStyle + background "#f1f5f9", color "#64748b")

Column 2 — "Em andamento" (em_andamento + em_ajuste per D-07):
  Header: "Em andamento" in amber tones (color "#a16207"), badge in background "#fef9c3", color "#a16207"

Column 3 — "Concluido" (concluida):
  Header: "Concluido" in green tones (color "#16a34a"), badge in background "#dcfce7", color "#16a34a"

FUNCIONALIDADE CARD:

Each card: div with background "#ffffff", border "1px solid #e8e8ed", borderRadius 10, padding "12px 14px", marginBottom 8, display flex, flexDirection column, gap 6.

Card contents:
1. titulo: span (fontSize 13, fontWeight 600, color "#111116", lineHeight 1.4)
2. Bottom row: display flex, gap 6, flexWrap wrap, alignItems center
   - Prioridade chip: small chip with text prioridade value; colors: alta→background "#fee2e2" color "#dc2626", media→background "#fef9c3" color "#a16207", baixa→background "#f1f5f9" color "#64748b". Apply chipBaseStyle + overrides.
   - Sprint badge (per D-08): if f.sprint_alvo exists, a chip with text "Sprint {f.sprint_alvo}" — background "#ede9fe", color "#7c3aed", chipBaseStyle. Per RESEARCH.md A5: sprint_alvo is a single value; this chip shows that single value. Per D-08: "chip simples — sprint_alvo e campo unico".

Empty column state: if a column list is empty, a placeholder div (padding "20px 0", textAlign center) with text "Nenhuma funcionalidade" in color "#b8b8c0", fontSize 12.

STATUS COLOR for column header badge when item is em_ajuste (same column as em_andamento per D-07): the "Em andamento" column merges both; a note below the header like "inclui em ajuste" in fontSize 11 color "#9696a0" clarifies the grouping.
  </action>

  <verify>
    <automated>cd /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit exits 0
    - grep -c "className=" /Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend/app/components/PainelTab.tsx returns 0
    - PainelTab.tsx contains "Planejado" and "Em andamento" and "Concluido" as column header strings
    - PainelTab.tsx contains the sprint_alvo filter: the string "sprint_alvo === String(sprintSelecionada)" appears in the file
    - PainelTab.tsx contains "Sprint {" for the sprint badge rendering (D-08 chip)
    - PainelTab.tsx contains "nao_iniciada" in the planejado filter condition
    - The select element uses sprintSelecionada state (grep "sprintSelecionada" appears multiple times: initial useState, select value, and onChange)
    - PainelTab.tsx line count is greater than 150 lines (full implementation, not stub)
  </acceptance_criteria>

  <done>Kanban section is fully rendered with sprint dropdown defaulting to most recent sprint, 3 columns correctly filtered, and each funcionalidade card showing titulo, prioridade chip, and sprint badge. TypeScript compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend JSON → Frontend render | PainelData from API is displayed directly — no user-input re-ingestion |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-08-04 | Information Disclosure | PainelTab renders titulo of funcionalidades | low | accept | Data is scoped to the authenticated project; no cross-project mixing; read-only display |
| T-08-05 | Spoofing | sprint_alvo chip displays raw value from DB | low | accept | Value is stored by the backend router, not from user text input in this component — display is safe |
| T-08-SC | Tampering | npm/pip installs | low | accept | No new packages in this plan — all features use React useState/useEffect and inline styles already in use |
</threat_model>

<verification>
Manual verification checklist (run after npm run dev in docudata-frontend):

1. Navigate to a project with no data_inicio/data_fim_contratada — Bloco A shows grey placeholder "Sem dados de contrato" and a button that switches to Configuracoes tab when clicked.

2. Navigate to a project with contract data set — Bloco A shows three percentages. If % prazo - % aprovado > tolerancia, an orange "Desvio detectado" badge appears.

3. Bloco B shows three sub-sections. Projects with funcionalidades in em_andamento for >7 days show them in "Travadas".

4. Bloco C shows "—" for cycle time if no funcionalidade is concluida.

5. Click "ver detalhe" on a Bloco D phase row — it expands to show funcionalidade-level times. Clicking again collapses it. Only one phase is expanded at a time.

6. Kanban dropdown defaults to the highest sprint number. Changing it updates the three columns immediately (no reload).

7. Each kanban card shows titulo, colored prioridade chip, and sprint_alvo chip (if set).

8. Funcionalidades with sprint_alvo = null do not appear in any kanban column.
</verification>

<success_criteria>
- TypeScript build passes with no errors after all tasks in this plan
- No className attributes in PainelTab.tsx
- All 5 CONTEXT.md visual decisions (D-02 grid, D-03 placeholder, D-04 accordion, D-05 desvio badge, D-07 3 columns) are visually present
- All 4 kanban decisions (D-06 dropdown, D-07 columns, D-08 badge, D-09 filter) are implemented
- Manual visual check passes for all 8 items in the verification section
</success_criteria>

<output>
Create `.planning/phases/08-painel-do-gerente-kanban-de-sprint/08-02-SUMMARY.md` when done
</output>
