---
phase: 04-template-v2-github-integration
plan: "04-02"
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-frontend/app/components/SprintDocModal.tsx
  - docudata-frontend/app/components/RetroModal.tsx
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: false
requirements: [FORM-01, FORM-02]

must_haves:
  truths:
    - "SprintDocModal planning section tem campos Squad, Período início/fim, Horas disponíveis, Horas estimadas, Dependências do cliente, Carry-over (textarea pré-preenchida com dados da sprint anterior)"
    - "SprintDocModal review section tem campos Percepção do cliente, Sinal de satisfação (select com 3 opções), Pedidos fora do escopo"
    - "RetroModal tem campo Pedido fora de escopo — status (textarea)"
    - "submitPlanning em api.ts envia squad, periodo_inicio, periodo_fim, horas_disponiveis, horas_estimadas, dependencias_cliente, carry_over como Form fields"
    - "submitReview em api.ts envia percepcao_cliente, sinal_satisfacao, pedidos_fora_escopo como Form fields"
    - "Ao abrir modal de Planning para sprint N, fetch da ingestion de review da sprint N-1 pré-preenche carry_over com itens_proxima_sprint"
  artifacts:
    - path: "docudata-frontend/app/components/SprintDocModal.tsx"
      provides: "Modal atualizado com 9 novos campos distribuídos em planning e review"
      contains: "squad"
    - path: "docudata-frontend/app/components/RetroModal.tsx"
      provides: "RetroModal com campo pedido_fora_escopo_status"
      contains: "pedido_fora_escopo_status"
    - path: "docudata-frontend/app/lib/api.ts"
      provides: "submitPlanning e submitReview com novos campos; submitRetrospectiva novo"
      contains: "squad"
---

<objective>
Atualizar os modais do frontend para coletar os novos campos estruturados dos templates CITi v2. Planning ganha 7 novos campos (Squad, Período, Horas, Dependências, Carry-over pré-preenchido). Review ganha 3 campos (Percepção do cliente, Sinal de satisfação dropdown, Pedidos fora do escopo). RetroModal ganha 1 campo (Pedido fora de escopo — status). As funções em api.ts são atualizadas para enviar esses campos via FormData.

Este plano é independente do 04-01 (backend) — pode rodar em paralelo. O frontend enviará os novos campos e o backend os armazenará.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar submitPlanning e submitReview em api.ts com novos campos</name>
  <files>docudata-frontend/app/lib/api.ts</files>
  <read_first>
    - docudata-frontend/app/lib/api.ts (funções submitPlanning linhas 268-283 e submitReview linhas 304-315 — tipos e FormData atuais)
  </read_first>
  <action>
    Em `submitPlanning`, adicionar ao tipo de input os campos:
    `squad?: string; periodoInicio?: string; periodoFim?: string; horasDisponiveis?: number; horasEstimadas?: number; dependenciasCliente?: string; carryOver?: string;`
    No corpo da função, adicionar ao FormData (somente se o valor for definido/não-vazio):
    ```typescript
    if (input.squad) form.append("squad", input.squad);
    if (input.periodoInicio) form.append("periodo_inicio", input.periodoInicio);
    if (input.periodoFim) form.append("periodo_fim", input.periodoFim);
    if (input.horasDisponiveis != null) form.append("horas_disponiveis", String(input.horasDisponiveis));
    if (input.horasEstimadas != null) form.append("horas_estimadas", String(input.horasEstimadas));
    if (input.dependenciasCliente) form.append("dependencias_cliente", input.dependenciasCliente);
    if (input.carryOver) form.append("carry_over", input.carryOver);
    ```

    Em `submitReview`, adicionar ao tipo de input:
    `percepcaoCliente?: string; sinalSatisfacao?: string; pedidosForaEscopo?: string;`
    No FormData:
    ```typescript
    if (input.percepcaoCliente) form.append("percepcao_cliente", input.percepcaoCliente);
    if (input.sinalSatisfacao) form.append("sinal_satisfacao", input.sinalSatisfacao);
    if (input.pedidosForaEscopo) form.append("pedidos_fora_escopo", input.pedidosForaEscopo);
    ```

    Adicionar nova função `submitRetrospectiva` abaixo de submitReview:
    ```typescript
    export async function submitRetrospectiva(input: {
      projetoId: string;
      sprintNumero: number;
      observacoes?: string;
      pedidoForaEscopoStatus?: string;
      anexo?: File | null;
    }): Promise<SprintDocResponse> {
      const form = new FormData();
      form.append("projeto_id", input.projetoId);
      form.append("sprint_numero", String(input.sprintNumero));
      if (input.observacoes) form.append("observacoes", input.observacoes);
      if (input.pedidoForaEscopoStatus) form.append("pedido_fora_escopo_status", input.pedidoForaEscopoStatus);
      if (input.anexo) form.append("anexo", input.anexo);
      return _postSprintDoc("retrospectiva", form);
    }
    ```
  </action>
  <acceptance_criteria>
    - api.ts: `submitPlanning` input type contém `squad?: string`
    - api.ts: `submitPlanning` body contém `form.append("squad"`
    - api.ts: `submitPlanning` body contém `form.append("carry_over"`
    - api.ts: `submitReview` input type contém `sinalSatisfacao?: string`
    - api.ts: `submitReview` body contém `form.append("sinal_satisfacao"`
    - api.ts: função `submitRetrospectiva` existe e chama `_postSprintDoc("retrospectiva", form)`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adicionar novos campos ao SprintDocModal — seção Planning</name>
  <files>docudata-frontend/app/components/SprintDocModal.tsx</files>
  <read_first>
    - docudata-frontend/app/components/SprintDocModal.tsx (planning state linhas ~107-110; planning JSX — seção com descricao + itens; handleSubmit caso "planning" linhas ~148-160; estilos inputStyle, textareaStyle, labelStyle definidos nas linhas ~60-90)
  </read_first>
  <action>
    Adicionar ao state do componente (junto com os outros estados do planning):
    ```typescript
    const [squad, setSquad] = useState("");
    const [periodoInicio, setPeriodoInicio] = useState("");
    const [periodoFim, setPeriodoFim] = useState("");
    const [horasDisponiveis, setHorasDisponiveis] = useState<number | "">("");
    const [horasEstimadas, setHorasEstimadas] = useState<number | "">("");
    const [dependenciasCliente, setDependenciasCliente] = useState("");
    const [carryOver, setCarryOver] = useState("");
    ```

    No `useEffect` de reset (quando `!open`), adicionar reset desses 7 campos para seus valores iniciais.

    No caso `"planning"` do `handleSubmit`, passar os novos campos para `submitPlanning`:
    ```typescript
    squad: squad || undefined,
    periodoInicio: periodoInicio || undefined,
    periodoFim: periodoFim || undefined,
    horasDisponiveis: horasDisponiveis !== "" ? horasDisponiveis : undefined,
    horasEstimadas: horasEstimadas !== "" ? horasEstimadas : undefined,
    dependenciasCliente: dependenciasCliente || undefined,
    carryOver: carryOver || undefined,
    ```

    No JSX da seção `{tipo === "planning"}`, adicionar após o bloco de "Itens do backlog":

    1. **Squad** — `<label style={labelStyle}>Squad</label><input style={inputStyle} value={squad} onChange={e => setSquad(e.target.value)} placeholder="Ex: Backend, Data, Full-stack" />`

    2. **Período** — linha horizontal com dois date inputs lado a lado:
    `<label style={labelStyle}>Período da sprint</label>`
    `<div style={{ display: "flex", gap: 8 }}>`
    `<input type="date" style={{...inputStyle, flex:1}} value={periodoInicio} onChange={e => setPeriodoInicio(e.target.value)} />`
    `<input type="date" style={{...inputStyle, flex:1}} value={periodoFim} onChange={e => setPeriodoFim(e.target.value)} />`
    `</div>`

    3. **Horas** — linha horizontal com dois number inputs:
    `<label style={labelStyle}>Capacidade vs. Estimativa</label>`
    `<div style={{ display: "flex", gap: 8 }}>`
    `<input type="number" min={0} style={{...inputStyle, flex:1}} value={horasDisponiveis} onChange={e => setHorasDisponiveis(e.target.value ? Number(e.target.value) : "")} placeholder="Horas disponíveis" />`
    `<input type="number" min={0} style={{...inputStyle, flex:1}} value={horasEstimadas} onChange={e => setHorasEstimadas(e.target.value ? Number(e.target.value) : "")} placeholder="Horas estimadas" />`
    `</div>`

    4. **Dependências do cliente** — `<label style={labelStyle}>Dependências do cliente</label><textarea style={textareaStyle} value={dependenciasCliente} onChange={e => setDependenciasCliente(e.target.value)} placeholder="Ex: Acesso ao banco de produção, aprovação do layout" />`

    5. **Carry-over** — `<label style={labelStyle}>Carry-over da sprint anterior</label><textarea style={{...textareaStyle, minHeight: 60}} value={carryOver} onChange={e => setCarryOver(e.target.value)} placeholder="Itens não entregues na sprint anterior (pré-preenchido automaticamente)" />`
  </action>
  <acceptance_criteria>
    - SprintDocModal.tsx state contém `const [squad, setSquad] = useState("")`
    - SprintDocModal.tsx state contém `const [carryOver, setCarryOver] = useState("")`
    - SprintDocModal.tsx JSX da seção planning contém input com placeholder "Horas disponíveis"
    - SprintDocModal.tsx handleSubmit caso "planning" passa `squad` e `carryOver` para submitPlanning
    - useEffect de reset inclui `setSquad("")` e `setCarryOver("")`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Adicionar campos de Review ao SprintDocModal e campo de status ao RetroModal</name>
  <files>docudata-frontend/app/components/SprintDocModal.tsx, docudata-frontend/app/components/RetroModal.tsx</files>
  <read_first>
    - docudata-frontend/app/components/SprintDocModal.tsx (review state — `const [observacoes, setObservacoes]`; JSX review section; handleSubmit caso "review"; useEffect de reset)
    - docudata-frontend/app/components/RetroModal.tsx (Props interface; state de observacoes e submitting; handleSubmit que chama onSubmit(observacoes, file); JSX do textarea de observacoes)
  </read_first>
  <action>
    **SprintDocModal.tsx — seção Review:**

    Adicionar ao state (junto com os outros estados do review):
    ```typescript
    const [percepcaoCliente, setPercepcaoCliente] = useState("");
    const [sinalSatisfacao, setSinalSatisfacao] = useState("");
    const [pedidosForaEscopo, setPedidosForaEscopo] = useState("");
    ```

    No useEffect de reset, resetar os 3 novos campos para `""`.

    No caso `"review"` do `handleSubmit`, adicionar:
    ```typescript
    percepcaoCliente: percepcaoCliente || undefined,
    sinalSatisfacao: sinalSatisfacao || undefined,
    pedidosForaEscopo: pedidosForaEscopo || undefined,
    ```

    No JSX `{tipo === "review"}`, após observações, adicionar:
    1. `<label style={labelStyle}>Percepção do cliente</label><textarea style={textareaStyle} value={percepcaoCliente} onChange={e => setPercepcaoCliente(e.target.value)} placeholder="Ex: Cliente satisfeito com a velocidade de entrega" />`
    2. Sinal de satisfação select (3 opções padrão CITi):
    ```tsx
    <label style={labelStyle}>Sinal de satisfação</label>
    <select style={{...inputStyle, cursor: "pointer"}} value={sinalSatisfacao} onChange={e => setSinalSatisfacao(e.target.value)}>
      <option value="">Selecione...</option>
      <option value="🟢 Verde">🟢 Verde</option>
      <option value="🟡 Amarelo">🟡 Amarelo</option>
      <option value="🔴 Vermelho">🔴 Vermelho</option>
    </select>
    ```
    3. `<label style={labelStyle}>Pedidos fora do escopo</label><textarea style={textareaStyle} value={pedidosForaEscopo} onChange={e => setPedidosForaEscopo(e.target.value)} placeholder="Ex: Cliente solicitou relatório PDF — registrado para avaliação" />`

    **RetroModal.tsx:**

    Atualizar Props: `onSubmit: (observacoes: string, pedidoForaEscopoStatus: string, file: File | null) => Promise<void>`

    Adicionar state: `const [pedidoForaEscopoStatus, setPedidoForaEscopoStatus] = useState("")`

    No useEffect de reset, adicionar `setPedidoForaEscopoStatus("")`.

    Em `handleSubmit`, alterar chamada para `onSubmit(observacoes, pedidoForaEscopoStatus, file)`.

    No JSX após textarea de observacoes:
    ```tsx
    <label style={labelStyle}>Pedido fora de escopo — status</label>
    <textarea style={textareaStyle} value={pedidoForaEscopoStatus}
      onChange={(e) => setPedidoForaEscopoStatus(e.target.value)}
      placeholder="Ex: Solicitação de relatório PDF — aceita para Sprint 4" />
    ```
  </action>
  <acceptance_criteria>
    - SprintDocModal.tsx state contém `const [sinalSatisfacao, setSinalSatisfacao] = useState("")`
    - SprintDocModal.tsx JSX review section contém `<option value="🟢 Verde">` e `<option value="🔴 Vermelho">`
    - SprintDocModal.tsx handleSubmit caso "review" passa `sinalSatisfacao` para submitReview
    - SprintDocModal.tsx useEffect de reset inclui `setSinalSatisfacao("")`
    - RetroModal.tsx Props.onSubmit tem 3 parâmetros: `(observacoes, pedidoForaEscopoStatus, file)`
    - RetroModal.tsx state contém `const [pedidoForaEscopoStatus, setPedidoForaEscopoStatus] = useState("")`
    - RetroModal.tsx JSX contém textarea com placeholder "Pedido fora de escopo — status"
    - RetroModal.tsx handleSubmit chama `onSubmit(observacoes, pedidoForaEscopoStatus, file)` com 3 argumentos
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Atualizar page.tsx — carry-over pre-fill e handleRetroSubmit</name>
  <files>docudata-frontend/app/projects/[id]/page.tsx</files>
  <read_first>
    - docudata-frontend/app/projects/[id]/page.tsx (handleRetroSubmit linhas ~225-232; RetroModal props linhas ~751-757; setModal para SprintDocModal; imports no topo)
    - docudata-frontend/app/lib/api.ts (listIngestionsBySprint — verificar se existe, ou usar listIngestions)
  </read_first>
  <action>
    **1. Atualizar handleRetroSubmit para usar submitRetrospectiva:**

    Substituir o body de `handleRetroSubmit` (atualmente chama `generateDoc`) para chamar `submitRetrospectiva` de api.ts:
    ```typescript
    async function handleRetroSubmit(observacoes: string, pedidoForaEscopoStatus: string, file: File | null) {
      if (!retroModal) return;
      try {
        const doc = await submitRetrospectiva({
          projetoId: id,
          sprintNumero: retroModal.sprintNumero,
          observacoes: observacoes || undefined,
          pedidoForaEscopoStatus: pedidoForaEscopoStatus || undefined,
          anexo: file,
        });
        // Atualizar lista de docs como o restante dos sprint docs handlers faz
        setDocs((prev) => [doc as unknown as GeneratedDoc, ...prev]);
        setRetroModal(null);
      } catch (e) {
        console.error("Erro ao submeter retrospectiva:", e);
      }
    }
    ```
    Adicionar `submitRetrospectiva` ao import de api.ts no topo do arquivo.

    **2. Carry-over pre-fill ao abrir modal de Planning:**

    Adicionar state: `const [carryOverPrefill, setCarryOverPrefill] = useState("")`

    Criar função `fetchCarryOver(sprintNumero: number)`:
    ```typescript
    async function fetchCarryOver(sprintNumero: number) {
      if (sprintNumero <= 1) { setCarryOverPrefill(""); return; }
      try {
        const ingestions = await listIngestionsBySprint(id, sprintNumero - 1);
        const review = ingestions.find((ing: any) => ing.tipo_documentacao === "review");
        const items: string[] = review?.extracted_content?.campos_review?.itens_proxima_sprint
          || review?.extracted_content?.proximos_passos
          || [];
        setCarryOverPrefill(Array.isArray(items) ? items.join("\n") : String(items));
      } catch {
        setCarryOverPrefill("");
      }
    }
    ```

    Chamar `fetchCarryOver(n)` ao abrir o modal de planning — no handler `onOpenSprintDoc` quando `tipo === "planning"`:
    ```typescript
    onOpenSprintDoc={(tipo, n) => {
      setModal({ tipo, sprintNumero: n });
      if (tipo === "planning") fetchCarryOver(n);
    }}
    ```

    Passar `carryOverPrefill` como prop para `SprintDocModal` e usá-lo como valor inicial do state `carryOver` via `useEffect` no modal (ou como prop `initialCarryOver`). A abordagem mais simples: adicionar prop `initialCarryOver?: string` ao SprintDocModal e usar em `useEffect([open])` para `setCarryOver(initialCarryOver || "")`.

    Verificar se `listIngestionsBySprint` existe em api.ts — se não existir, adicionar:
    ```typescript
    export async function listIngestionsBySprint(projetoId: string, sprint: number) {
      const res = await fetch(`${API}/ingestions/${projetoId}/${sprint}`);
      return res.json();
    }
    ```
  </action>
  <acceptance_criteria>
    - page.tsx importa `submitRetrospectiva` de `../../lib/api`
    - page.tsx: `handleRetroSubmit` chama `submitRetrospectiva` (não `generateDoc`) com os 3 parâmetros
    - page.tsx: state `carryOverPrefill` existe
    - page.tsx: função `fetchCarryOver` existe e faz fetch de ingestions da sprint anterior
    - page.tsx: `onOpenSprintDoc` chama `fetchCarryOver(n)` quando `tipo === "planning"`
    - SprintDocModal recebe `initialCarryOver` prop (ou equivalente) e usa no useEffect de reset
    - api.ts exporta `listIngestionsBySprint` (criar se não existir)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- SprintDocModal planning: Squad, Período (2 date inputs), Horas disponíveis/estimadas (2 number inputs), Dependências do cliente, Carry-over (textarea pré-preenchida) visíveis no formulário
- SprintDocModal review: Percepção do cliente, Sinal de satisfação (select com 🟢/🟡/🔴), Pedidos fora do escopo visíveis
- RetroModal: campo "Pedido fora de escopo — status" visível e enviado ao backend
- Carry-over pre-fill: abrir modal de Planning para Sprint 2 com Review da Sprint 1 existente pré-preenche o campo
- Abrir Sprint 1 Planning: carry-over vazio (sprint anterior inexistente)
</verification>

<success_criteria>
- FORM-01: Modal de Planning coleta Squad, Período, Horas, Dependências, Carry-over no frontend
- FORM-02: Modal de Review coleta Percepção do cliente, Sinal de satisfação (dropdown), Pedidos fora do escopo no frontend
</success_criteria>
