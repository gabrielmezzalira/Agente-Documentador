---
phase: 04-template-v2-github-integration
plan: "04-04"
type: execute
wave: 2
depends_on: [04-01-PLAN.md]
files_modified:
  - docudata-backend/routers/commit_ingest.py
  - docudata-backend/main.py
  - docudata-backend/hooks/docudata_agent.py
  - docudata-backend/hooks/docudata.yml
autonomous: false
requirements: [GH-01, GH-02]

must_haves:
  truths:
    - "GET /projects/{project_id}/current-sprint retorna sprint_number baseado na última ingestion de tipo 'planning' do projeto"
    - "POST /ingest/commit aceita sprint_number vindo do agente (não hardcoded)"
    - "docudata_agent.py detecta sprint via GET /current-sprint e respeita override [sprint:N] na mensagem do commit"
    - "docudata_agent.py escreve GitHub Commit Status com 'DocuData — Sprint N' após registrar o commit"
    - "docudata.yml dispara em push de qualquer branch ('**') e roda docudata_agent.py"
    - "main.py registra o router de commit_ingest"
  artifacts:
    - path: "docudata-backend/routers/commit_ingest.py"
      provides: "GET /projects/{id}/current-sprint + POST /ingest/commit"
      contains: "current-sprint"
    - path: "docudata-backend/hooks/docudata_agent.py"
      provides: "Script Python para GitHub Actions — detecção de sprint, coleta de diff, POST, commit status"
      contains: "DOCUDATA_PROJECT_ID"
    - path: "docudata-backend/hooks/docudata.yml"
      provides: "GitHub Actions workflow — dispara em todo push, roda docudata_agent.py"
      contains: "branches: ['**']"
---

<objective>
Implementar a integração com GitHub via GitHub Actions + Python agent. Em vez de um hook bash local, o gerente copia dois arquivos para o repositório do projeto: `scripts/docudata_agent.py` e `.github/workflows/docudata.yml`. A partir daí, todo commit em qualquer branch dispara automaticamente o agent, que detecta a sprint atual via DocuData (baseada na última Planning ingerida), verifica se há override `[sprint:N]` na mensagem do commit, envia o diff real para `POST /ingest/commit`, e escreve um GitHub Commit Status visível no GitHub ("✓ DocuData — Sprint 3").

Depende do Plan 04-01 para que `tipo_documentacao='commit'` seja válido na CHECK constraint.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar GET /projects/{project_id}/current-sprint e atualizar POST /ingest/commit</name>
  <files>docudata-backend/routers/commit_ingest.py, docudata-backend/main.py</files>
  <read_first>
    - docudata-backend/routers/commit_ingest.py (estrutura atual — CommitPayload, endpoint POST /ingest/commit)
    - docudata-backend/routers/projects.py (padrão de router e como busca projetos no Supabase)
    - docudata-backend/services/supabase_client.py (get_client)
    - docudata-backend/main.py (imports e include_router existentes)
  </read_first>
  <action>
    **1. Novo endpoint GET /projects/{project_id}/current-sprint em commit_ingest.py:**

    ```python
    @router.get("/projects/{project_id}/current-sprint")
    async def get_current_sprint(project_id: str):
        client = get_client()
        # Verifica que projeto existe
        proj = client.table("projects").select("id").eq("id", project_id).execute()
        if not proj.data:
            raise HTTPException(status_code=404, detail="Project not found")
        # Busca última ingestion de planning
        resp = (
            client.table("ingestions")
            .select("sprint_number, created_at")
            .eq("project_id", project_id)
            .eq("tipo_documentacao", "planning")
            .order("created_at", desc=True)
            .limit(1)
            .execute()
        )
        sprint_number = resp.data[0]["sprint_number"] if resp.data else 1
        started_at = resp.data[0]["created_at"] if resp.data else None
        return {"sprint_number": sprint_number, "started_at": started_at}
    ```

    **2. Adicionar `diff` ao CommitPayload:**

    Na classe `CommitPayload`, adicionar campo:
    `diff: Optional[str] = None` — diff real dos arquivos modificados (truncado em 8000 chars pelo agente)

    Atualizar o prompt do Gemini no endpoint POST /ingest/commit para incluir o diff quando disponível:
    ```
    Diff das mudanças (arquivos modificados):
    {payload.diff or 'não informado'}
    ```

    **3. Registrar router em main.py:**

    Adicionar (se ainda não existir do plano original):
    ```python
    from routers import commit_ingest
    app.include_router(commit_ingest.router)
    ```
  </action>
  <acceptance_criteria>
    - `GET /projects/{project_id}/current-sprint` retorna `{"sprint_number": N, "started_at": "..."}` quando há planning ingestion
    - `GET /projects/{project_id}/current-sprint` retorna `{"sprint_number": 1, "started_at": null}` quando não há planning ingestion
    - `GET /projects/inexistente/current-sprint` retorna 404
    - `CommitPayload` tem campo `diff: Optional[str] = None`
    - O prompt do Gemini em POST /ingest/commit inclui o diff quando `payload.diff` não é None
    - `main.py` contém `app.include_router(commit_ingest.router)`
    - `GET /openapi.json` lista as rotas `/projects/{project_id}/current-sprint` e `/ingest/commit`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Criar docudata_agent.py — detecção de sprint, override, diff, POST, commit status</name>
  <files>docudata-backend/hooks/docudata_agent.py</files>
  <read_first>
    - docudata-backend/routers/commit_ingest.py (CommitPayload — campos esperados pelo endpoint)
    - docudata-backend/.env.example (variáveis de ambiente do projeto)
  </read_first>
  <action>
    Criar `docudata-backend/hooks/docudata_agent.py`:

    ```python
    #!/usr/bin/env python3
    """
    DocuData Commit Agent
    Instalação no repositório do projeto:
      cp <caminho>/docudata_agent.py scripts/docudata_agent.py
    Variáveis de ambiente (GitHub Secrets):
      DOCUDATA_API_URL      — ex: https://docudata-backend.railway.app
      DOCUDATA_PROJECT_ID   — UUID do projeto no DocuData
      GITHUB_TOKEN          — fornecido automaticamente pelo GitHub Actions
      GITHUB_REPOSITORY     — fornecido automaticamente (owner/repo)
      GITHUB_SHA            — fornecido automaticamente (hash do commit)
    """
    import os, subprocess, re, json
    import urllib.request, urllib.error

    API_URL    = os.environ["DOCUDATA_API_URL"].rstrip("/")
    PROJECT_ID = os.environ["DOCUDATA_PROJECT_ID"]
    GH_TOKEN   = os.environ.get("GITHUB_TOKEN", "")
    GH_REPO    = os.environ.get("GITHUB_REPOSITORY", "")
    GH_SHA     = os.environ.get("GITHUB_SHA", "")

    def git(*args):
        return subprocess.run(["git"] + list(args), capture_output=True, text=True).stdout.strip()

    def http_json(url, method="GET", data=None, token=None):
        body = json.dumps(data).encode() if data else None
        headers = {"Content-Type": "application/json"}
        if token:
            headers["Authorization"] = f"token {token}"
        req = urllib.request.Request(url, data=body, headers=headers, method=method)
        try:
            with urllib.request.urlopen(req, timeout=15) as r:
                return json.loads(r.read()), r.status
        except urllib.error.HTTPError as e:
            return {}, e.code

    # 1. Detectar sprint via DocuData (última planning ingerida)
    sprint_data, status = http_json(f"{API_URL}/projects/{PROJECT_ID}/current-sprint")
    sprint_number = sprint_data.get("sprint_number", 1) if status == 200 else 1

    # 2. Verificar override [sprint:N] na mensagem do commit
    commit_msg = git("log", "-1", "--pretty=%s")
    override = re.search(r'\[sprint:(\d+)\]', commit_msg)
    if override:
        sprint_number = int(override.group(1))
        print(f"[docudata] Sprint override detectado: Sprint {sprint_number}")
    else:
        print(f"[docudata] Sprint detectada automaticamente: Sprint {sprint_number}")

    # 3. Coletar metadados do commit
    commit_hash  = git("log", "-1", "--pretty=%H")
    author       = git("log", "-1", "--pretty=%an")
    date         = git("log", "-1", "--pretty=%aI")
    diff_stat    = git("diff", "HEAD~1", "HEAD", "--stat")
    # Diff real truncado em 8000 chars para não estourar o contexto do Gemini
    diff_full    = git("show", "HEAD", "--unified=3")[:8000]

    # 4. Enviar para DocuData
    payload = {
        "project_id":    PROJECT_ID,
        "sprint_number": sprint_number,
        "commit_hash":   commit_hash,
        "commit_message": commit_msg,
        "author":        author,
        "date":          date,
        "diff_stat":     diff_stat,
        "diff":          diff_full,
    }
    _, post_status = http_json(f"{API_URL}/ingest/commit", method="POST", data=payload)

    # 5. Escrever GitHub Commit Status
    if GH_TOKEN and GH_REPO and GH_SHA:
        state   = "success" if post_status == 201 else "failure"
        desc    = f"DocuData — Sprint {sprint_number}" if post_status == 201 else "DocuData — falha ao registrar"
        status_payload = {
            "state":       state,
            "description": desc,
            "context":     "DocuData",
        }
        http_json(
            f"https://api.github.com/repos/{GH_REPO}/statuses/{GH_SHA}",
            method="POST", data=status_payload, token=GH_TOKEN,
        )
        print(f"[docudata] Commit Status escrito: {state} — {desc}")

    if post_status == 201:
        print(f"[docudata] Commit {commit_hash[:7]} registrado na Sprint {sprint_number}")
    else:
        print(f"[docudata] Aviso: falha ao registrar (HTTP {post_status}) — continuando")
    ```

    O script usa apenas stdlib Python (sem dependências externas) para manter o GitHub Actions leve.
  </action>
  <acceptance_criteria>
    - `docudata_agent.py` existe em `docudata-backend/hooks/`
    - Script chama `GET /projects/{PROJECT_ID}/current-sprint` para detectar sprint
    - Script detecta `[sprint:N]` via regex na mensagem do commit e sobrescreve sprint_number
    - Script envia `diff` truncado em 8000 chars no payload
    - Script chama `POST .../repos/{GITHUB_REPOSITORY}/statuses/{GITHUB_SHA}` com state "success"/"failure"
    - Script usa apenas stdlib Python (sem `import requests`, sem `import anthropic`)
    - Comentário de instalação presente no topo do arquivo
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Criar docudata.yml — GitHub Actions workflow para o repositório do projeto</name>
  <files>docudata-backend/hooks/docudata.yml</files>
  <read_first>
    - docudata-backend/hooks/docudata_agent.py (Task 2 — variáveis de ambiente consumidas pelo script)
  </read_first>
  <action>
    Criar `docudata-backend/hooks/docudata.yml`:

    ```yaml
    # DocuData Commit Tracker — GitHub Actions Workflow
    #
    # Instalação no repositório do projeto:
    #   mkdir -p .github/workflows scripts
    #   cp <caminho>/docudata_agent.py scripts/docudata_agent.py
    #   cp <caminho>/docudata.yml     .github/workflows/docudata.yml
    #   git add .github/workflows/docudata.yml scripts/docudata_agent.py
    #   git commit -m "chore: adicionar DocuData commit tracker"
    #
    # Configurar GitHub Secrets no repositório do projeto:
    #   DOCUDATA_API_URL     — https://docudata-backend.railway.app
    #   DOCUDATA_PROJECT_ID  — UUID do projeto no DocuData
    #
    # Override de sprint na mensagem do commit:
    #   git commit -m "feat: nova tela [sprint:4]"

    name: DocuData Commit Tracker

    on:
      push:
        branches: ['**']   # todo commit em toda branch é registrado

    jobs:
      track:
        runs-on: ubuntu-latest
        permissions:
          statuses: write   # necessário para escrever commit status
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 2   # precisa do commit anterior para o diff

          - name: Registrar commit no DocuData
            env:
              DOCUDATA_API_URL:    ${{ secrets.DOCUDATA_API_URL }}
              DOCUDATA_PROJECT_ID: ${{ secrets.DOCUDATA_PROJECT_ID }}
              GITHUB_TOKEN:        ${{ secrets.GITHUB_TOKEN }}
            run: python scripts/docudata_agent.py
    ```

    `GITHUB_TOKEN`, `GITHUB_REPOSITORY` e `GITHUB_SHA` são fornecidos automaticamente pelo GitHub Actions — o gerente não precisa configurar.
  </action>
  <acceptance_criteria>
    - `docudata.yml` existe em `docudata-backend/hooks/`
    - Workflow dispara em `push` com `branches: ['**']`
    - Job tem `permissions: statuses: write`
    - Step de checkout usa `fetch-depth: 2`
    - `GITHUB_TOKEN` passado como env (não como Secret manual — é automático)
    - Comentário de instalação presente com os 4 passos (mkdir, cp, cp, git add/commit)
    - Comentário documenta os 2 Secrets que o gerente precisa configurar
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verificação manual — instalar no repositório de teste e validar commit status</name>
  <files>docudata-backend/hooks/docudata_agent.py, docudata-backend/hooks/docudata.yml</files>
  <action>Instalar o workflow em um repositório de teste no GitHub e verificar o fluxo completo.</action>
  <what-built>
    GitHub Actions workflow + Python agent que: detecta sprint automaticamente via última Planning ingerida, suporta override [sprint:N], registra o diff real do commit via POST /ingest/commit, escreve GitHub Commit Status visível no GitHub.
  </what-built>
  <how-to-verify>
    1. Criar um repositório de teste no GitHub (ou usar um existente).

    2. Copiar os arquivos:
       ```bash
       mkdir -p .github/workflows scripts
       cp docudata-backend/hooks/docudata_agent.py scripts/docudata_agent.py
       cp docudata-backend/hooks/docudata.yml .github/workflows/docudata.yml
       git add .github/workflows/docudata.yml scripts/docudata_agent.py
       git commit -m "chore: adicionar DocuData commit tracker"
       git push
       ```

    3. No repositório do GitHub → Settings → Secrets → Actions, adicionar:
       - `DOCUDATA_API_URL` = https://docudata-backend.railway.app (ou localhost via ngrok)
       - `DOCUDATA_PROJECT_ID` = UUID de um projeto existente no DocuData

    4. **Testar detecção automática de sprint:**
       - No DocuData, ingerir um Planning para Sprint 2 no projeto de teste.
       - No repositório de teste, fazer um commit qualquer e push:
         ```bash
         git commit --allow-empty -m "test: verificar detecção de sprint"
         git push
         ```
       - No GitHub → commit → verificar que aparece `✓ DocuData — Sprint 2` no status do commit.
       - No DocuData → Histórico → verificar nova ingestion com `tipo_documentacao='commit'` e `sprint_number=2`.

    5. **Testar override [sprint:N]:**
       ```bash
       git commit --allow-empty -m "fix: correcao urgente [sprint:1]"
       git push
       ```
       - Verificar que o status mostra `✓ DocuData — Sprint 1` (não Sprint 2).
       - Verificar no banco que a ingestion foi salva com `sprint_number=1`.

    6. **Testar commit em branch secundária:**
       ```bash
       git checkout -b feat/nova-funcionalidade
       git commit --allow-empty -m "feat: inicio da feature"
       git push origin feat/nova-funcionalidade
       ```
       - Verificar que o workflow rodou também nessa branch (GitHub → Actions).
       - Verificar ingestion no DocuData com `sprint_number` detectado automaticamente.

    7. **Testar sem planning ingerida (fallback Sprint 1):**
       - Criar um projeto novo no DocuData sem nenhuma Planning ingerida.
       - Atualizar `DOCUDATA_PROJECT_ID` no Secret e fazer um push.
       - Verificar que o agente salva com `sprint_number=1`.
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva o erro (ex: status não aparece no GitHub, sprint detectada errada, 404 no current-sprint).</resume-signal>
</task>

</tasks>

<verification>
- `GET /projects/{id}/current-sprint`: retorna sprint_number correto baseado na última planning; retorna 1 quando não há planning; 404 para projeto inexistente
- `POST /ingest/commit`: aceita payload com `diff`, inclui diff no prompt do Gemini, salva ingestion com 6 campos extraídos
- Commit em qualquer branch: GitHub Actions dispara, agent roda, ingestion criada, commit status visível no GitHub
- Override `[sprint:N]`: sprint salva = N, independente da sprint atual do projeto
- Fallback sem planning: sprint_number = 1, sem erro
- Script sem dependências externas: apenas stdlib Python
</verification>

<success_criteria>
- GH-01: Commit em qualquer branch dispara GitHub Actions que registra ingestion automaticamente no DocuData com sprint detectada via última Planning ingerida
- GH-02: POST /ingest/commit salva ingestion com tipo_documentacao=commit, extracted_content com 6 campos extraídos do diff real pelo Gemini
</success_criteria>
