---
phase: 04-template-v2-github-integration
plan: "04-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-backend/supabase_schema.sql
  - docudata-backend/routers/sprint_docs.py
autonomous: false
requirements: [FORM-01, FORM-02]

must_haves:
  truths:
    - "submit_planning recebe squad, periodo_inicio, periodo_fim, horas_disponiveis, horas_estimadas, dependencias_cliente, carry_over via Form e armazena em extracted_content.campos_planning"
    - "submit_review recebe percepcao_cliente, sinal_satisfacao, pedidos_fora_escopo via Form e armazena em extracted_content.campos_review"
    - "POST /sprint-docs/retrospectiva existe, recebe pedido_fora_escopo_status e armazena em extracted_content.campos_retrospectiva"
    - "supabase_schema.sql lista 'retrospectiva' e 'commit' na CHECK constraint de tipo_documentacao"
  artifacts:
    - path: "docudata-backend/routers/sprint_docs.py"
      provides: "campos_planning, campos_review, campos_retrospectiva sub-dicts in extracted_content"
      contains: "campos_planning"
    - path: "docudata-backend/supabase_schema.sql"
      provides: "Constraint atualizada com retrospectiva e commit"
      contains: "'retrospectiva'"
---

<objective>
Adicionar os campos estruturados dos novos templates CITi ao backend: novos Form fields nos endpoints de planning e review (squad, período, horas, dependências, percepção do cliente, sinal de satisfação, pedidos fora do escopo, carry-over), armazenados como sub-dicts `campos_planning` e `campos_review` dentro de `extracted_content` JSONB — seguindo o padrão `campos_daily` já existente. Criar endpoint `/sprint-docs/retrospectiva` com `campos_retrospectiva.pedido_fora_escopo_status`. Atualizar a CHECK constraint no SQL para incluir 'retrospectiva' e 'commit'.

Esses campos são a fonte de verdade que o Plan 04-03 (template export) irá ler.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Atualizar CHECK constraint de tipo_documentacao no supabase_schema.sql</name>
  <files>docudata-backend/supabase_schema.sql</files>
  <read_first>
    - docudata-backend/supabase_schema.sql (linha 21 — constraint atual; linhas 39-41 — ALTER TABLE comentado)
  </read_first>
  <action>
    Na linha 21 de supabase_schema.sql, substituir o CHECK constraint de tipo_documentacao de:
    `CHECK (tipo_documentacao IS NULL OR tipo_documentacao IN ('planning','daily','review','outro'))`
    para:
    `CHECK (tipo_documentacao IS NULL OR tipo_documentacao IN ('planning','daily','review','retrospectiva','commit','outro'))`

    Adicionar ao final do arquivo (após os comentários de ALTER TABLE existentes) um bloco de migration SQL para o banco já em produção:
    ```
    -- Migration v2: adicionar 'retrospectiva' e 'commit' ao tipo_documentacao
    -- ALTER TABLE ingestions DROP CONSTRAINT ingestions_tipo_documentacao_check;
    -- ALTER TABLE ingestions ADD CONSTRAINT ingestions_tipo_documentacao_check
    --   CHECK (tipo_documentacao IS NULL OR tipo_documentacao IN ('planning','daily','review','retrospectiva','commit','outro'));
    ```
  </action>
  <acceptance_criteria>
    - supabase_schema.sql linha da CHECK constraint contém a string `'retrospectiva'`
    - supabase_schema.sql linha da CHECK constraint contém a string `'commit'`
    - O bloco de migration comentado para ALTER TABLE está presente após os comentários existentes
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Adicionar campos_planning a submit_planning com novos Form fields</name>
  <files>docudata-backend/routers/sprint_docs.py</files>
  <read_first>
    - docudata-backend/routers/sprint_docs.py (função submit_planning, linhas 210-267 — signature e base_content atual; função submit_daily linhas 269-320 — padrão campos_daily a replicar)
  </read_first>
  <action>
    Na função `submit_planning` (linha 210), adicionar os seguintes Form params após `itens_backlog`:
    - `squad: Optional[str] = Form(None)`
    - `periodo_inicio: Optional[str] = Form(None)` — formato ISO date YYYY-MM-DD
    - `periodo_fim: Optional[str] = Form(None)` — formato ISO date YYYY-MM-DD
    - `horas_disponiveis: Optional[int] = Form(None)` — horas reais disponíveis do squad
    - `horas_estimadas: Optional[int] = Form(None)` — horas estimadas necessárias
    - `dependencias_cliente: Optional[str] = Form(None)`
    - `carry_over: Optional[str] = Form(None)` — itens não entregues da sprint anterior

    No `base_content` dict, adicionar a chave `"campos_planning"` com sub-dict:
    ```python
    "campos_planning": {
        "squad": squad or "",
        "periodo_inicio": periodo_inicio or "",
        "periodo_fim": periodo_fim or "",
        "horas_disponiveis": horas_disponiveis,
        "horas_estimadas": horas_estimadas,
        "dependencias_cliente": dependencias_cliente or "",
        "carry_over": carry_over or "",
    },
    ```
    Todos os campos são opcionais — a função não quebra se não enviados. Importar `Optional` se não estiver importado.
  </action>
  <acceptance_criteria>
    - `submit_planning` tem `squad: Optional[str] = Form(None)` na assinatura
    - `submit_planning` tem `carry_over: Optional[str] = Form(None)` na assinatura
    - `base_content` em submit_planning contém a chave `"campos_planning"` com sub-dict
    - `base_content["campos_planning"]` contém as 7 chaves: squad, periodo_inicio, periodo_fim, horas_disponiveis, horas_estimadas, dependencias_cliente, carry_over
    - Importar `Optional` no topo do arquivo se ainda não importado
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Adicionar campos_review a submit_review com novos Form fields</name>
  <files>docudata-backend/routers/sprint_docs.py</files>
  <read_first>
    - docudata-backend/routers/sprint_docs.py (função submit_review, linhas 407-460 — signature e base_content atual)
  </read_first>
  <action>
    Na função `submit_review` (linha 407), adicionar os seguintes Form params após `observacoes`:
    - `percepcao_cliente: Optional[str] = Form(None)` — frase do gerente sobre percepção do cliente
    - `sinal_satisfacao: Optional[str] = Form(None)` — um de: "🟢 Verde", "🟡 Amarelo", "🔴 Vermelho"
    - `pedidos_fora_escopo: Optional[str] = Form(None)` — pedidos recebidos fora do escopo da sprint

    No `base_content` dict, adicionar a chave `"campos_review"` com sub-dict:
    ```python
    "campos_review": {
        "percepcao_cliente": percepcao_cliente or "",
        "sinal_satisfacao": sinal_satisfacao or "",
        "pedidos_fora_escopo": pedidos_fora_escopo or "",
    },
    ```
    Todos os campos são opcionais — a função não quebra se não enviados.
  </action>
  <acceptance_criteria>
    - `submit_review` tem `percepcao_cliente: Optional[str] = Form(None)` na assinatura
    - `submit_review` tem `sinal_satisfacao: Optional[str] = Form(None)` na assinatura
    - `submit_review` tem `pedidos_fora_escopo: Optional[str] = Form(None)` na assinatura
    - `base_content` em submit_review contém a chave `"campos_review"` com sub-dict com 3 chaves
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Criar endpoint POST /sprint-docs/retrospectiva com campos_retrospectiva</name>
  <files>docudata-backend/routers/sprint_docs.py</files>
  <read_first>
    - docudata-backend/routers/sprint_docs.py (função submit_review linhas 407-460 — modelo a seguir; função _run_generation linhas 132-175 — como chamar geração; função _insert_ingestion linhas 176-208)
    - docudata-backend/services/google_docs.py (função _extract_section_replacements linhas ~254-293 — mapeamento de seções para entender como o doc de retrospectiva é gerado)
  </read_first>
  <action>
    Adicionar novo endpoint `@router.post("/retrospectiva", response_model=SprintDocResponse, status_code=201)` com async def `submit_retrospectiva`. Seguir o mesmo padrão de submit_review:

    Form params: `projeto_id: str = Form(...)`, `sprint_numero: int = Form(...)`, `observacoes: Optional[str] = Form(None)` (contexto geral), `pedido_fora_escopo_status: Optional[str] = Form(None)` (status do pedido fora do escopo: se foi aceito, recusado, postergado etc.), `anexo: Optional[UploadFile] = File(None)`

    `base_content`:
    ```python
    {
        "resumo": observacoes or f"Retrospectiva da Sprint {sprint_numero}",
        "tarefas": [], "decisoes": [], "problemas": [],
        "contexto_cliente": "", "proximos_passos": [], "tecnologias": [],
        "campos_retrospectiva": {
            "pedido_fora_escopo_status": pedido_fora_escopo_status or "",
        },
    }
    ```

    Chamar `_insert_ingestion` com `tipo_documentacao="retrospectiva"`, `file_name=f"retrospectiva-sprint-{sprint_numero}"`.
    Chamar `_run_generation` com `tipo_doc="retrospectiva"`, `ingestion_id=None` (igual ao review — busca todas as ingestions da sprint).
    Retornar `SprintDocResponse` com os campos do doc gerado.
  </action>
  <acceptance_criteria>
    - `sprint_docs.py` contém `@router.post("/retrospectiva"` 
    - A função `submit_retrospectiva` recebe `pedido_fora_escopo_status: Optional[str] = Form(None)`
    - `_insert_ingestion` é chamado com `tipo_documentacao="retrospectiva"`
    - `extracted_content` contém chave `"campos_retrospectiva"` com sub-dict `{"pedido_fora_escopo_status": ...}`
    - `_run_generation` é chamado com `tipo_doc="retrospectiva"`
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Verificação manual — rodar migration no Supabase e testar novos campos</name>
  <files>docudata-backend/supabase_schema.sql</files>
  <action>Executar migration de constraint no Supabase e verificar que os novos campos chegam ao banco.</action>
  <what-built>Novos Form fields nos endpoints de planning e review, endpoint de retrospectiva, constraint atualizada no SQL.</what-built>
  <how-to-verify>
    1. No Supabase SQL Editor, executar as duas linhas de migration (descomentadas):
       ```sql
       ALTER TABLE ingestions DROP CONSTRAINT ingestions_tipo_documentacao_check;
       ALTER TABLE ingestions ADD CONSTRAINT ingestions_tipo_documentacao_check
         CHECK (tipo_documentacao IS NULL OR tipo_documentacao IN ('planning','daily','review','retrospectiva','commit','outro'));
       ```
    2. `cd docudata-backend && uvicorn main:app --reload`
    3. Testar planning com novos campos:
       ```bash
       curl -X POST http://localhost:8000/sprint-docs/planning \
         -F "projeto_id=<uuid>" -F "sprint_numero=1" -F "descricao=Teste" \
         -F "squad=Backend" -F "periodo_inicio=2026-07-14" -F "periodo_fim=2026-07-28" \
         -F "horas_disponiveis=40" -F "horas_estimadas=35" \
         -F "dependencias_cliente=Acesso ao banco" -F "carry_over=Item anterior"
       ```
       Verificar no Supabase Table Editor que a ingestion criada tem `extracted_content.campos_planning.squad = "Backend"`.
    4. Testar review:
       ```bash
       curl -X POST http://localhost:8000/sprint-docs/review \
         -F "projeto_id=<uuid>" -F "sprint_numero=1" \
         -F "percepcao_cliente=Satisfeito" -F "sinal_satisfacao=🟢 Verde" \
         -F "pedidos_fora_escopo=Relatório PDF"
       ```
       Verificar `extracted_content.campos_review.sinal_satisfacao = "🟢 Verde"` no banco.
    5. Testar retrospectiva:
       ```bash
       curl -X POST http://localhost:8000/sprint-docs/retrospectiva \
         -F "projeto_id=<uuid>" -F "sprint_numero=1" \
         -F "pedido_fora_escopo_status=Aceito para próxima sprint"
       ```
       Verificar `extracted_content.campos_retrospectiva.pedido_fora_escopo_status` no banco e `tipo_documentacao = "retrospectiva"`.
  </how-to-verify>
  <resume-signal>Type "approved" ou descreva o erro encontrado.</resume-signal>
</task>

</tasks>

<verification>
- supabase_schema.sql CHECK constraint inclui 'retrospectiva' e 'commit'
- submit_planning armazena campos_planning com 7 chaves no JSONB
- submit_review armazena campos_review com 3 chaves no JSONB
- POST /sprint-docs/retrospectiva existe e armazena campos_retrospectiva com pedido_fora_escopo_status
- Todos os novos Form params são Optional — endpoints existentes não quebram sem eles
</verification>

<success_criteria>
- FORM-01: Modal de Planning coleta e armazena squad, período, horas, dependências, carry-over
- FORM-02: Modal de Review coleta e armazena percepção do cliente, sinal de satisfação, pedidos fora do escopo
- Retro: endpoint armazena pedido_fora_escopo_status (prerequisite para TMPL-03)
</success_criteria>
