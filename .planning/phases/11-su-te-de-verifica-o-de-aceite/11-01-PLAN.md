---
phase: 11-su-te-de-verifica-o-de-aceite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docudata-backend/supabase_schema.sql
  - docudata-backend/models/schemas.py
  - docudata-backend/routers/aceite_ingest.py
  - docudata-backend/routers/funcionalidades.py
  - docudata-backend/main.py
autonomous: true
requirements:
  - M5 (§5)
  - §4.4 (ExecucaoAceite)

estimate:
  tokens: 72000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - PATCH /funcionalidades/{id} com status=concluida retorna 200 imediatamente; ExecucaoAceite é inserida em background (per D-02)
    - POST /ingest/aceite com payload {funcionalidade_id, commit_sha, gates} atualiza gates e concluido_em na tabela execucoes_aceite (per D-05)
    - Se project.github_token ou project.github_repo estiverem ausentes, todos os 5 gates são registrados como sem_cobertura imediatamente — sem tentar dispatch (per D-03)
    - GET /execucoes_aceite/{project_id} retorna lista de ExecucaoAceite do projeto
    - github_token nunca aparece em nenhum response de API; ProjectResponse expõe has_github_config boolean (per D-Discretion)
  artifacts:
    - docudata-backend/routers/aceite_ingest.py (novo arquivo)
    - execucoes_aceite table existe no Supabase (migration aplicada manualmente pelo desenvolvedor)
    - FuncionalidadeResponse contém campo testes_e2e: list[str]
    - FuncionalidadeUpdate contém campo testes_e2e: Optional[list[str]]
    - ProjectResponse contém campo has_github_config: bool
  key_links:
    - patch_funcionalidade adiciona BackgroundTasks param e chama dispatch_aceite_background quando status transiciona para concluida
    - aceite_ingest.router registrado em main.py
    - dispatch_aceite_background usa urllib.request (stdlib) para POST api.github.com/repos/{owner}/dispatches
---

<objective>
Tracer end-to-end da suíte de aceite: migration SQL + schemas + endpoint POST /ingest/aceite + trigger BackgroundTasks no PATCH /funcionalidades + wiring no main.py + GET /execucoes_aceite/{project_id}.

Purpose: Provar o caminho completo — PATCH status=concluida cria ExecucaoAceite pendente, POST /ingest/aceite preenche o resultado — antes de construir o agente GitHub Actions.

Output: Backend completo da suíte de aceite. O agente CI (Wave 2) pode implementar aceite_agent.py sabendo exatamente qual payload enviar e qual endpoint chamar.
</objective>

<execution_context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/ROADMAP.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-CONTEXT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-RESEARCH.md
</context>

<tasks>

<task type="tracer">
  <name>Tracer: Migration SQL + schemas + POST /ingest/aceite ponta a ponta</name>
  <files>
    docudata-backend/supabase_schema.sql,
    docudata-backend/models/schemas.py,
    docudata-backend/routers/aceite_ingest.py,
    docudata-backend/main.py
  </files>

  <read_first>
    - docudata-backend/supabase_schema.sql — ver estrutura atual; adicionar execucoes_aceite, ALTER TABLE projects, ALTER TABLE funcionalidades ao final
    - docudata-backend/routers/revisao_ingest.py — template para aceite_ingest.py (padrão POST /ingest/*, Supabase insert, BaseModel, router sem prefix)
    - docudata-backend/models/schemas.py — ver FuncionalidadeResponse (linha 248), FuncionalidadeUpdate (linha 215), ProjectResponse (linha 42) para adicionar campos
    - docudata-backend/main.py — ver padrão de include_router
  </read_first>

  <action>
    1. SUPABASE_SCHEMA.SQL — Ao final do arquivo, adicionar bloco "Phase 11: Suíte de Verificação de Aceite":

    CREATE TABLE IF NOT EXISTS execucoes_aceite (
      id                uuid        PRIMARY KEY DEFAULT gen_random_uuid(),
      funcionalidade_id uuid        NOT NULL REFERENCES funcionalidades(id) ON DELETE CASCADE,
      project_id        uuid        NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
      commit_sha        text        NOT NULL,
      gates             jsonb       NOT NULL DEFAULT '[]'::jsonb,
      disparado_em      timestamptz NOT NULL DEFAULT now(),
      concluido_em      timestamptz
    );

    CREATE INDEX IF NOT EXISTS idx_execucoes_aceite_project
        ON execucoes_aceite (project_id, disparado_em DESC);

    CREATE INDEX IF NOT EXISTS idx_execucoes_aceite_func
        ON execucoes_aceite (funcionalidade_id, disparado_em DESC);

    ALTER TABLE projects
      ADD COLUMN IF NOT EXISTS github_token text,
      ADD COLUMN IF NOT EXISTS github_repo  text;

    ALTER TABLE funcionalidades
      ADD COLUMN IF NOT EXISTS testes_e2e text[] NOT NULL DEFAULT '{}';

    Adicionar comentário com instruções de migration incremental (padrão do arquivo).

    2. SCHEMAS.PY — Três edições cirúrgicas (per D-04, D-05, D-06, D-Discretion):

    a) Em FuncionalidadeUpdate (linha 215): adicionar campo
       testes_e2e: Optional[list[str]] = None
       (após o campo motivo existente)

    b) Em FuncionalidadeResponse (linha 248): adicionar campo
       testes_e2e: list[str] = []
       (após sprint_alvo, antes de created_at)

    c) Em ProjectResponse (linha 42): adicionar campo
       has_github_config: bool = False
       (após periodo_garantia_dias)

    d) Adicionar ao final do arquivo o novo schema:
       class ExecucaoAceitePayload(BaseModel):
           funcionalidade_id: str
           commit_sha: str
           gates: list[dict]  -- cada dict: {nome: str, resultado: str}

       class ExecucaoAceiteResponse(BaseModel):
           id: str
           funcionalidade_id: str
           project_id: str
           commit_sha: str
           gates: list[dict]
           disparado_em: datetime
           concluido_em: Optional[datetime] = None

    3. ACEITE_INGEST.PY — Criar novo arquivo seguindo o padrão de revisao_ingest.py:

    Imports: fastapi (APIRouter, HTTPException), models.schemas (ExecucaoAceitePayload), services.supabase_client (get_client), datetime (datetime, timezone)

    router = APIRouter(tags=["aceite-ingest"])

    POST /ingest/aceite (status_code=200):
    - Recebe ExecucaoAceitePayload per D-05
    - Busca execucoes_aceite pelo funcionalidade_id E commit_sha (filtro duplo per pitfall 4 do RESEARCH)
    - Se não encontrado: insert novo registro com gates e concluido_em=now()
    - Se encontrado: update com gates e concluido_em=now()
    - Retorna {"status": "ok", "funcionalidade_id": payload.funcionalidade_id}

    GET /execucoes_aceite/{project_id} (response_model=list[ExecucaoAceiteResponse]):
    - Busca execucoes_aceite WHERE project_id = project_id ORDER BY disparado_em DESC
    - Retorna lista (pode ser vazia)

    4. MAIN.PY — Adicionar na linha de imports e include_router:
    - Import: from routers import aceite_ingest
    - app.include_router(aceite_ingest.router)  — após revisao_ingest
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python -c "from routers import aceite_ingest; from models.schemas import ExecucaoAceitePayload, ExecucaoAceiteResponse, FuncionalidadeResponse, FuncionalidadeUpdate, ProjectResponse; f = FuncionalidadeResponse.__fields__; u = FuncionalidadeUpdate.__fields__; p = ProjectResponse.__fields__; assert 'testes_e2e' in f, 'testes_e2e missing from FuncionalidadeResponse'; assert 'testes_e2e' in u, 'testes_e2e missing from FuncionalidadeUpdate'; assert 'has_github_config' in p, 'has_github_config missing from ProjectResponse'; print('OK: schemas corretos')"
    </automated>
  </verify>

  <done>
    - supabase_schema.sql contém CREATE TABLE execucoes_aceite, ALTER TABLE projects (github_token, github_repo), ALTER TABLE funcionalidades (testes_e2e)
    - FuncionalidadeResponse.testes_e2e: list[str] existe
    - FuncionalidadeUpdate.testes_e2e: Optional[list[str]] existe
    - ProjectResponse.has_github_config: bool existe
    - aceite_ingest.py contém POST /ingest/aceite e GET /execucoes_aceite/{project_id}
    - main.py importa e registra aceite_ingest.router
    - python -c import verifica tudo sem erro
  </done>
</task>

<task type="auto">
  <name>BackgroundTasks dispatch em patch_funcionalidade (per D-01, D-02, D-03)</name>
  <files>
    docudata-backend/routers/funcionalidades.py
  </files>

  <read_first>
    - docudata-backend/routers/funcionalidades.py — ler patch_funcionalidade completo (linhas 162-229); ver onde a transição de status é detectada (linha 175-202)
    - docudata-backend/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-RESEARCH.md — Pattern 1 (dispatch_github_aceite) e Pattern 2 (BackgroundTasks), Pattern 3 (commit_sha via git rev-parse), Pattern 4 (Supabase jsonb insert)
  </read_first>

  <action>
    Editar funcionalidades.py com as seguintes mudanças cirúrgicas:

    1. IMPORTS: Adicionar BackgroundTasks ao import do fastapi:
       from fastapi import APIRouter, HTTPException, Query, BackgroundTasks

    2. FUNÇÃO dispatch_aceite_background — adicionar antes do router = APIRouter(...):
       Função síncrona (não async) que recebe funcionalidade_id: str, project_id: str.
       Corpo:
       a) Busca o projeto no Supabase: SELECT github_token, github_repo, name FROM projects WHERE id = project_id
       b) Busca testes_e2e da funcionalidade: SELECT testes_e2e FROM funcionalidades WHERE id = funcionalidade_id
       c) github_token = proj.get("github_token") ou ""; github_repo = proj.get("github_repo") ou ""
       d) Se github_token falsy OU github_repo falsy:
             — Obtém commit_sha via subprocess.run(["git", "rev-parse", "HEAD"], ...).stdout.strip() ou "unknown"
             — Insert em execucoes_aceite com gates=[{"nome": g, "resultado": "sem_cobertura"} for g in ("build","testes_unitarios","e2e","acessibilidade","performance")], commit_sha=commit_sha, concluido_em=datetime.now(timezone.utc).isoformat()
             — Retorna (sem tentar dispatch)
       e) Se configurado:
             — Obtém commit_sha via subprocess.run(["git", "rev-parse", "HEAD"], ...).stdout.strip() ou "unknown"
             — Insert em execucoes_aceite com gates=[], disparado_em=now — registro pendente
             — Monta client_payload: {funcionalidade_id, project_id, commit_sha, testes_e2e: testes_e2e[:50], api_url: os.environ.get("DOCUDATA_API_URL", "")}
             — Chama dispatch via urllib.request conforme Pattern 1 do RESEARCH: POST https://api.github.com/repos/{github_repo}/dispatches, headers Authorization Bearer + Accept vnd.github+json + X-GitHub-Api-Version 2022-11-28, body json.dumps({"event_type": "docudata-aceite", "client_payload": client_payload}), timeout=15
             — Se falhar (exception), loga via print mas NÃO propaga — função é best-effort

    3. HANDLER patch_funcionalidade — adicionar BackgroundTasks como parâmetro:
       async def patch_funcionalidade(funcionalidade_id: str, data: FuncionalidadeUpdate, background_tasks: BackgroundTasks):

    4. Após o bloco de transicoes_status (após linha 202, antes de "Build update payload"):
       Adicionar detecção da transição para concluida:
           novo_status = getattr(data, "status", None)
           status_anterior = func.get("status")
           if novo_status == "concluida" and status_anterior != "concluida":
               background_tasks.add_task(
                   dispatch_aceite_background,
                   funcionalidade_id=funcionalidade_id,
                   project_id=func["project_id"],
               )

    5. IMPORTS no topo do arquivo: adicionar import os, import subprocess, import json, import urllib.request, import urllib.error. Adicionar datetime.timezone ao import existente de datetime.

    IMPORTANTE: dispatch_aceite_background é função SÍNCRONA (def, não async def) — BackgroundTasks do FastAPI executa funções sync em threadpool automaticamente. Não usar asyncio.create_task (per RESEARCH pitfall 1).
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python -c "import ast, sys; src = open('routers/funcionalidades.py').read(); tree = ast.parse(src); names = [n.id for n in ast.walk(tree) if isinstance(n, ast.Name)]; assert 'BackgroundTasks' in src, 'BackgroundTasks ausente'; assert 'dispatch_aceite_background' in src, 'dispatch_aceite_background ausente'; assert 'background_tasks.add_task' in src, 'add_task ausente'; assert 'asyncio.create_task' not in src, 'asyncio.create_task NÃO deve ser usado'; print('OK: funcionalidades.py correto')"
    </automated>
  </verify>

  <done>
    - patch_funcionalidade recebe background_tasks: BackgroundTasks como terceiro parâmetro
    - dispatch_aceite_background existe como função síncrona no módulo
    - background_tasks.add_task é chamado quando novo_status == "concluida" e status_anterior != "concluida"
    - asyncio.create_task não aparece no arquivo
    - Se github_token/github_repo ausentes, insert execucoes_aceite com todos gates=sem_cobertura imediatamente
    - github_token nunca é retornado em nenhum response
  </done>
</task>

<task type="auto">
  <name>ProjectResponse: has_github_config + testes_e2e propagado no PATCH + projects router (per D-Discretion)</name>
  <files>
    docudata-backend/routers/projects.py
  </files>

  <read_first>
    - docudata-backend/routers/projects.py — ler get_project e list_projects para ver como ProjectResponse é montado; identificar se has_api_key é setado manualmente (buscar "has_api_key")
    - docudata-backend/models/schemas.py — ver ProjectResponse após edição da Task 1 (has_github_config adicionado)
  </read_first>

  <action>
    Em routers/projects.py, nos handlers que retornam ProjectResponse (get_project, list_projects, create_project, patch_project — todos que retornam dados de projeto):

    Para cada row de projeto retornada, garantir que has_github_config seja calculado antes de retornar:
    - has_github_config = bool(row.get("github_token")) and bool(row.get("github_repo"))
    - Remover github_token e github_repo do dict antes de construir o response (ou não os incluir no SELECT se aplicável)
    - Usar o mesmo padrão de has_api_key já existente no arquivo

    NUNCA incluir o campo github_token no response — apenas has_github_config: bool.

    Se o projeto usa SELECT("*") e Pydantic ignora campos extras, verificar que ProjectResponse não tem campo github_token declarado (a Task 1 só adicionou has_github_config). Se a Pydantic schema não tem o campo, o campo extra do Supabase é descartado automaticamente. Nesse caso, apenas garantir que has_github_config seja calculado corretamente.

    Também garantir que testes_e2e seja suportado no PATCH /funcionalidades/{id}: a Task 2 já garante FuncionalidadeUpdate.testes_e2e. Adicionar testes_e2e ao bloco "Build update payload" em funcionalidades.py se o campo não foi incluído na Task 2:
        val = getattr(data, "testes_e2e", None)
        if val is not None:
            updates["testes_e2e"] = val
    (Verificar se isso já foi coberto na Task 2; se sim, pular.)
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python -c "import ast; src = open('routers/projects.py').read(); assert 'github_token' not in src.replace('github_token', '').replace('\"github_token\"', '') or 'has_github_config' in src, 'github_token exposto sem has_github_config'; assert 'has_github_config' in src, 'has_github_config ausente em projects.py'; print('OK: projects.py seguro')"
    </automated>
  </verify>

  <done>
    - has_github_config: bool calculado a partir de (github_token AND github_repo) nos handlers de projeto
    - github_token nunca enviado para o cliente em nenhum response
    - testes_e2e incluído no bloco de updates do patch_funcionalidade (se não coberto pela Task 2)
    - python -c import de routers.projects não levanta erro
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI externo → POST /ingest/aceite | GitHub Actions envia payload sem autenticação — qualquer agente pode POST resultados fabricados |
| Backend → api.github.com | github_token sai do backend para GitHub API — nunca deve aparecer em logs ou responses |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-01 | Spoofing | POST /ingest/aceite — sem autenticação | medium | accept | MVP sem auth; os dados de aceite não alteram status de funcionalidade — apenas registram resultado informativo. Aceite consciente para MVP. |
| T-11-02 | Information Disclosure | github_token em ProjectResponse | high | mitigate | has_github_config: bool exposto; github_token nunca serializado — ProjectResponse não tem o campo declarado. Verificar com grep que "github_token" não aparece em nenhum response body. |
| T-11-03 | Tampering | gates jsonb — CI pode enviar resultado fabricado | low | accept | Gates são informativos, não bloqueiam nem revertem status. Impacto baixo no MVP. |
| T-11-SC | Tampering | npm/pip installs | low | accept | Esta fase não adiciona novas dependências Python ou npm. |
</threat_model>

<verification>
Checklist de verificação manual após execução:

1. Aplicar migration SQL no Supabase (SQL Editor): executar o bloco Phase 11 adicionado ao supabase_schema.sql
2. Verificar tabela execucoes_aceite criada: SELECT * FROM execucoes_aceite LIMIT 1
3. PATCH /funcionalidades/{id} com status=concluida retorna 200 imediatamente
4. Verificar linha criada em execucoes_aceite no Supabase após o PATCH
5. Simular POST /ingest/aceite com payload válido e verificar gates + concluido_em preenchidos
6. GET /execucoes_aceite/{project_id} retorna lista
7. GET /projects/{id} não contém campo github_token no response (inspecionar JSON)
</verification>

<success_criteria>
- Migration SQL adicionada e documentada no supabase_schema.sql
- python -c "from routers import aceite_ingest; from models.schemas import ExecucaoAceitePayload, FuncionalidadeResponse; assert 'testes_e2e' in FuncionalidadeResponse.__fields__" executa sem erro
- patch_funcionalidade usa BackgroundTasks (não asyncio.create_task)
- github_token ausente de todos os responses de projeto
</success_criteria>

<output>
Criar `.planning/phases/11-su-te-de-verifica-o-de-aceite/11-01-SUMMARY.md` ao concluir.
</output>
