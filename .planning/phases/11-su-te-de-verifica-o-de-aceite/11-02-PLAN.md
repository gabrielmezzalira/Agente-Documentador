---
phase: 11-su-te-de-verifica-o-de-aceite
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - docudata-backend/hooks/aceite_agent.py
  - docudata-backend/hooks/aceite.yml
  - docudata-backend/routers/painel.py
  - docudata-frontend/app/lib/api.ts
  - docudata-frontend/app/components/PainelTab.tsx
  - docudata-frontend/app/projects/[id]/page.tsx
autonomous: true
requirements:
  - M5 (§5)
  - §4.4 (ExecucaoAceite)

estimate:
  tokens: 80000
  raw_tokens: 40000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - aceite_agent.py roda gates via subprocess (build, testes_unitarios, e2e, acessibilidade, performance) e envia POST /ingest/aceite ao DocuData (per D-01, D-04)
    - aceite.yml dispara em repository_dispatch event_type docudata-aceite e é best-effort (continue-on-error true) — nunca quebra CI do projeto (per D-01)
    - GET /projects/{id}/painel retorna cobertura_aceite: float e bloco_b contém funcionalidades_com_aceite_falhando (per D-09, D-10)
    - Kanban coluna Concluído exibe badge vermelho/ambar quando execucao_aceite tem gate falhou ou erro (per D-08, zero className)
    - Bloco B do Painel exibe sub-seção Cobertura de Aceite com funcionalidades concluídas com suíte falhando (per D-09, zero className)
    - cobertura_aceite não altera pct_escopo_concluido do Bloco A (per success criteria 3 do ROADMAP)
  artifacts:
    - docudata-backend/hooks/aceite_agent.py (novo script Python stdlib)
    - docudata-backend/hooks/aceite.yml (novo workflow GitHub Actions)
    - painel.py: get_painel retorna cobertura_aceite no response root
    - api.ts: interface ExecucaoAceite exportada + getExecucoesAceite(projectId) function
    - PainelTab.tsx: Bloco B expandido + KanbanCard recebe execucoesAceite map
  key_links:
    - PainelTab carrega execucoesAceite em paralelo com getPainel e listFuncionalidades
    - KanbanCard recebe Map<funcionalidade_id, ExecucaoAceite> e renderiza badge se temFalha
    - calcular_bloco_b recebe execucoes_aceite list e calcula cobertura_aceite
---

<objective>
Expansion: agente GitHub Actions (aceite_agent.py + aceite.yml) + expansão do Painel (cobertura_aceite no backend, Bloco B + badge Kanban no frontend).

Purpose: Completar a suíte de aceite — o CI do projeto pode reportar resultados, e o gerente vê o status diretamente no Painel e no Kanban.

Output: Sistema completo — from PATCH status=concluida até badge visual no Kanban.
</objective>

<execution_context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.claude/gsd-core/workflows/execute-plan.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/ROADMAP.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-CONTEXT.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-RESEARCH.md
@/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>aceite_agent.py + aceite.yml — agente GitHub Actions (per D-01, D-04, D-06)</name>
  <files>
    docudata-backend/hooks/aceite_agent.py,
    docudata-backend/hooks/aceite.yml
  </files>

  <read_first>
    - docudata-backend/hooks/revisor_agent.py — padrão completo: stdlib only, http_json helper, best-effort, SystemExit(0) em casos normais
    - docudata-backend/hooks/revisor.yml — template de workflow: jobs, continue-on-error, env, permissions
    - docudata-backend/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-RESEARCH.md — Pattern 5 (aceite.yml), Pattern 6 (aceite_agent.py), Pitfall 2 (repository_dispatch no default branch), Pitfall 3 (commit SHA via git rev-parse HEAD)
  </read_first>

  <action>
    1. ACEITE_AGENT.PY — Criar docudata-backend/hooks/aceite_agent.py:

    Docstring no topo explicando instalação (mesma estrutura de revisor_agent.py):
      mkdir -p .github/workflows scripts
      cp aceite_agent.py scripts/aceite_agent.py
      cp aceite.yml .github/workflows/aceite.yml

    Secrets necessários no repo do projeto:
      DOCUDATA_API_URL  — ex: https://docudata-backend.railway.app
      (funcionalidade_id, project_id, testes_e2e chegam via env injetado pelo workflow)

    Imports: os, subprocess, json, urllib.request, urllib.error (stdlib apenas)

    Variáveis de ambiente:
      API_URL            = os.environ.get("DOCUDATA_API_URL", "").rstrip("/")
      FUNCIONALIDADE_ID  = os.environ.get("FUNCIONALIDADE_ID", "")
      PROJECT_ID         = os.environ.get("PROJECT_ID", "")
      TESTES_E2E_JSON    = os.environ.get("TESTES_E2E", "[]")

    Guard: se API_URL ou FUNCIONALIDADE_ID vazios, print aviso e SystemExit(0).

    Helper run_cmd(cmd: list[str]) -> tuple[int, str]:
      subprocess.run(cmd, capture_output=True, text=True, timeout=120)
      Retorna (returncode, stdout+stderr)

    Helper gate_resultado(cmd: list[str] | None) -> str:
      Se cmd is None: return "sem_cobertura"
      try: rc, _ = run_cmd(cmd); return "passou" if rc == 0 else "falhou"
      except Exception: return "erro"

    Obter commit_sha:
      subprocess.run(["git", "rev-parse", "HEAD"], capture_output=True, text=True).stdout.strip() ou "unknown"
      (per RESEARCH Pitfall 3 — usar git rev-parse HEAD do repo do projeto, não GITHUB_SHA)

    Detectar runtime (heurística best-effort):
      has_package_json = os.path.exists("package.json")
      has_requirements = os.path.exists("requirements.txt") or os.path.exists("pyproject.toml")

    Parsear testes_e2e:
      testes_e2e: list[str] = json.loads(TESTES_E2E_JSON) if TESTES_E2E_JSON else []
      (tratar exception de parse com fallback para lista vazia)

    Construir lista de gates (per D-04 — 5 gates fixos):
      build: gate_resultado(["npm","run","build"] se has_package_json else ["pip","install","-e","."] se has_requirements else None)
      testes_unitarios: gate_resultado(["npm","test","--","--watchAll=false"] se has_package_json else ["pytest","--tb=short","-q"] se has_requirements else None)
      e2e: "sem_cobertura" se testes_e2e vazio; else gate_resultado(["python","-m","pytest"] + testes_e2e)
      acessibilidade: "sem_cobertura"  — MVP
      performance: "sem_cobertura"  — MVP

    Reportar ao DocuData via urllib.request:
      payload = {"funcionalidade_id": FUNCIONALIDADE_ID, "commit_sha": commit_sha, "gates": gates}
      POST {API_URL}/ingest/aceite, Content-Type: application/json, timeout=30
      Log resultado (status 200 = ok). Se exception: print aviso e continuar (best-effort).

    2. ACEITE.YML — Criar docudata-backend/hooks/aceite.yml:

    name: DocuData Aceite

    on:
      repository_dispatch:
        types: [docudata-aceite]

    jobs:
      aceite:
        runs-on: ubuntu-latest
        continue-on-error: true   — best-effort, nunca quebra CI do projeto
        permissions:
          contents: read
        steps:
          - uses: actions/checkout@v4
            with:
              fetch-depth: 1
          - name: Executar suíte de aceite
            continue-on-error: true
            env:
              DOCUDATA_API_URL:      ${{ secrets.DOCUDATA_API_URL }}
              FUNCIONALIDADE_ID:     ${{ github.event.client_payload.funcionalidade_id }}
              PROJECT_ID:            ${{ github.event.client_payload.project_id }}
              TESTES_E2E:            ${{ toJson(github.event.client_payload.testes_e2e) }}
            run: python scripts/aceite_agent.py

    Adicionar comentário no topo (mesmo padrão de revisor.yml) explicando que o workflow deve estar no default branch do repo do projeto (per RESEARCH Pitfall 2).
  </action>

  <verify>
    <automated>
      python3 -c "import ast; ast.parse(open('/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/hooks/aceite_agent.py').read()); print('OK: aceite_agent.py sintaxe válida')" && python3 -c "src = open('/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend/hooks/aceite_agent.py').read(); banned = ['import requests','import httpx','import aiohttp','asyncio']; found = [b for b in banned if b in src]; assert not found, f'Dependências não-stdlib encontradas: {found}'; print('OK: stdlib only')"
    </automated>
  </verify>

  <done>
    - aceite_agent.py parseia sintaticamente sem erro
    - aceite_agent.py usa apenas stdlib (os, subprocess, json, urllib.request)
    - aceite.yml tem on.repository_dispatch.types: [docudata-aceite] e continue-on-error: true
    - 5 gates presentes: build, testes_unitarios, e2e, acessibilidade, performance
    - commit_sha obtido via git rev-parse HEAD (não GITHUB_SHA)
    - testes_e2e vazio → gate e2e = sem_cobertura
  </done>
</task>

<task type="auto">
  <name>painel.py: cobertura_aceite + funcionalidades_com_aceite_falhando no Bloco B (per D-09, D-10)</name>
  <files>
    docudata-backend/routers/painel.py
  </files>

  <read_first>
    - docudata-backend/routers/painel.py — ler calcular_bloco_b (linha 69-144) e get_painel (linha 293-337) por completo para entender a estrutura atual
    - docudata-backend/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-CONTEXT.md — D-09 (sub-seção Bloco B), D-10 (cobertura_aceite float no response do painel), decisão que cobertura_aceite não afeta pct_escopo_concluido
  </read_first>

  <action>
    1. CALCULAR_BLOCO_B — Adicionar parâmetro execucoes_aceite: list[dict] = [] à assinatura:
       def calcular_bloco_b(funcs, transicoes, revisao_recente=None, execucoes_aceite=None):

       Ao final do corpo (após calcular travadas/aguardando_cliente/em_ajuste/achados_criticos):

       Calcular funcionalidades_com_aceite_falhando:
       - Construir mapa: aceite_por_func = {ea["funcionalidade_id"]: ea for ea em execucoes_aceite ou []}
       - Para cada f em funcs onde f["status"] == "concluida":
             ea = aceite_por_func.get(f["id"])
             se ea e ea.get("concluido_em"):
                 gates = ea.get("gates") ou []
                 tem_falha = any(g.get("resultado") in ("falhou","erro") for g in gates)
                 se tem_falha: adicionar {"id": f["id"], "titulo": f["titulo"]} à lista

       Adicionar funcionalidades_com_aceite_falhando ao dict de retorno.

    2. CALCULAR_COBERTURA_ACEITE — Nova função local (não exposta):
       def _calcular_cobertura_aceite(funcs, execucoes_aceite) -> float | None:
       - concluidas = [f para f em funcs se f["status"] == "concluida"]
       - se não concluidas: return None
       - aceite_por_func = {ea["funcionalidade_id"]: ea para ea em execucoes_aceite}
       - com_cobertura = sum(1 para f em concluidas se f["id"] em aceite_por_func e aceite_por_func[f["id"]].get("concluido_em"))
       - return round(com_cobertura / len(concluidas) * 100, 1)

    3. GET_PAINEL — Expandir para buscar execucoes_aceite:
       Após buscar trans_list, adicionar:

       execucoes_resp = (
           client.table("execucoes_aceite")
           .select("funcionalidade_id, commit_sha, gates, disparado_em, concluido_em")
           .eq("project_id", project_id)
           .order("disparado_em", desc=True)
           .execute()
       )
       execucoes_aceite_list: list[dict] = execucoes_resp.data or []

       — Para evitar duplicatas (múltiplas execucoes por funcionalidade), dedupe mantendo apenas a mais recente por funcionalidade_id (a query já ordena DESC, então tomar o primeiro de cada funcionalidade_id em Python).

       Passar execucoes_aceite_list para calcular_bloco_b:
       bloco_b = calcular_bloco_b(func_list, trans_list, revisao_recente, execucoes_aceite=execucoes_aceite_list_deduped)

       Calcular cobertura_aceite:
       cobertura_aceite = _calcular_cobertura_aceite(func_list, execucoes_aceite_list_deduped)

       Adicionar ao response final:
       return {
           "bloco_a": bloco_a,
           "bloco_b": bloco_b,
           "bloco_c": bloco_c,
           "bloco_d": bloco_d,
           "cobertura_aceite": cobertura_aceite,   — float ou None (per D-10)
       }

    IMPORTANTE: cobertura_aceite é campo separado no response root, não dentro de bloco_a — per D-10. O campo pct_escopo_concluido em bloco_a não é alterado.
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-backend" && python -c "import ast; src = open('routers/painel.py').read(); ast.parse(src); assert 'execucoes_aceite' in src, 'execucoes_aceite ausente'; assert 'cobertura_aceite' in src, 'cobertura_aceite ausente'; assert 'funcionalidades_com_aceite_falhando' in src, 'funcionalidades_com_aceite_falhando ausente'; print('OK: painel.py expandido')"
    </automated>
  </verify>

  <done>
    - calcular_bloco_b aceita parâmetro execucoes_aceite e retorna funcionalidades_com_aceite_falhando
    - get_painel busca execucoes_aceite do Supabase e deduplica por funcionalidade_id (mais recente)
    - Response de GET /projects/{id}/painel inclui campo raiz cobertura_aceite: float | None
    - pct_escopo_concluido em bloco_a não é alterado
    - python parse de painel.py sem erro
  </done>
</task>

<task type="auto">
  <name>api.ts + PainelTab.tsx + page.tsx: badge Kanban + Bloco B Cobertura de Aceite (per D-08, D-09, zero className)</name>
  <files>
    docudata-frontend/app/lib/api.ts,
    docudata-frontend/app/components/PainelTab.tsx,
    docudata-frontend/app/projects/[id]/page.tsx
  </files>

  <read_first>
    - docudata-frontend/app/lib/api.ts — ver BlocoB (linha 684), PainelData (linha 718), listFuncionalidades. Entender onde adicionar ExecucaoAceite interface e getExecucoesAceite()
    - docudata-frontend/app/components/PainelTab.tsx — ler KanbanCard (linhas 473-510) e o useEffect/Promise.all (linha 522+) para entender como adicionar a chamada getExecucoesAceite em paralelo. Ler a renderização do Bloco B para entender onde inserir sub-seção.
    - docudata-backend/.planning/phases/11-su-te-de-verifica-o-de-aceite/11-RESEARCH.md — Pattern 7 (badge zero className), D-08 (zero className obrigatório)
  </read_first>

  <action>
    1. API.TS — Adicionar ao final (após confirmarPlanning):

    a) Interface ExecucaoAceite:
       export interface ExecucaoAceite {
         funcionalidade_id: string;
         commit_sha: string;
         gates: { nome: string; resultado: string }[];
         disparado_em: string;
         concluido_em: string | null;
       }

    b) Adicionar cobertura_aceite ao PainelData:
       No interface PainelData existente, adicionar:
         cobertura_aceite?: number | null;

    c) Adicionar funcionalidades_com_aceite_falhando ao BlocoB:
       No interface BlocoB existente, adicionar:
         funcionalidades_com_aceite_falhando?: Array<{ id: string; titulo: string }>;

    d) Função getExecucoesAceite:
       export async function getExecucoesAceite(projectId: string): Promise<ExecucaoAceite[]> {
         const res = await fetch(`${API}/execucoes_aceite/${projectId}`);
         if (!res.ok) return [];   — falha silenciosa; badge é informativo
         return res.json();
       }

    2. PAINELTAB.TSX — Quatro edições:

    a) Import: adicionar getExecucoesAceite e ExecucaoAceite ao import de api.ts

    b) State: adicionar dentro de PainelTab:
       const [execucoesAceite, setExecucoesAceite] = useState<ExecucaoAceite[]>([]);

    c) useEffect Promise.all: expandir para 3 chamadas paralelas:
       Promise.all([getPainel(projectId), listFuncionalidades(projectId), getExecucoesAceite(projectId)])
         .then(([d, funcs, execs]) => {
           setData(d);
           setFuncionalidades(funcs);
           setExecucoesAceite(execs);
           ...
         })

    d) KanbanCard — adicionar prop execucaoAceite: ExecucaoAceite | null ao componente KanbanCard:
       function KanbanCard({ f, allSprints, execucaoAceite }: { f: FuncionalidadeResponse; allSprints: string[]; execucaoAceite: ExecucaoAceite | null })

       Dentro do card (após os chips de prioridade e sprint), adicionar badge APENAS quando status=concluida E execucaoAceite existe E temFalha:
         const temFalha = execucaoAceite?.concluido_em != null && (execucaoAceite.gates ?? []).some(
           (g) => g.resultado === "falhou" || g.resultado === "erro"
         );
         if (temFalha) renderizar:
           <span style={{ display:"inline-flex", alignItems:"center", padding:"2px 7px", borderRadius:999, fontSize:11, fontWeight:600, background:"#fee2e2", color:"#dc2626", marginLeft:4 }}>
             ⚠ aceite
           </span>
       ZERO className — apenas style={{}} (per D-08).

    e) Na renderização do Kanban (coluna Concluído), passar execucaoAceite ao KanbanCard:
       Construir mapa antes do JSX:
         const aceiteMap = new Map(execucoesAceite.map(ea => [ea.funcionalidade_id, ea]));
       No KanbanCard de funcionalidades concluídas:
         execucaoAceite={aceiteMap.get(f.id) ?? null}
       Nas outras colunas passar execucaoAceite={null} (badge não aparece).

    f) Bloco B — Sub-seção "Cobertura de Aceite": Dentro do componente que renderiza bloco_b, adicionar após as sub-seções existentes (travadas, aguardando_cliente, em_ajuste, achados_criticos):
       Se bloco.funcionalidades_com_aceite_falhando existir e length > 0:
         Renderizar sub-seção com título "Cobertura de Aceite" usando subSectionTitleStyle existente.
         Lista de funcionalidades concluídas com suíte falhando: apenas id e titulo.
         ZERO className — apenas style={{}} (per D-09).

    g) Bloco A — Adicionar cobertura_aceite se disponível:
       Em PainelTab, se data.cobertura_aceite != null, exibir no painel abaixo do Bloco A (ou como métrica extra dentro do Bloco A):
         "% com cobertura de aceite: {data.cobertura_aceite}%"
       ZERO className.

    3. PAGE.TSX — Verificar se a página passa sprints para PainelTab corretamente. Nenhuma alteração estrutural esperada neste arquivo; apenas confirmar que o componente PainelTab está importado e recebe projectId.
  </action>

  <verify>
    <automated>
      cd "/Users/gabrielmezzalira/Documents/Faculdade/CIti/Liderança Dados/Agentes/Documentador/docudata-frontend" && npx tsc --noEmit 2>&1 | head -30
    </automated>
  </verify>

  <done>
    - api.ts exporta ExecucaoAceite interface e getExecucoesAceite() function
    - BlocoB interface tem funcionalidades_com_aceite_falhando
    - PainelData interface tem cobertura_aceite?: number | null
    - KanbanCard recebe execucaoAceite prop e renderiza badge ⚠ aceite com style={{}} quando temFalha
    - Bloco B exibe sub-seção Cobertura de Aceite listando funcionalidades concluídas com falha
    - Nenhum className em nenhum elemento novo (zero className per D-08, D-09)
    - npx tsc --noEmit passa sem erros de tipo nos arquivos modificados
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repo do projeto → DocuData | aceite_agent.py faz POST /ingest/aceite sem autenticação |
| DocuData backend → api.github.com | usa github_token — já mitigado na Wave 1 (nunca exposto) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-11-04 | Denial of Service | aceite_agent.py subprocess timeout | low | mitigate | Cada gate tem timeout=120s no run_cmd; o job inteiro tem continue-on-error true — nunca bloqueia o CI do projeto |
| T-11-05 | Spoofing | aceite.yml — qualquer repositório com DOCUDATA_API_URL pode POST /ingest/aceite | medium | accept | MVP sem auth; resultado é informativo, não altera status. Aceite consciente. |
| T-11-SC | Tampering | npm/pip installs | low | accept | Nenhuma nova dependência adicionada nesta fase — stdlib Python e npm tsc nativo. |
</threat_model>

<verification>
Checklist de verificação manual após execução:

1. Instalar aceite_agent.py + aceite.yml em um repo de teste:
   - cp hooks/aceite_agent.py scripts/aceite_agent.py
   - cp hooks/aceite.yml .github/workflows/aceite.yml
   - Configurar GitHub Secrets: DOCUDATA_API_URL
2. Trigger manual via GitHub API: POST /repos/{owner}/{repo}/dispatches com event_type docudata-aceite
3. Verificar que o workflow roda e POST /ingest/aceite chega ao backend
4. GET /projects/{id}/painel → verificar cobertura_aceite no response
5. No Painel frontend: verificar sub-seção Cobertura de Aceite no Bloco B com funcionalidades falhando
6. No Kanban coluna Concluído: badge ⚠ aceite aparece em funcionalidade com gate falhou/erro
7. Inspecionar DOM: zero className em todos os elementos novos
8. Verificar que pct_escopo_concluido em bloco_a NÃO muda com dados de aceite
</verification>

<success_criteria>
- aceite_agent.py passa em python3 -c "import ast; ast.parse(open('...').read())" e usa apenas stdlib
- aceite.yml tem on.repository_dispatch.types: [docudata-aceite] e continue-on-error: true
- GET /projects/{id}/painel retorna cobertura_aceite no response root
- npx tsc --noEmit sem erros nos arquivos modificados
- Zero className em todos os elementos HTML/JSX adicionados
</success_criteria>

<output>
Criar `.planning/phases/11-su-te-de-verifica-o-de-aceite/11-02-SUMMARY.md` ao concluir.
</output>
